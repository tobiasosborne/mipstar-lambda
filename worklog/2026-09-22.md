# 2026-09-22 — session 5 (cloud container)
- Environment: Julia 1.12.7 (official tarball, /opt/julia), bd 1.3.0 (npm @beads/bd; metrics off; no database in the clone — `.beads/dolt/` is gitignored; no GitHub issues). Manifest.toml gitignored (stdlib-only deps).
- Found: main (5f2e091) does not precompile from a clean clone — `precompile_compress.jl` included but never committed. Restored from wip/tb7-brief80; main then 12337/12338 (O2 wall only).
- Merged wip/tb7-brief80; ran tb6b (a)–(m), tb6a, tb5, tb7 standalone for the first time. Fixed: kernel -O0 standalone (module + @optlevel 2), lower_sampler Lambda(8)→Lambda(7) (RED SortError(:apply_arity) shown; mutant M7-lower-sampler-arity), BitVector in the metered nested TypedDecider, TB6b (m) BitVector args, TB6b (k) FuelExhausted-vs-rejection expectation (DESIGN 11.4 wins; probed boundaries 22632/22618/22617), runner sandbox for test-file mutants, two stale TB5 evidence strings.
- Gates: TB5/TB6b K's set from one in-suite run here (advisory; no governor). DESIGN §12.1 D13: not binding. §9.5/§9.6 sentences, 46→58, C12/C13 verdict edits landed.
- Lesson: a mutant scored KILLED-BY-CRASH can be a load failure of the TEST file (the runner prints MUTANT_TEST_STARTED before including it). Two of the five gate red witnesses were false kills until the sandbox fix.
- Lesson: `falses(n)` is a BitVector; the description primitives' contracts and several internal signatures require Vector{Bool}.
