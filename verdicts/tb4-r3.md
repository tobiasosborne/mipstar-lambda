# TB4 r3 — C11 stale-clause ruling; archived `2eff253`

1. **Registry count:** `test/mutations/tb4_compress.jl` defines and registers **33 distinct TB4 mutants**, on **9 distinct targets**. All 33 produced actual `Test Failed` evidence and all 9 baselines passed. The shared suite passed `12500/12500`; the whole registry exited 1 because the TB0 full-suite baseline hit its absolute wall ceiling. That unrelated failure does not affect TB4's targets. Runs/load and the N8 scoring defect elsewhere are recorded in `verdicts/tb6-r3.md`.
2. **Both former NOT-red-capable items are owned.** `M-fuelbound-unlocated` fails `tb4_compress_ir.jl:318` on `facts.source == "gt-12-compression.tex" && facts.lines == 448:449`, plus the two step-5 text checks and display/range match (40 pass, 4 fail, 0 errors). `M-stub-misnamed` fails the exact leading-display check `occursin("is COMPRESS_STUB, the constant", …)` at line 230 and the identity-compressor checks (33 pass, 3 fail). `M-stub-symbol-misnamed` fails `facts.compressor == :COMPRESS_STUB` at line 229 and the opposite identity fact (34 pass, 2 fail). Each is KILLED in the required registry. Their registered `expected_evidence` fields are **nothing**; the strings just quoted are the actual failed-assertion evidence, independently read from preserved process logs, not invented registry filters.
3. **Gate:** the archive's `CALIBRATED_GATES` has no TB4 entry. The authoritative value is `TB4_RATIO = floor(6.0 / 0.154) = 38` in `test/tb4_compress_ir.jl:39–41`; the in-suite assertion is at line 720. This run printed `10.006 s / 0.6073 s = 16.5 < 38`, with an enforced budget of `23.08 s` at that rate. At the reference rate the exact budget is `38·0.154 = 5.852 s`; the old 42 and 6.5–8.0 s clause is withdrawn. Both numerator/denominator gate mutants were assertion-killed.
4. **Necessary adjacent citation correction:** these same repairs also make C11's “7 of 9” / “two display-only” / “no SOURCE_REPAIR citation grepped” sentence false. Lines 415–431 pin all 9 CITED locations and the tenth located node, `HaltDeciderFuelBound`; lines 318–323 check its source text. The full replacement below includes this directly verified consequence and changes no theorem status.

**C11 — AUTHORIZE the following complete row verbatim; status remains TESTED.**

| C11 | (Compress skeleton — composition, contracts and the quoted fixed point) On the TB4 fixture `V = (S_λ stub, D_{M,λ})` at λ = 1024, with `\|D_{M,λ}\| = 388` bytes and fnv1a64 `dbf2c0815358657f` (critic-reserialized independently from the DESIGN §1.1 codec in `verdicts/tb4-r1.md` §1 and again in `verdicts/tb4-r2.md` §2), `Compress = Repeat ∘ AnswerReduce ∘ Introspect` builds the three ASSUME/PROVE contracts in fig:compress order (`gt-12-compression.tex:L75-L98`) with level chain 9→5→7→9 (`introspect_levels(9)=5`, `answer_reduce_levels(5)=max(7,5)=7`, `repeat_levels(7)=9`). The ORDER conjunct is enforced by the certificate's own `:LevelChain` replay and is red-capable: a hand-built Introspect→Repeat→AnswerReduce chain has the identical levels `[9,5,7,9]` (at ℓ = 5 both rules give 7, so levels alone cannot witness the swap) and the node's replay refuses it with `rule=:level_chain, actual=[9,5,7,9]` (`M-order-blind`, the critic's CRITIC-2 verbatim, KILLED). fig:compress's literal ℓ = 9 is pinned only as a certificate FACT that the test file asserts — `Compress` of a 5-level verifier records `levels = 5, ell = 9 => FAIL` at `ell_level` (`M-ell-from-input`, CRITIC-1 verbatim, KILLED) — while `verify_certificate` refuses at `nine_level` under both the constant and the input's own level, so no verification OUTCOME distinguishes them. Composed runtime bounds have free parameters exactly {n, λ} after binding ℓ = 9, the toy literals μ = γ = τ = 1, and `c'` and `\|D1\|` to prose descriptions of unexposed constants (`bind_parameter` ignores an absent name and accepts any host value, so that closure is partly a naming fact, `verdicts/tb4-r1.md` N2); the composed sampler-DEPENDENCY-SYMBOL set is ⊆ {λ, ℓ, μ, γ, τ, \|D1\|} and contains no component of `V` (no sampler bytes exist at TB4: the output is a `StubVerifier`; DESIGN §12.3's byte/hash form is TB5's `S^rep`), and in the form TB4 supports, `Compress` of the two byte-distinct 388-byte inputs at one λ yields 71-line traces differing in exactly one line — the input decider's `Quote` hash `dbf2c0815358657f` vs `2b9de4fb8ea9b2f9`, both critic-reserialized — with equal dependency symbols (`M-independence-leak` KILLED). The executable evidence is CHECKED Quote/Specialize on `D_{M,λ}` (host-side specialization; size law `385 − 31 − 2·22 + 31 + 2·15 + c_fix(5) = 376` with `c_fix = 5` bytes, critic-recomputed; both `specialize`'s and `fix_specialize`'s nodes are identity-bound and recompute the substitution by `program_equal`, `M-specialize-open`/`M-specialize-unbound` KILLED) and the TB0/TB2/TB3 PCP and typed-answer-reduction subtrees **on the 33-byte trivial front-end fixture**, every one of which is a DESCENDANT of `[ASSUMED] AnswerReduceSurrogate` (critic-verified by tree walk: no node under `:AnswerReduce` outside the contract nodes lies outside it; 20 of the tree's 30 CHECKED nodes are below it) with σ = 33 passed explicitly (`M-surrogate-sibling` KILLED), the front end's Tseitin reproduction being the CONSTRUCTED child `:UpstreamReproduction` of the identity-anchored `:UpstreamEvidence` (`M-reproduction-undisclosed` KILLED). The certificate is 70 nodes (CONSTRUCTED 7, CHECKED 30, CITED 9, ASSUMED 19, SOURCE_REPAIR 5); every CITED leaf carries `replay === nothing`; all 9 CITED nodes carry `source`/`lines`/`label` and each label is grepped inside its cited range, including `:AnswerReduceQuantumContract` (`thm:ar`) and `:CookLevinGeneral` (`prop:standard-succinct-sat`), as is every one of the 12 `\label`s named by a hypothesis source; the `source`/`lines` facts of `SOURCE_REPAIR(:HaltDeciderFuelBound)` are pinned to `gt-12-compression.tex:L448-L449`, whose step-5 text is grepped, and the display must name that same range; a violated checkable hypothesis (\|V\| > λ; wrong ℓ; a Concrete TIME > n^λ at n = 2) is an ASSUMED FAIL node that `verify_certificate` rejects at that hypothesis; and every Introspect and Compress hypothesis is scoped "(completeness/soundness only)" per gt-08:L789-L803 / gt-12:L26-L53, thm:ar's and thm:repetition's hypotheses being correctly unscoped. **Scope:** three stubs stand in for the construction, all three disclosed in the tree — `S_λ` (`λx.x`, 34 bytes), the `Introspect` and `Repeat` stages (CITED, no execution), and the `Compress` program inlined in `D_{M,λ}` itself (`[ASSUMED] CompressStubInTerm`: `COMPRESS_STUB`, 46 of 376 term bytes critic-recomputed, the constant `(pair,λ) ↦ Quote(λnxyab.true)`), so the "compressed branch" evaluated in testset (b) is that constant; and the outer `Eval` runs the returned decider under an enforced `FuelBound(n,λ)` where `fig:halt_f` step 5 (`gt-12-compression.tex:L448-L449`) imposes no budget and `TIME_{D^halt}(n) ≤ n^λ` is `lem:lambda`'s conclusion (`L570-L638`) — `SOURCE_REPAIR(:HaltDeciderFuelBound)`, the verifier's time bound reading "enforced by construction, not measured", never binding on the fixtures (12 and 91 units at n = 2). Red-capable by 33/33 registered TB4 mutants (`test/mutations/tb4_compress.jl`, all KILLED with 9/9 TB4 baselines OK); which compressor the `CompressStubInTerm` disclosure names is pinned in both its display and symbol fact (`M-stub-misnamed`, `M-stub-symbol-misnamed` KILLED), and the `source`/`lines` facts of `SOURCE_REPAIR(:HaltDeciderFuelBound)` are red-covered by `M-fuelbound-unlocated` (KILLED). The in-suite body is gated at `elapsed / TB4_CALIBRATION < 38`, where `38 = floor(6.0 / 0.154)` is defined by `TB4_RATIO` in `test/tb4_compress_ir.jl` against its GF(8) kernel timed in-process; the enforced budget is `38 * TB4_CALIBRATION` seconds (5.852 s at the 0.154 s reference rate), and `M-gate-body-inflated` and `M-gate-uncalibrated` are KILLED. | TESTED | C9, C10, C16, C18 | `src/compress.jl`, `src/ir/programs.jl` | `test/tb4_compress_ir.jl`; red: `test/mutations/tb4_compress.jl` | `verdicts/tb4-r2.md`; `verdicts/tb4-r3.md` (stale clauses corrected) |

VERDICT: PASS
