\begin{tikzpicture}[node distance=7mm and 9mm]
  % ---- the concrete two-variable run, boxed by a fitted panel
  \node[descbox] (f) {$f=x^2y-xy$};
  \node[stage, right=7mm of f] (expand) {$xy-y\,x(1-x)-xy$};
  \node[checked, right=7mm of expand] (result)
    {$f=(-y)\operatorname{zero}(x)+0\operatorname{zero}(y)+\boxed{0}$};
  \draw[xform] (f) -- (expand);
  \draw[xform] (expand) -- (result);
  \node[tag check, below=2mm of result] (rz) {REMAINDER ZERO};
  \node[panel title, anchor=south west] (pt)
    at ([yshift=4mm]f.north west) {two-variable run};
  \begin{scope}[on background layer]
    \node[panel, inner sep=4mm, fit=(pt)(f)(expand)(result)(rz)] (example) {};
  \end{scope}
  % ---- the general rewrite above it, with room for the arrow label
  \node[descbox, minimum width=3.4cm, anchor=south west] (power)
    at ([xshift=9mm,yshift=13mm]pt.north west) {$a u z_i^e$\\$e\geq2$};
  \node[checked, right=30mm of power, minimum width=5.5cm] (step)
    {$a u z_i^{e-1}-a u z_i^{e-2}\,z_i(1-z_i)$};
  \draw[xform] (power) -- node[xformlbl] {one exact rewrite} (step);
  \node[lblsm, below=4mm of example]
    {$c_1=-y,\quad c_2=0,\quad r=0$ \qquad coefficient dictionaries agree exactly};
\end{tikzpicture}
