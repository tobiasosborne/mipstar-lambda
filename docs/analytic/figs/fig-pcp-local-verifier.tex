\begin{tikzpicture}[node distance=7mm and 9mm]
  \node[descbox, minimum width=4.1cm, minimum height=5.2cm] (oracle) {};
  \node[font=\footnotesize\sffamily\bfseries,text=desc] at ([yshift=2.15cm]oracle.center)
    {proof oracle $\Pi$};
  \matrix (P) [matrix of nodes, row sep=1mm, anchor=center,
    nodes={bytes,minimum width=3.25cm,anchor=center}] at ([yshift=-2mm]oracle.center) {
    $g_1,\ldots,g_5$ evaluation tables \\
    $c_0$ evaluation table \\
    $c_1,\ldots,c_{m'}$ evaluation tables \\
    point values $\alpha_i,\beta_j$ \\
    line restrictions $f_j(T)$ \\
  };
  \node[stage, right=12mm of oracle, minimum width=4.1cm] (queries)
    {local queries\\$z$ and $\operatorname{ev}_z(\Pi)$\\axis line through $z$\\diagonal line through $z$};
  \draw[quote,<->] (oracle) -- node[above,lblsm,text=desc] {few locations} (queries);
  \node[checked, right=12mm of queries, minimum width=5.5cm] (accept)
    {formula test\\$\beta_0=F_{\rm arith}(z)\prod_{i=1}^5(\alpha_i-o_i)$\\[1.5mm]
     zero-on-subcube test\\$\beta_0=\sum_{j=1}^{m'}\beta_jz_j(1-z_j)$};
  \draw[xform] (queries) -- node[xformlbl] {accept iff both} (accept);
  \node[tag check, above=2mm of accept] {EXECUTED ON A SUPPLIED VIEW};
  \node[citedbox, below=8mm of $(queries.south)!0.5!(accept.south)$,
    minimum width=8.8cm] (ld)
    {low-degree enforcement and strategy extraction};
  \draw[xform,dashed,cited] (ld.north) -- (queries.south);
  \node[tag cited, right=3mm of ld] {CITED};
\end{tikzpicture}
