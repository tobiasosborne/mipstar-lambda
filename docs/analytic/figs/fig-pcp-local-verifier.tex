\begin{tikzpicture}[node distance=7mm and 9mm]
  % ---- the proof oracle: panel fitted around its table strips
  \node[font=\footnotesize\sffamily\bfseries,text=desc] (otitle) {proof oracle $\Pi$};
  \matrix (P) [matrix of nodes, row sep=1mm, anchor=north,
    nodes={bytes,minimum width=3.5cm,anchor=center}]
    at ([yshift=-4mm]otitle.south) {
    $g_1,\ldots,g_5$ tables \\
    $c_0$ table \\
    $c_1,\ldots,c_{m'}$ tables \\
    point values $\alpha_i,\beta_j$ \\
    line restrictions $f_j(T)$ \\
  };
  \begin{scope}[on background layer]
    \node[descbox, inner sep=3mm, fit=(otitle)(P)] (oracle) {};
  \end{scope}
  \node[stage, right=12mm of oracle, minimum width=4.1cm] (queries)
    {local queries\\$z$ and $\operatorname{ev}_z(\Pi)$\\axis line through $z$\\diagonal line through $z$};
  \draw[quote,<->] (oracle) --
    node[above=1.6mm,lblsm,text=desc,align=center,inner sep=0.5pt] {few\\reads} (queries);
  \node[checked, right=16mm of queries, minimum width=4.6cm] (accept)
    {formula test\\$\beta_0=F_{\rm arith}(z)\prod_{i=1}^5(\alpha_i-o_i)$\\[1.5mm]
     zero-on-subcube test\\$\beta_0=\sum_{j=1}^{m'}\beta_jz_j(1-z_j)$};
  \draw[xform] (queries) --
    node[above=1.4mm,text=ink,font=\scriptsize\sffamily\bfseries,align=center,inner sep=0.5pt]
    {accept\\iff both} (accept);
  \node[tag check, above=2mm of accept] {EXECUTED ON A SUPPLIED VIEW};
  \node[citedbox, below=8mm of $(queries.south)!0.5!(accept.south)$,
    minimum width=8.2cm] (ld)
    {low-degree enforcement and strategy extraction};
  % the executed local checks hand off to what stays cited
  \draw[xform,dashed,cited] (accept.south) -- (accept.south |- ld.north);
  \node[tag cited, right=3mm of ld] {CITED};
\end{tikzpicture}
