\begin{tikzpicture}[x=1cm,y=1cm]
  % ---- what the prototype actually evaluates
  \node[checked, anchor=north, text width=6.6cm, align=left,
        minimum height=1.35cm] (l1a) at (6.7,0)
    {\textbf{1(a) \ evaluate the two identities at \(z\)}\\[1pt]
     \scriptsize the formula test and the zero test are computed on the named
     instance};
  \node[tag check, anchor=south west] at (3.4,0.06) {EXECUTED};

  % ---- the two premises of the implication, one on each side of the spine
  \node[citedbox, anchor=north west, text width=4.2cm, align=left,
        minimum height=1.5cm] (l2) at (0,-2.30)
    {\textbf{2 \ low-degree enforcement}\\[1pt]
     \scriptsize a successful game yields measurements of individual degree
     \(\leq d\) (\code{lem:ld-soundness})};
  \node[checked, anchor=north east, text width=4.6cm, align=left,
        minimum height=1.5cm] (l3) at (13.4,-2.30)
    {\textbf{3 \ random evaluation}\\[1pt]
     \scriptsize the two bounds \((\degF+5d)m'/q\) and \((2+d)m'/q\) are
     computed here and compared with \(1/2\); the identity they then imply is
     \code{lem:schwartz-zippel}, \grade{CITED}};
  \node[tag cited, anchor=south west] at (0,-2.24) {CITED};
  \node[tag check, anchor=south east] at (13.4,-2.24)
    {BOUNDS CHECKED; IMPLICATION CITED};

  % ---- what may then be concluded
  \node[citedbox, anchor=north, text width=6.6cm, align=left,
        minimum height=1.35cm] (l1b) at (6.7,-5.15)
    {\textbf{1(b) \ \(\Rightarrow\) an accepting bounded computation}\\[1pt]
     \scriptsize holds only if both premises do and both bounds are
     \(<1/2\) (\code{thm:pcp-decider})};
  \node[tag cited, anchor=south west] at (3.4,-5.09) {CITED --- C5 SKETCH};

  % ---- layer 4, and the contract the whole section feeds
  \node[citedbox, anchor=north west, text width=4.4cm, align=left,
        minimum height=1.4cm] (l4) at (0,-7.55)
    {\textbf{4 \ quantum consistency and rigidity}\\[1pt]
     \scriptsize transport one polynomial view back to the original verifier};
  \node[citedbox, anchor=north west, text width=5.2cm, align=left,
        minimum height=1.4cm] (ar) at (5.6,-7.55)
    {\textbf{the answer-reduction contract}\\[1pt]
     \scriptsize completeness, soundness and entanglement bounds
     (\code{thm:ar})};
  \node[tag cited, anchor=south west] at (0,-7.49) {CITED};
  \node[tag cited, anchor=south west] at (5.6,-7.49) {CITED};

  % ---- the implication spine, with its two premises joining it
  \draw[xform,dashed,cited] (6.7,-1.40) -- (6.7,-5.15);
  \node[lblsm,anchor=west] at (6.95,-1.80) {\code{thm:pcp-decider}};
  \draw[xform,dashed,cited] (l2.east) -- (6.7,-3.20);
  \draw[xform,dashed,cited] (l3.west) -- (6.7,-3.20);
  \draw[xform,dashed,cited] (6.7,-6.55) -- (8.2,-7.55);
  \draw[xform,dashed,cited] (l4.east) -- (ar.west);

  \node[lblsm, anchor=north, align=center, text width=12.9cm] at (6.7,-10.05)
    {\chip{check} executed or finite-checked here \quad$\bullet$\quad
     \chip{cited} imported analytic theorem, dashed border\\
     layer 3 is used \emph{inside} layer 1, and layer 4 does not follow from
     layer 3; in layer 3 only the two bounds are executed --- the step from a
     random agreement to a formal identity is \code{lem:schwartz-zippel},
     which no sample count can replace};
\end{tikzpicture}
