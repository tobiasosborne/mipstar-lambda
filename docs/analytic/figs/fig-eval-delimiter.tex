\begin{tikzpicture}
  \node[closure, minimum width=111mm, minimum height=37mm] (ambient) {};
  \node[panel title, text=term] at ([xshift=2mm,yshift=-2mm]ambient.north west)
    {AMBIENT RUN: budget $f$};
  \node[draw=term,fill=white,rounded corners=2pt,minimum width=98mm,minimum height=16mm,
    below=8.5mm of ambient.north] (inner) {};
  \node[panel title, text=term] at ([xshift=2mm,yshift=-1.5mm]inner.north west)
    {EVAL DELIMITER: requested budget $F$};
  \matrix (ticks) [matrix of nodes, nodes={fuel,minimum width=16mm}, column sep=3mm,
    anchor=south] at ([yshift=1.5mm]inner.south) {
    $F$ & $F-1$ & $F-2$ & $\cdots$ & $F-k$ \\
  };
  % edge-to-edge, so the shaft never crosses a counter box
  \foreach \a/\b in {1/2,2/3,3/4,4/5}
    {\draw[beta] (ticks-1-\a.east)--(ticks-1-\b.west);}
  % the ambient ledger runs inside the ambient panel, one column per microstep
  \matrix (oticks) [matrix of nodes, nodes={fuel,minimum width=16mm}, column sep=3mm,
    anchor=north] at ([yshift=-4mm]inner.south) {
    $f-2$ & $f-3$ & $f-4$ & $\cdots$ & $f-2-k$ \\
  };
  \foreach \a/\b in {1/2,2/3,3/4,4/5}
    {\draw[beta] (oticks-1-\a.east)--(oticks-1-\b.west);}
  \foreach \i in {1,2,3,4,5}
    {\draw[cited, dotted, line width=0.6pt]
       (ticks-1-\i.south|-inner.south) -- (oticks-1-\i.north);}
  \node[tag focus, above=2mm of ambient] {INSTALL $+$ REMOVE COST TWO};
  \draw[brace] ([xshift=3mm,yshift=-1.5mm]ambient.south west)
            -- ([xshift=-3mm,yshift=-1.5mm]ambient.south east);
  \node[lbl, anchor=north] at ([yshift=-5.5mm]ambient.south)
    {each inner microstep decrements both counters};
\end{tikzpicture}
