\begin{tikzpicture}
  \node[tnode] (fix) {$\mathsf{Fix}$};
  \node[tnode, below=5mm of fix] (lam) {$\lambda C$};
  \node[redex, below=5mm of lam] (iff) {$\mathsf{If}$};
  \draw[tedge] (fix)--(lam); \draw[tedge] (lam)--(iff);

  % the three If branches share one baseline and are symmetric about If
  \node[tnode] (halt) at ([xshift=-26mm,yshift=-16mm]iff.center) {$\mathsf{halt}$};
  \node[tnode] (out)  at ([yshift=-16mm]iff.center) {$\mathsf{out}$};
  \node[tnode] (self) at ([xshift=26mm,yshift=-16mm]iff.center) {$\mathsf{self}$};
  \draw[tedge] (iff)--(halt); \draw[tedge] (iff)--(out); \draw[tedge] (iff)--(self);
  \node[lblsm, anchor=south east] at ([xshift=-1mm,yshift=0.5mm]$(iff.center)!0.5!(halt.center)$)
    {test};
  \node[lblsm, anchor=west] at ([xshift=1.5mm]$(iff.south)!0.5!(out.north)$) {then};
  \node[lblsm, anchor=south west] at ([xshift=1mm,yshift=0.5mm]$(iff.center)!0.5!(self.center)$)
    {else};
  \node[tleaf, below=4mm of halt] (hc) {$C$}; \draw[tedge] (halt)--(hc);
  \node[tleaf, below=4mm of out] (oc) {$C$}; \draw[tedge] (out)--(oc);

  \node[tnode, below=4mm of self] (move) {$\mathsf{move}$};
  \node[tleaf] (mc)    at ([xshift=-15mm,yshift=-16mm]move.center) {$C$};
  \node[redex] (delta) at ([xshift=15mm,yshift=-16mm]move.center) {$\mathsf{TMDelta}$};
  \node[tleaf] (m)     at ([xshift=-13mm,yshift=-15mm]delta.center) {$m$};
  \node[tnode] (view)  at ([xshift=13mm,yshift=-15mm]delta.center) {$\mathsf{view}$};
  \node[tleaf, below=4mm of view] (vc) {$C$};
  \draw[tedge] (self)--(move);
  \draw[tedge] (move)--(mc); \draw[tedge] (move)--(delta);
  \draw[tedge] (delta)--(m); \draw[tedge] (delta)--(view); \draw[tedge] (view)--(vc);

  \node[checked, right=10mm of delta, text width=31mm] (chg)
    {$\kappa_{\mathsf{TMDelta}}\le 8(|m|+k+2)$};
  \draw[callout] (delta.east)--(chg.west);
  \node[lbl, text=term, above=3mm of fix]
    {$\mathsf{loop}_m=\operatorname{Fix}(\lambda C.\,\cdots)$};
\end{tikzpicture}
