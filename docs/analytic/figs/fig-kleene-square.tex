\begin{tikzpicture}[node distance=7mm and 9mm]
  \node[descbox, text width=48mm] (p)
    {$p(z,x)$ computes \(s(z,z)\), then \(Q(s(z,z))\),\\
     then universally simulates that code on \(x\)};
  \node[checked, right=20mm of p, minimum width=24mm] (s)
    {specialize\\\(s(p,p)\)};
  \node[descbox, right=17mm of s, minimum width=30mm,
        fill=focuslt, draw=focus] (eq)
    {$e_Q:=s(p,p)$};
  \draw[xform] (p) -- node[xformlbl, align=center] {two copies\\of \(p\)} (s);
  \draw[xform] (s) -- node[xformlbl] {emit code} (eq);
  % arc ABOVE the row: bend right, since the path travels right-to-left.
  \draw[->, desc, line width=.9pt, shorten >=1pt] (eq.north) to[bend right=22]
    node[lblsm, above, text=desc] {its own specialized description}
    (p.north);

  \node[stage, below=18mm of p, minimum width=21mm, font=\scriptsize] (r1)
    {$\varphi^{(k)}_{e_Q}(x)$};
  \node[stage, right=5mm of r1, minimum width=23mm, font=\scriptsize] (r2)
    {$\varphi^{(k)}_{s(p,p)}(x)$};
  \node[stage, right=5mm of r2, minimum width=23mm, font=\scriptsize] (r3)
    {$\varphi^{(k+1)}_p(p,x)$};
  \node[stage, right=5mm of r3, minimum width=29mm, font=\scriptsize] (r4)
    {$\varphi^{(k)}_{Q(s(p,p))}(x)$};
  \node[stage, right=5mm of r4, minimum width=24mm, font=\scriptsize] (r5)
    {$\varphi^{(k)}_{Q(e_Q)}(x)$};
  % the four equalities sit IN the gaps, not across the box borders
  \foreach \a/\b in {r1/r2,r2/r3,r3/r4,r4/r5}
    \node[text=focus, font=\small\bfseries] at ($(\a.east)!0.5!(\b.west)$) {$=$};
  \draw[->, desc, dashed, shorten >=1pt] (eq.south) -- (r1.north);
  \draw[->, machine, dashed, shorten >=1pt] (p.south) -- (r3.north);
  \node[checked, below=7mm of r3, minimum width=64mm] (fixed)
    {$\varphi^{(k)}_{e_Q}=\varphi^{(k)}_{Q(e_Q)}$};
  % the chain yields the identity, so the head belongs at the identity
  \draw[callout] (r3) -- (fixed);
\end{tikzpicture}
