\begin{tikzpicture}[node distance=5mm and 7mm]
  \node[checked, minimum width=4.0cm] (base) {inductive CL datatype};
  \node[checked, below left=of base, minimum width=3.6cm] (concat)
    {concatenation\\levels add};
  \node[checked, below=of base, minimum width=3.6cm] (sum)
    {direct sum / product\\levels take $\max$};
  \node[checked, below right=of base, minimum width=3.6cm] (push)
    {shared-seed pushforward\\marginals replay};
  \draw[xform,check] (base) -- (concat);
  \draw[xform,check] (base) -- (sum);
  \draw[xform,check] (base) -- (push);
  \node[tag check, above=2mm of base] {DERIVED LOCALLY};

  \node[citedbox, below=13mm of sum, minimum width=13.9cm, minimum height=2.0cm] (global)
    {$\Introspect(\mathcal Q)\subseteq\mathcal Q$\qquad
     $\mathsf{PCPCompose}(\mathcal Q)\subseteq\mathcal Q$\qquad
     $\mathcal Q\times\mathcal Q\subseteq\mathcal Q$\\[1.5mm]
     \scriptsize one useful algebra must survive the complete compilers};
  \draw[xform,dashed,cited] (concat.south) -- (global.north west);
  \draw[xform,dashed,cited] (sum.south) -- (global.north);
  \draw[xform,dashed,cited] (push.south) -- (global.north east);
  \node[tag cited, left=4mm of global] {C7 CONJECTURE};
  \node[lblsm,text=cited,below=2mm of global]
    {local closure laws do not imply global closure under introspection, PCPP composition, and repetition};
\end{tikzpicture}
