
# Beads / Dolt files (added by bd init)
.dolt/
*.db
.beads-credential-key

# codex worker transcripts (large; regenerable)
briefs/*.codex.log

# analytic doc build dirs (lane compiles)
docs/analytic/build/
docs/analytic/*.aux
docs/analytic/*.log
docs/analytic/*.out
docs/analytic/*.toc
docs/analytic/*.lof
