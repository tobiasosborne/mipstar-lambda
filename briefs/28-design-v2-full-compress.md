# Brief 28 — DESIGN v2: the complete executable Compress (Repeat, Introspect, end-to-end fixed point)

Proposer (gpt-5.6-sol, xhigh). Autonomous; no questions; no git. Lane: `docs/DESIGN.md` (append new sections 9–13; do not rewrite §1–8 except to add forward references), `docs/definitions.md` (add rows), new `docs/design-v2-notes.md` if you need scratch. Do NOT touch docs/analytic/, src/, test/, claims/ (put claim rows in a MERGE PROPOSALS section). Report: `briefs/28-design-v2-full-compress.last.md` (≤ 15 lines).

## North star (user, binding)
A COMPLETE executable implementation of Compress = Repeat ∘ AnswerReduce ∘ Introspect on descriptions, every invariant tracked and adversarially verified, so that the construction can be *extended* by others. Introspect and Repeat are to be executable rungs, not CITED stubs. The soundness THEOREMS stay CITED; the CONSTRUCTIONS are executed and their bookkeeping (levels, dimensions, field sizes, runtimes, description sizes, question/answer lengths, gap maps as symbolic functions) is CHECKED.

## Read (ground truth first, then our docs/code)
- `ground-truth/gt-04-cl.tex` sec:cls — **def:sampler** (the 6-input sampler TM answering `dimension`, `marginal(j,z)`, `linear(j,u,y)`, `factor(j,u)`), def:sampler-sample, def:downsize_sampler, lem:downsize_sampler; lem:cl-func-prod (direct sums do not increase level), lem:cl-concat.
- `ground-truth/gt-05-games-normalform.tex` — def:decider, def:normal-game / normal form verifier, def:lambda (λ-bounded), Ent(·,·).
- `ground-truth/gt-06-types.tex` — typed samplers/deciders, **detyping** (lem:detyping-verifiers: +2 levels, dimension +?, the 16^{|T|} soundness factor) — this must become EXECUTABLE (it is a construction), with its soundness leaf CITED.
- `ground-truth/gt-11-parallel-repetition.tex` — sec:anchoring (typed sampler \hat S^anch on types {Game, Anchor}, decider, prop:anchoring: levels ℓ+2, dimension s(n)+8, soundness Ent map 4·16²ε), sec:anchored-repetition (S^rep as k(n)-fold direct sum over the four queries; D^rep with the (λn)^τ length guard; thm:repetition; k(n) = (λn)^{(1+c')τ}).
- `ground-truth/gt-08-introspection.tex` — sec:intro-overview, sec:intro-verifier (the typed introspective sampler and decider in full: type set incl. Pauli, Introspect, Read, Hide_k, the CL functions, how it consumes S(N,·) queries for N = 2^n, what it needs from the Pauli basis test), sec on complexity (lem:intro-sampler-complexity: sampler depends only on ℓ and time bounds), thm:introspection (contract). Read the completeness section enough to specify the HONEST strategy shape (it is a quantum strategy on EPR pairs — see below for what we execute).
- `ground-truth/gt-07-ldt.tex` sec:pauli-verifier — the (binary) Pauli basis test: its typed CL sampler (the question distribution) and decider (classical predicate on answers) — we implement the SAMPLER and the DECIDER; quantum soundness (thm for the Pauli basis test) is CITED.
- `ground-truth/gt-12-compression.tex` — fig:compress, thm:compression, lem:compress-independent-samplers, the parameter table (tab:params) and eq:mu-gamma / eq:c_rep for the universal constants; the Halting protocol section.
- Our side: `docs/DESIGN.md` §1.5–1.6, §2, §3, §5; `docs/definitions.md`; `src/samplers/*.jl`, `src/verifiers/*.jl` (what exists: CLStep datatype, typed samplers, TB2 product/oracularization); `briefs/21-tb1-repair.md` (lazy CLStep is being built now); `briefs/23-tb3.md`, `briefs/24-tb4.md`.

## What DESIGN v2 must specify (numbered §9–§13, DDs continue from DD-23)
9. **Sampler descriptions as the API.** A `SamplerDescription` sort whose *only* interface is the four def:sampler queries plus field size q(n) and level ℓ, implemented for (a) our CL datatype (lazy CLStep) and (b) every transformation output. Prove (in the design, as lemmas to be tested) that `downsize`, `detype`, `anchor`, `repeat(k)`, `product`, `direct_sum` are definable purely through the four queries, with their level/dimension/field-size/time laws (cite each law). The decider side: `DeciderDescription` = quoted program (TB3) with TIME bound; transformations `anchor_decider`, `repeat_decider(k, length guard)`, `detype_decider`. Every transformation returns `Checked{·, Cert}` with the contract as ASSUME/PROVE (as in §1.6).
10. **TB5 — Repeat.** Exact transcription of sec:anchoring and sec:anchored-repetition into the sort of §9; the k(n) formula and the (λn)^τ guard; the honest-strategy test we can execute: for a small ℓ-level verifier V (use TB2's answer-reduced verifier at a tiny parameter, or a toy 1-level verifier) build V^anch and V^rep, sample questions, and check that the CLASSICAL honest strategy (if V has a classical value-1 strategy — choose the toy so it does) is accepted; check levels ℓ+2, dimension s+8 then k·(s+8), time bookkeeping, and that S^rep depends only on S and (λ,τ) (hash test). Mutations. Instance sizes and times.
11. **TB6 — Introspect.** The typed introspective sampler and decider transcribed from gt-08 (and the Pauli basis test sampler/decider from gt-07) as executable objects over the §9 API of the input sampler at index N=2^n: what queries it makes (factor spaces and linear maps of L^w at index N), the type set and CL functions of \hat S^intro, the decider's checks (consistency, Pauli test predicate, introspection cross-checks, hiding checks, and the final call of D(N, x_A, x_B, a_A, a_B) on the introspected questions). Specify precisely what we can EXECUTE: (i) sampler construction and its CL level (5 per thm:introspection), dimension, time polylog(N) — CHECKED by construction and measurement; (ii) the decider as a classical predicate — CHECKED on synthetic transcripts; (iii) the honest strategy is QUANTUM (EPR + Pauli measurements) — we can execute a *classical simulation of the honest quantum strategy's transcript distribution* for tiny dimensions (state the exact simulation: sample the EPR measurement outcomes for the specified Pauli observables; for commuting families this is a classical sampling problem — say exactly when it is, citing fig:intro-honest and lem:commute) and check the decider accepts w.p. 1; (iv) soundness (thm:introspection) CITED. Give instance parameters (n, N, ℓ, dimension) small enough to run and honest about what is skipped.
12. **TB7 — Compress end to end.** Compress(V, λ) = Repeat(AnswerReduce(Introspect(V, λ, 9), λ, μ, γ), λ, τ) executed on a tiny λ-bounded verifier description; the level bookkeeping 9 → (thm:compression) checked; the sampler-independence lemma (lem:compress-independent-samplers) as a hash test; the universal constants μ, γ, τ carried symbolically with their defining equations; then the Halting fixed point D_{M,L} = YΨ_{M,L} (TB4) run for a 2-state M at the smallest n where anything is executable, reporting which factors are executed vs CITED at that n. Be honest about feasibility: N = 2^n and the paper's parameter regime are astronomically large; specify a "toy regime" policy (like TB0's) where the constructions run with all divisibility/admissibility predicates evaluated and reported, and state which predicates FAIL in the toy regime.
13. **Ladder and claims.** Update the tracer-bullet ladder (TB5, TB6a design/TB6b impl, TB7) with instances, expected times, mutations; a MERGE PROPOSALS section with new claim rows C12–C15 (statuses CONJECTURE) and the missing-step list for each. Note explicitly which paper objects will remain CITED at the end of TB7 (the list must be exact) — this is the "what remains mathematically difficult" deliverable.

## Style
As DESIGN §1–8: addressable, DDs with rejected alternatives, grades per invariant, ground-truth line citations. 400–700 new lines.
