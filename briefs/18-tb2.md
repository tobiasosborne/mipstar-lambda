# Brief 18 — TB2 implementation (red/green TDD): the typed answer-reduced decider (fig:decider-pcp) on the TB0 fixture

Proposer (gpt-5.6-sol, xhigh). Autonomous; no questions; no git. Lane: `src/verifiers/answer_reduce.jl` (new), `src/samplers/pcp_sampler.jl` (new), `src/samplers/oracularize.jl` (new), `test/tb2_answer_reduce.jl` (new), `test/mutations/tb2_*.jl` (new), MINIMAL additive edits to `src/MIPStarLambda.jl`, `test/runtests.jl`, `test/mutations/run.jl`. Do NOT modify other existing files (if a TB0/TB1 API is inadequate, wrap it in your new files and record the request under "API REQUESTS" in your report). Report: `briefs/18-tb2.last.md`.

## Read order
1. `CLAUDE.md`; rk-light law 4. `briefs/16-tb1.last.md` (TB1 API as built) and `briefs/14-tb0-repair.last.md`.
2. `docs/DESIGN.md` §1.5 (typed PCP sampler: 18 types Point_i/ALine_i/DLine_i, registers V_{i,pt}/V_{i,coord}/V_{i,dir} incl. dim V_{6,coord}=6 SOURCE_REPAIR, product with the oracularized sampler), §1.6 (the transcription of table:tpcp and the five checks of fig:decider-pcp with exact type-pair guards, both ldparams tuples (q,m,d,1) and (q,m',d,m'+6), the i∈{3,4,5} restriction, `detype` as CITED), §2 (`answer_reduce_pcp`, `oracularize_sampler`), §5.4 (TB2 instance: q=2^11 row, trivial 1-level original sampler, three-role oracularization, 18-type PCP sampler, 54-type product; honest strategy from Π; branch-covering deterministic suite + 256 seeded questions conditioned on check-triggering type pairs; report the unconditioned fraction of the 54²=2916 ordered type pairs that trigger no check; mutations: c₀+1 → formula test; g₃+1 in the individual copy but not the bundle → proof-consistency check; truncate a line polynomial → D^ld). `docs/definitions.md`.
3. Ground truth: `ground-truth/gt-10-answer-reduction.tex` sec:ld-compiler (sampler paragraphs, table:tpcp, fig:decider-pcp, thm:ar statement); `ground-truth/gt-09-oracularization.tex` (types alice/bob/oracle, L^alice, L^bob, the oracularized decider — implement only what fig:decider-pcp step 5 needs); `ground-truth/gt-06-types.tex` (typed samplers; detyping only as a CITED leaf).
4. Existing code: TB0 (`src/verifiers/pcp.jl`, `src/tb0.jl`: PCPProof, ev_z, pcpverifier, witness (i)/(ii)), TB1 (`src/samplers/*.jl`, `src/verifiers/ldt.jl`: CL datatype, L_Point/L_ALine/L_DLine, restrict, D^ld).

## Process
1. Write `test/tb2_answer_reduce.jl` FIRST (RED; paste excerpt): (a) the typed PCP sampler has 18 types, complete type graph, level 3 by datatype, register dimensions per eq:V-pcp (V_{6,pt}, V_{6,dir} of dim m'=16; V_{6,coord} of dim 6 with the SOURCE_REPAIR node visible in the certificate); (b) the product with the oracularized trivial sampler has 54 types and level max(ℓ,3)=3; (c) question/answer parsers for every type in table:tpcp round-trip; (d) the honest strategy built from Π (witness (i) for speed, witness (ii) for the 4(a)/4(b) checks on i∈{3,4,5} — the design says witness (ii) owns those) is accepted by every check of fig:decider-pcp on a deterministic branch-covering suite (every one of the five checks, every guard branch, both ldparams) and on 256 seeded questions conditioned on triggering type pairs; (e) the unconditioned no-check fraction over the 2916 ordered type pairs is computed and printed; (f) step 5 (game check) calls `pcpverifier` with x_{w,alice}=L^alice(x_{Q,w}), x_{w,bob}=L^bob(x_{Q,w}) from the oracularized sampler — with a trivial original sampler make sure this path is actually exercised, not vacuous (state what x,y the decider specification receives).
2. Implement; GREEN; paste summary, wall time, and the trace printout for one question of each of the five checks.
3. Mutations on copies, registered in run.jl: the three from DESIGN §5.4 plus (M-guard) swap the (Point_i, ALine_i) guard to (Point_i, DLine_i) so the wrong ldparams is used (must be killed by the degree/format check), (M-i345) extend the individual low-degree test to i∈{1,2} (must be killed by a test asserting the guard set exactly — if no honest-strategy test can detect this, say so and add a structural test on the guard table instead, marking it as such).
4. Time: TB2 ≤ 45 s; whole suite under the 60 s gate on a warm cache (report cold and warm wall).

## Report (≤ 30 lines)
RED excerpt; GREEN summary; no-check fraction; trace printouts; mutation lines; API REQUESTS; deviations from DESIGN; MERGE PROPOSAL for a new claim row "C9: the typed answer-reduced decider accepts the honest strategy on the TB0 fixture on every check of fig:decider-pcp" (proposed TESTED) and any amendment to C4 — proposals only.
