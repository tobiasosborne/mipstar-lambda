# Brief 65 — TB1 + TB2 repair r5 (work orders: verdicts/tb1-r5.md N29 MAJOR, N30 decision, N31 MINOR; verdicts/tb2-r5.md NG1, NG2 MAJOR, N27, N28, N30 NOTE) — small closing round; the DESIGN §9 lockstep is the orchestrator's

Proposer (Fable). Autonomous; no questions; **no git**. Lane: `src/verifiers/ldt.jl`, `src/verifiers/answer_reduce.jl` (the replay-case list), `src/samplers/**` only if N30 needs it, `test/tb1_ld_sampler.jl`, `test/tb2_answer_reduce.jl`, `test/mutations/tb1_*.jl`, `test/mutations/tb2_*.jl`, `test/mutations/run.jl` (additively). Report: `briefs/65-tb1-tb2-repair-r5.last.md` (≤ 20 lines).

Scratch: `/tmp/claude-1000/-home-tobias-Projects-discussions/548f633b-c857-457a-bb33-0aa1a7879403/scratchpad/tb1-tb2-r5/`

Read `verdicts/tb1-r5.md` and `verdicts/tb2-r5.md` IN FULL — every FIX DEMAND is binding:
- **TB1 N29 (MAJOR)**: on a zero-direction diagonal line `_line_parameter` returns `t=0`, so fig:ld-decider item 3 is checked at one admissible parameter instead of all eight (`gt-07-ldt.tex:379-384`); reached by 1,024 of the 37,888 honest line-vs-point decisions; the critic's degree-1 cheat `f(t)=1+t` at seed `(0,0,0,0,0)` is accepted. Fix: check item 3 at EVERY admissible `t` on a degenerate line (or declare the weaker reading as a `SOURCE_REPAIR` with its 1,024 count printed — the critic pre-wrote the C4c row in both variants; prefer the source-exact fix); the critic's NM15 registered and KILLED; the cheat becomes a named negative transcript.
- **TB1 N30**: choose the rule the critic asks for and say which (see the verdict's FIX DEMAND next to DD-4) — a wording proposal for DESIGN §9.x; **N31**: as demanded (a one-line assertion).
- **TB2 NG1/NG2 (MAJOR)**: add the eighth and ninth cases to `_answer_reduce_replay_cases()` — the diagonal orientation of steps 4(b) and 4(c) with a corrupted-reject witness (`:ld_diagonal_point`) — so the certificate replay covers them; the critic's two disarming mutants registered and KILLED. C9's row already says "nine cases / 27 (case,seed) pairs" (unfrozen by the critic) — match it.
- **TB2 N27** (lockstep "honest" beyond all-zero answers at the zero seed: run the 2916-pair lockstep at one nonzero seed with honest TB0-proof answers), **N28** (`TB2_GUARD_NAMES` distinguishes axis/diagonal), **N30** (certificate coverage: one line of disposition).
- Items the critics routed to TB5 (tb1-r5 N30/N31's brief-39 notes; tb2-r5 N23–N25) are NOT for this round.
Rules: red/green with the critics' survivors (NM15, NG1, NG2) as red witnesses; baseline-first runner exit 0, every mutant KILLED; the suite on a quiet machine (report `uptime`; the TB0 gate is CPU-clock-bound — ~18 s at performance). Report: response table with file:line; runs; CROSS-LANE EDITS; MERGE PROPOSALS: C4c (the critic's pre-written row in the variant you implemented), C4a/C9 scope clauses struck where fixed (verbatim; proposals only).
