# Brief 64 — the interactive tutorial artifact: "Compress, Explained" — one HTML page that explains everything built so far, exploits the analytic document's explanations, and goes far beyond it with interactive instruments, animations, three.js scenes and embedded videos

You are an Opus agent acting as design lead + engineer. User directive (verbatim): "produce a gorgeous html artifact that explains all the work done so far and exploits all the pdflatex explanations, but goes way beyond: the artifact should have interactive demos of all the ideas, animations, three.js or beyond. I want this artifact to be amazing. It should have videos in it. It should make 3blue1brown look clumsy."

Lane: `docs/tutorial/**` (the page: `docs/tutorial/compress-explained.html`; build scripts and frame renderers under `docs/tutorial/build/`), scratch `/tmp/claude-1000/-home-tobias-Projects-discussions/548f633b-c857-457a-bb33-0aa1a7879403/scratchpad/tutorial/`. No git. Read `src/`, `test/`, `docs/`, `claims/`, `verdicts/`, `ground-truth/` freely; write nothing else. Julia is contended by other agents: use at most a few SHORT read-only Julia runs (`julia --project=. -e '...'`, ≤ 1 min each, never the test suite) to print real objects; prefer the already-printed outputs in `/tmp/claude-1000/-home-tobias-Projects-discussions/548f633b-c857-457a-bb33-0aa1a7879403/scratchpad/tutorial-data/` (suite logs with the certificate trees and numbers) and the reports `briefs/*.last.md`.

## What the page is
A physicist opens it and, in one sitting, understands what MIP*=RE's compression step IS, what we BUILT, and what is PROVED vs TESTED vs SKETCH vs CITED. It is the tutorial deliverable of `CLAUDE.md` (north star) and must obey rk-light law 1: never state anything stronger than `claims/CLAIMS.md` — the claims ratchet is itself a chapter, rendered live from the current table (C1–C4c, C5–C9, C12–C19 with their statuses), and every demo says which claim/verdict backs it.

## Design plan (binding; refine, don't replace)
- **Continuity with the PDF**: the page's palette is `docs/analytic/figstyle.tex`'s six semantic colours (machine / term / desc / check / cited / focus, plus bad) — read the hex values there and use them with the SAME meanings (teal-ish machine = Turing machines, violet = lambda terms, etc.; amber focus = exactly one highlight per figure). Neutrals: a cool slate ground biased toward the machine hue; dark-first design with a full light theme (three-state theme rule below).
- **Type** (Google Fonts, with real fallback stacks): display `Fraunces` (optical size, used sparingly for chapter theses), body `Source Serif 4`, UI/labels `Source Sans 3`, code/data `Inconsolata` (the PDF's own mono). Type scale set once; ~65-char measure for prose; tabular numerals for every number column.
- **Layout**: a long-scroll "instrument bench": a sticky left rail is the pipeline itself (TM → λ → descriptions → CL samplers → low-degree test → PCP → answer reduction → introspection → repetition → Compress → Y Ψ → Halting → MIP*=RE) and doubles as a progress map; each chapter = one-sentence thesis in display type → one INTERACTIVE INSTRUMENT (the hero of the chapter) → one VIDEO → the real numbers and the certificate/claim backing → "what the paper says" with the gt label. Reduced-motion respected; everything readable at rest (no opacity-0 waiting on observers).
- **One aesthetic risk**: the certificate-tree explorer and the compression ladder are rendered as a single continuous three.js scene the reader flies through ("the machine room"), with the semantic colours as light.

## Chapters and their instruments (all driven by REAL objects from this repo — never invented numbers)
1. **Turing machines**: the two-state equality machine of docs/analytic §1.2 as a live tape simulator (step/run/scrub; configurations `(q,(L,s,R))`), then `U` reading a description (§2).
2. **Lambda calculus**: β-reduction animator with de Bruijn indices (§4), Church numerals, the Z/Y combinator unfolding (YCode, c_Y) as an animation; quotation from term to bytes (the repo's canonical-bytes codec: show the actual bytes of the trivial decider, |D| = 33, fnv1a64 f8561ef8c5761695 — from `briefs/23-tb3.last.md`).
3. **Fuel and the CEK machine**: interactive evaluator with a fuel meter (the real charge table from `src/ir/programs.jl`): Value / OutOfFuel / SortError; the equality decider needs T=3.
4. **Descriptions vs closures**: why Compress consumes code (handoff.md §"Representation requirements"); the four-query sampler API (Dimension/Marginal/Factor/Linear) as a clickable machine.
5. **CL samplers**: the level-1/2/3 maps `L_Point`, `L_ALine`, `L_DLine` over `F_8^5` (C4a): a 3D three.js scene of `F_8^2` points, axis lines and diagonal lines, seeds → question pairs; the exact histograms (support 512 / 18,432 of 32,768 — C4a).
6. **Low-degree test**: polynomial over GF(8) with degree slider; line restriction; the decider accepting/rejecting (C4c evidence: 71,360 support decisions, 40,768 non-noop).
7. **Tseitin, arithmetization, F1**: the real six-gate circuit; the occurrence vector `(2,0,0,0,2,2,0,0,0,0,6,4,4,4,4,3)` vs the paper's degree claim (C8 — framed SOFTLY: "our reading of NW19 may be wrong", cite docs/findings.md).
8. **Zero-basis / the Boolean cube**: three.js `{0,1}^n` cube with `c_0 = Σ c_i zero(z_i)` (C2): witnesses (i), (ii) accept, (iii) has remainder 2 monomials.
9. **The PCP verifier**: walk through fig:pcpverifier with the real numbers (β₀ = 2 over GF(8), 96 over GF(2^11), the separator; C1).
10. **Answer reduction**: the 54-type product graph (interactive: hover a type pair → which of the five guards fires; 2736/2916 pairs trigger no check — C9), with the detyping +2 levels CITED.
11. **The quoted front end (TB3)**: bounded trace → succinct 3SAT → decoupled 5SAT → PCP, animated on the trivial decider (T=1, one clause, widths (0,0,1,1,1)) and the equality decider (15 clauses → 47, `ExpansionRefused(279,936 > 160,000)` shown honestly).
12. **Introspection** (DESIGN §11, CONJECTURE C14 — NOT built yet, say so): EPR pairs and Pauli measurements as an animation; the 3Q boundary; the fuel gate — labelled "design, not yet executable".
13. **Anchoring and repetition** (DESIGN §10, C13 CONJECTURE): 81-fold direct sum visual; the k(n) formula; labelled as design.
14. **Compress and the fixed point** (DESIGN §12, C15): the level chain 9→5→7→9 and dimensions 206→840→848→1696 as the three.js ladder; `D_{M,λ} = Y Ψ_{M,λ}`; the Halting verifier; MIP*=RE in one paragraph.
15. **The process**: the rk-light loop as an animated objection trajectory per lane (design 10→2→0; TB0 7→5→1→0; TB1/TB2 …; doc 11→6→0) from `verdicts/`; the mutation runner (84 mutants, baseline-first, UNATTRIBUTABLE); the claims ratchet table live.
16. **Certificate tree explorer**: the real printed tree from `test/tb2_answer_reduce.jl` / TB3's printout (CHECKED / CONSTRUCTED / CITED / ASSUMED / SOURCE_REPAIR / NOT_EVALUABLE / VACUOUS), each leaf clickable to its gt line or claim.

## Videos (real, embedded)
Render 6–8 short clips (5–10 s, 640×360 or 800×450, 24–30 fps) with matplotlib (numpy 2.4, mpl 3.10 available) → PNG frames → `ffmpeg` (available, 6.1) → WebM VP9 (`-c:v libvpx-vp9 -b:v 0 -crf 34`), each ≤ 800 KB; embed as `<video controls muted playsinline preload="metadata" src="data:video/webm;base64,...">` with a poster frame. Subjects: the equality machine running; a β-reduction cascade; seeds flowing through a CL sampler into a histogram; the low-degree test on a line; the Boolean cube zero-set; the compression ladder. Keep the whole page ≤ 12 MB (hard limit 16 MB).

## Technical constraints (CSP — non-negotiable)
External scripts ONLY from `https://cdnjs.cloudflare.com` (three.js: `https://cdnjs.cloudflare.com/ajax/libs/three.js/r128/three.min.js`, UMD global `THREE`; pin exact versions); fonts ONLY from `fonts.googleapis.com`; everything else inline (CSS, JS, images as data URIs, videos as data URIs). No fetch/XHR. No `<!DOCTYPE>`/`<html>`/`<head>`/`<body>` tags — the file starts with `<title>Compress, Explained</title>` then `<style>` then content. Theme: full light palette on bare `:root`, dark tokens under `@media (prefers-color-scheme: dark)` guarded `:root:not([data-theme="light"])` AND under `:root[data-theme="dark"]`; `body` has an explicit token background; every colour via tokens. Responsive; wide content scrolls in its own container; `prefers-reduced-motion` honoured; keyboard focus visible; three.js scenes degrade to a static canvas frame if WebGL is unavailable. Mermaid is rendered natively via `<pre class="mermaid">` if you want it (no library).

## Process
1. Read: `handoff.md`, `CLAUDE.md`, `claims/CLAIMS.md`, `docs/analytic/parts/*.tex` (the explanations to exploit — every chapter thesis should be traceable to a section there), `docs/analytic/FIGURES.md` §1, `docs/DESIGN.md` §1–3, §9–13 (skim), the latest `briefs/*.last.md` for TB0–TB3, `verdicts/*-r*.md` final lines (objection trajectories).
2. Write the design plan (tokens: 6 semantic + 3 neutrals as hex; type roles; layout) at the top of `docs/tutorial/build/PLAN.md`; then build. Build the videos first (scripts in `docs/tutorial/build/`), then the page.
3. Look at the result ONCE: render one screenshot if a headless browser exists (`chromium`/`google-chrome --headless --screenshot`), otherwise validate the HTML (well-formed, sizes) and one pass of fixes. Do not loop.
4. Report `briefs/64-tutorial-artifact.last.md` (≤ 40 lines): chapter → instrument → data source → claim/verdict backing; video list with sizes; total page size; anything you could not ground in a real object (and therefore left out or labelled "illustrative").
