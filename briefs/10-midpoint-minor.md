# Brief 10 — apply MINOR fixes O15, O16, O17 (and O19, O20 if cheap) from verdicts/midpoint-r2.md

You are the proposer (gpt-5.6-sol, xhigh). Work fully autonomously; do not ask questions; do not run git. Lane: `toys/midpoint/` only, plus append a section "## r2 MINOR fixes" to `toys/midpoint/repair-r1-response.md`.

Read `verdicts/midpoint-r2.md` §3 (O15–O21) and apply EXACTLY the fix demands of O15 (⟨1⟩6 last sentence replaced by the critic's wording), O16 (the three tested lines with the 30-digit anchors; extend `mutations/run.jl` so a mutation may target `test.jl`, and add the critic's X1 swap as permanent mutant M13), O17 (renumber ⟨1⟩6 into ASSUME/PROVE with ⟨2⟩1 transcript-shape induction, ⟨2⟩2 exact cost 2n+2 = Θ(n), ⟨2⟩3 product; replace "O(n)" by "exactly 2n+2 = Θ(n)"). Then O19 (move the recurrence to a numbered PROVE step with the bijection derivation) and O20 (expose an `unchecked=true` keyword and assert `optval ≤ cheating_bound(n)` on the H-violating instance D={0,1,2}, f(t)=t+1 mod 5, n=2) if each takes < 15 minutes. Do not change any number, any claim, or any other test. Run `julia toys/midpoint/test.jl` and `julia toys/midpoint/mutations/run.jl` from the repo root and paste the summary lines (M13 must be listed as killed). Final message ≤ 8 lines.
