# Brief 43 — TB6 implementation (red/green TDD): executable Pauli test (§11.1–11.2), introspection sampler and decider (§11.3–11.4), exact stabilizer honest-strategy simulation (§11.5), fixtures TB6b-E / TB6b-M (§11.6). Two sub-rungs: TB6a (design audit) then TB6b.

Proposer (Fable). Autonomous; no questions; **no git** (orchestrator commits). Lane: `src/introspect/**` (new: pauli_params, pauli_sampler, pauli_decider, intro_sampler, intro_decider, stabilizer, fuel meter), `test/tb6a_audit.jl`, `test/tb6b_introspect.jl`, `test/mutations/tb6_*.jl`, MINIMAL additive edits to `src/MIPStarLambda.jl`, `test/runtests.jl`, `test/mutations/run.jl`. Earlier rungs' files only additively (CROSS-LANE EDITS listed). Report: `briefs/43-tb6-introspect.last.md` (≤ 45 lines).

Scratch: `/tmp/claude-1000/-home-tobias-Projects-discussions/548f633b-c857-457a-bb33-0aa1a7879403/scratchpad/tb6/`

## Why this rung matters
Introspect is the transformation that makes Compress possible: it replaces a verifier's questions by Pauli-basis measurements on EPR pairs and reads the input verifier ONLY through the four-query description API (§9). Build it as the general construction parameterized by `(V :: (SamplerDescription, DeciderDescription), lambda, ell)`; the two fixtures live in tests. Everything that is quantum stays CITED; what executes is the classical predicate, the sampler, the query schedule, the metered child calls, and an exact stabilizer-tableau sampler for the deterministic-child fixtures (DD-27).

## Read order
1. `CLAUDE.md`; rk-light laws 1, 4, 5; `claims/CLAIMS.md` C14 (CONJECTURE — the row this rung targets; statuses change only by verdict).
2. `docs/DESIGN.md` §11 IN FULL (every number there is a test oracle: 26 Pauli types, 86 oriented Pauli pairs; `|TypeIntro|=32+2ell`, `2ell+39` non-loops, `6ell+110` oriented pairs; dims `3m+3`, `(3m+3)log2 q`, `+4(32+2ell)`; levels 3 → 3 → 5; the nine-item intro predicate; the query schedule for `y_<k` and `(L_{j,prefix})^perp`; the single Q-bit wire format; PAPER_LITERAL `>=3Q` vs OPERATIVE `>3Q` with `SOURCE_REPAIR(intro-3Q-guard)`; the fuel unit = one metered quoted-interpreter step; the TB6b-E/M policy reports verbatim; the ten honest-child cost slots; the twelve owned mutations; `T6-view-swap`), §9 (description API; `describe_cl`; DL9 laws; detype), §12.4 (ToyPolicy, DD-28, DD-31), §13.1 TB6 rows. `docs/definitions.md` §H (IntroAnswerEncoding, TIME_child/F_child/toy_child_fuel rows). `verdicts/design-v2-r2.md` §R(a)–R(d) and `verdicts/design-v2-r3.md` §2 (the critic's independent recomputations of every TB6 number — your tests must reproduce them).
3. Ground truth (recompute from it, never from memory): `ground-truth/gt-07-ldt.tex` L550–L610, L1012–L1068 (type graphs), L1070–L1120 (Pauli sampler), L1126–L1227 (fig:decider_pauli), L1426–L1447 (thm:pauli, CITED), L1492–L1569 (def:introparams, lem:delta-bound); `gt-08-introspection.tex` L217–L315 (fig:type-graph-intro), L317–L392 (intro sampler, lem:intro-sampler-complexity), L401–L498 (answer formats, fig:intro-decider), L417–L419 (fuel gate), L524–L530 (wire format), L550–L579, L641–L684 (query schedule), L588–L591 (3Q), L694–L776 (decider complexity, CITED), L784–L817 (thm:introspection, CITED), L923–L953 (lem:commute), L1002–L1050, L1070–L1172 (honest strategy). `gt-06-types.tex` for typed samplers/deciders.
4. Existing code: TB5's `src/descriptions/` and `src/repeat/` (API and certificate conventions — follow them exactly), TB1's `ld_decider`, TB3's program IR / quoted evaluator (the metered interpreter must be THE quoted evaluator with a step counter, not a Julia call counter).

## Process
**TB6a** (`test/tb6a_audit.jl`, target < 1 s): for `ell in {1,3,9}` construct `TypePauli`, `G^pauli`, `TypeIntro`, `G^intro`; assert the counts above and equality with an explicit hand-transcribed edge list from the gt figures; generate every parser schema (the four non-Pauli answer schemas + the Pauli table) and every guard; emit the source-query plan and compare with the schedule of §11.4. Mutations: missing edge, wrong count, missing guard. No theorem claim.

**TB6b** (`test/tb6b_introspect.jl`), tests FIRST (RED excerpt in the report):
(a) `pauli_params(R; a,b symbolic)` builds the AST of §11.1, reports admissibility, `m|q`, the capacity chain `s(N)<=R<=M<=Q` link by link, `Q>=s(N)`, `TIME_child(N)<=R`, `|V|<=lambda` — each PASS/FAIL/NOT_EVALUABLE/VACUOUS exactly as the two policy reports in §11.6 (they are the oracle, including canonical `introparams` equality FAIL with the even-c obstruction printed).
(b) `pauli_sampler(q,m,d)`: typed level-3 description, dim `3m+3`, zero maps with `SOURCE_REPAIR(zero-map-factor-partition)`, §9.2 replay + output-sampler row; `pauli_decider` on all eight guards in both orders with hand-built accept/reject transcripts per guard.
(c) `tilde_S_intro(lambda,ell)` → downsize → detype: field 2, level 5, dimension `(3m+3)log2 q + 4(32+2ell)` (142 for E, 179 for M); description depends only on `(lambda,ell)` and the constant symbols (intensional hash test with two byte-distinct input verifiers).
(d) `typed_intro_decider(V,lambda,ell)` reads `V` only via a RECORDING description wrapper: assert the query log equals the §11.4 schedule (Dimension first; Factor(1,0); Marginal/Factor per stage; Linear on every basis vector of the factor; canonical Gaussian elimination + complement); `M6-N` red (calls at `n` not `2^n`).
(e) fuel meter: child calls run in the quoted interpreter with exactly `R` steps in production (`M-intro-fuel` boundary: step `R` may return, step `R+1` never executes) and `F_child=65,536` in toy mode with `toy_child_fuel=FAIL(owner=tb6-child-meter)` printed; every child-call record has fixture, quote hash, mode, role, stage/prefix/input, exact metered cost, source R, supplied F_child, timeout/return. Fill the ten cost slots of §11.6 with MEASURED exact counts (this is the obligation `tb6-child-meter` owns); a slot fits `F_child` only if measured.
(f) TB6b-E: 116 oriented pairs; for each, enumerate every nonzero-support stabilizer outcome with exact dyadic probability, total mass one; operative-toy acceptance PASS only after (e) shows fit; literal `>=3Q` rejects exactly 10 of 116 (Hide-incident) — print both `P_intro_literal` and `P_intro_operative`; detyped decision tree covered by every valid graph encoding + invalid branch; 256 seeded draws.
(g) TB6b-M: the binary child sampler of §11.6 (prefix-dependent factors, nonsymmetric stage-2 matrix, `L^alice != L^bob`, asymmetric decider) — assert on `z*=e1+e3+e5+e6` the prefixes and final questions differ; live `Hide_1--Hide_2--Hide_3` edges with dual-map checks; 8 branch-directed transcripts + 512 seeded; literal guard rejects exactly 22 of 128; `T6-view-swap` rejects typed and after detyping; `enu:hiding-same` and low-degree checks VACUOUS on E, live on M; `dm/q=1/4` margin printed.
(h) stabilizer simulator: symplectic commutation precheck rejects an anticommuting family (`M6-noncommuting`); `lem:commute` criterion asserted for the Read family; no dense state vector (assert peak allocation).
(i) the full certificate tree printed: CHECKED / CONSTRUCTED / CITED (`thm:pauli`, `thm:introspection`, `lem:intro-sampler-complexity`, `lem:intro-decider-complexity` with gt line ranges greppable) / SOURCE_REPAIR / NOT_EVALUABLE / VACUOUS / FAIL(owner) leaves.
Then implement; GREEN; walls (E: construction < 3 s + transcripts < 15 s; M warm < 25 s; combined < 43 s; peak < 512 MiB — measured values replace targets); then the twelve owned mutations of §11.6 (`M6-pauli-edge`, `M6-pauli-gamma`, `M6-sampler-nonzero`, `M6-N`, `M6-factor-prefix`, `M6-perp`, `M6-game`, `M6-boundary`, `M6-noncommuting`, `M-factor-partition`, `M-detype-view-orientation`, `M-intro-fuel`) registered and KILLED under the unmutated-first runner. Whole suite under the TB0 gate; runner exit 0.

## Report (≤ 45 lines)
RED excerpt; TB6a counts; GREEN summary + walls + peak; the two policy reports as printed; the filled ten-slot cost table; literal/operative rejection counts (10/116, 22/128); certificate tree for `Introspect(TB6b-M)`; mutation lines; CROSS-LANE EDITS; API REQUESTS; deviations from §11 with the gt line that forced each; MERGE PROPOSAL: C14 row text with the measured evidence (proposal only).

## ADDENDUM (2026-09-05, from verdicts/tb5-r1.md §5 — TB6 readiness; these are DESIGN DECISIONS for this rung, to be implemented and their DESIGN §11.4/§9.5/§9.6 wording proposed for the TB6 critic)
- **Child-fuel unit (Blocker 1).** Descriptions are data for the fixed interpreter machines `query`/`decide`, not §1.1 programs. The fuel unit of §11.4 is therefore defined as **one primitive step of the universal interpreter machine** executing the child description: every register read/write, every matrix-row multiply-accumulate over one field element, every branch selection, every input-decoding and output-serialization byte, every control transfer counts 1; a whole vector operation or a whole child query never counts 1. Implement a step meter inside the interpreter machines (`_decide`, `_validated_answer`, the four-query dispatch) that is depth-aware (per-mode, per-child attribution — Gap 5) and that the sizing `Dimension` probe does NOT bypass (it runs on the same meter). The production budget is exactly `R = N^λ` steps per child call, timeout before step R+1; TB6b substitutes `F_child = 65,536` and prints `toy_child_fuel = FAIL(owner=tb6-child-meter)`. The charge table is printed in the report and proposed as DESIGN §11.4's definition of the unit; the lowering into §1.1 `Eval` fuel remains an API REQUEST for TB7 and is NOT required here.
- **Typed decider term (Blocker 2).** Add one grammar tag `TypedDecider(TypeSet, code)` to `DeciderDescription` whose bytes carry the type labels, replacing the hard-wired `ANCHOR_TYPING` special case (which becomes an instance); `typed_intro_decider` and `pauli_decider` are its instances; `_decider_typing` reads the labels from the bytes.
- **Gap 3**: fix the edge convention once in DESIGN §9.5 — the stored graph is the oriented pair set; an undirected source graph (G^pauli: 26 loops + 30 non-loops = 86 oriented pairs; G^intro: 6ℓ+110) is stored as both orientations, and `detype_decider` iterates the oriented list. Assert the oriented counts against the undirected source counts.
- **Gap 4**: three new primitive sampler tags `pauli_sampler`, `tilde_S_intro`, `graph_sampler` with the mandatory §9.6 output-sampler replay rows.
- **Risk**: measure `_require_image`'s cost at dimensions 142 and 179, level 5, in TB6a and print it; if it is super-linear, replace the column-space rebuild by the stored stage matrices (report the choice).
- Build on TB5's `StageVerifier`/`params` carrier; `Introspect` becomes a real `CompressStage` swappable for TB4's stub. Do NOT start until brief 77 has landed (the orchestrator sequences this).
