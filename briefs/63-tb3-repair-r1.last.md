# Brief 63 — TB3 repair r1: report (Fable, 2026-09-05; tree = 3f2d1f1 + this lane, NO git; scratch tb3-r1/)

**RED → GREEN.** Red witnesses first: the critic's C-N2a/C-N2b applied by hand SURVIVED 162/162 before the (c) row pin (verdict §3); the A/B chimera passed `verify_certificate` and reached `frontend_pcp` printing A's `|D| = 33` before the slot. After the first draft: `count` shadowing in `decouple5`, `Dict{Any,Any}` in `specialize(Fix,())`, and my own wrong expectation `m' = 16` for the T=2 padded object (N7 makes it m=2, live 3, s=17, m'=32) — each seen red, fixed, rerun.
**Runs (governor performance).** Suite: `julia --project=. test/runtests.jl` → **989/989** (907 + 82 new), exit 0, wall 71 s, **TB0 body 17.157 s** (gate 60), `pgrep runtests|mutations/run` empty, load 3.29→2.47 (lagging average; no Julia but mine). TB3 in-suite (a) 0.7 (b) 0.4 (c) 1.1 (d) 0.3 (e) 2.1 (f) 7.4 (g) 1.9 (h) 0.6 ≈ 14.5 s (≤ 20). Runner: `MUTATION REGISTRY: killed=97/97 baselines ok=46/46 wall=324.61 s`, exit 0 (84 prior + 13 new; load 2.8→4.6, the first two passes 96/97 with my own concurrent pass contaminating). Cold: `tools/cold_precompile.sh` = **89.4 s** (load ≈ 3, performance governor; 188.2 s at brief 23 under powersave). Peak RSS test (f) 628 MB.
| obj. | response | where |
|---|---|---|
| N1 | equality run at T=3: control label, fuel, outcome and ordered `fields` (control key, `k1` prim frame, `k1v1`) pinned per row; accepting run keys identical; C-N2a, C-N2b registered → KILLED | test/tb3_frontend.jl:337; test/mutations/tb3_r1.jl |
| N2 | `build_pcp(...; upstream=())` slot: build time = `tseitin(upstream_circuit(term))` must reproduce `tf` AND the upstream certificate must verify on its own term (else `ArgumentError` naming the node), replay = `:UpstreamEvidence` node recomputes the reproduction against the attached proof and `_bind_certificate`s the subtree to `proof.tf`; every front-end node is identity-bound to its subject (`_bound_replay`) and relocated through the constructor chain (`_relocate`: Pad5.source→Decouple5.source→3SAT.trace→trace.program), no captured fallback anywhere; graft gone. Chimera (B = `λnxyab.true` with an `Opaque` bound, |D|=45, same padded object) → `:certificate_binding` at `:Pad5`; `frontend_pcp(chimera)` throws; B's own tree prints |D|=45 and never A's hash; foreign padded (equality) on A's tf refused. M-bind, M-upstream KILLED. The critic's B (`not(false)`, T=2) no longer pads identically (N7 forces m=2) — asserted. | src/verifiers/pcp.jl:190,196,256; src/ir/programs.jl:548,555; decouple5.jl:300,366; test:608 |
| N3 | option (b): DESIGN §1.1 instantiation clause (bytes vs bits, folded navigation) + `8.5`→`8.3` cite; **`c_Y` aligned to 3** (`_charge!(m,3)`, one line; `fixed` now uses 8 = unfolded + 3, M-cY KILLED) | programs.jl:1024, header; DESIGN.md:53; test (b) |
| N4 | `:CookLevinGeneral` names (1) observed-support function fit (2) no initialisation clauses (3) no counter/head/endmarker/track/ω; `:RawAnswerBlocks` names (4) no 2F reserved bits | cook_levin.jl:534; decouple5.jl:200 |
| N5 | C-N1, C-N3 registered → KILLED; `tb3_pcp` now has M-bind, M-upstream | tb3_r1.jl |
| N6 | `gate_budget=0` drives `CompilationRefused(1,0)` in cook_levin/decouple5/pad5; M-budget KILLED; §1.2 sentence landed after the test | test:506; DESIGN.md:164 |
| N7 | `2^m ≥ 2T` is a construction step (`_padded_width`), replay-checked and displayed; T=2 → m=2; M-width KILLED | decouple5.jl:241,274,292; test:499 |
| N8 | ASSUMED leaf `:PerIndexEqualityGadgets` (count of omitted indices) | decouple5.jl:210 |
| N9 | `Aborted` documented as host guard; `hard_cap ≥ fuel` asserted (`ArgumentError`) | programs.jl:801,1067,1074; DESIGN §1.1 |
| N10 | stated in the C10 row (critic's wording) and DESIGN §5.5 (equality fixture is the discriminating one; never reaches build_pcp) | DESIGN.md §5.5 |
| DESIGN | §5.5 replaced (dead gates named; "medium confidence/six-gate" retracted: normalization does NOT preserve TB0's circuit; T qualified by the charge clause; both witnesses to build_pcp, (ii) to TB2); §1.1 trace row split CHECKED (result/rows/contents pinned)/ASSUMED (general locality); §1.2 `general Cook--Levin locality | ASSUMED` row + CompilationRefused; §5 cold clause load caveat; definitions §F `FuelBound(P{Nat},P{Nat})`, `SortError`, Level/L rows | DESIGN.md:53,129,164,203,781,971; definitions.md:152,157,169 |
| gaps 1–6 | `YCode(P)=Fix(P)` exported; `halts_within` (charge 1+steps, one-tape MachineDesc bit format), `quoted_pair`/`fst_code`/`snd_code`, functional charges; Ψ_{M,λ} (stub Compress) EVALUATES: M_3 halting branch used 13 exactly, compressed branch 91, M_loop 92; `DECLARED_SORTS` checked in Quote/quote_program/specialize/Fix (hole names A) + `SortError(:eval_sort)`; FuelBound overflow → clamped budget, Value/OutOfFuel never SortError; specialize-the-body contract tested + noted; `Verifier` record with sort checks; M-halts/M-sort/M-fuelbound/M-verifier KILLED. Gap 8 not done (report only). | programs.jl:123,131,143,775,896,938,1089; test:694 |
**CROSS-LANE EDITS:** src/verifiers/pcp.jl only — `upstream_circuit` hook, `_same_tseitin`, `_bind_upstream`, the `upstream` keyword and node order in `build_pcp` (evidence nodes unchanged). Additive: MIPStarLambda.jl exports (`YCode, DECLARED_SORTS, FUNCTION_SORTS, sort_of, Verifier, upstream_circuit`); run.jl includes tb3_r1.jl; tb3_fuel.jl evidence re-keyed to `starved=Value` (the clamped inner budget now stops the loop by delimiter, not the hard cap — the mutant still fails 5 assertions).
**Deviations to flag:** Fix bodies admit no hole but `self_code` (constructor), so runtime `Specialize` on quoted code is vacuous (Quote admits only closed terms) — TB4 must specialize on the host or admit a partial-code sort (noted in §1.1). DESIGN's `Apply(Quote(Compress),…)` is not evaluable (Apply of code → SortError); test (h) uses `Eval(Apply(Compress,…),…)` — reading note added to §1.1, display unchanged. Test (a)'s `Quote(Lambda(1,·))` needed sort `:Sampler`; `Hole(:self_code,:Quoted)`→`:Program`/`:Decider`. `docs/analytic/parts/part2b.tex` L262–270 ("no bounded_trace … exists") is out of lane and still false.
**MERGE PROPOSALS:** (1) **C10 TESTED — the critic's authorized row VERBATIM** (verdicts/tb3-r1.md §5), with two factual corrections the round forces: "five **dead** NOT gates" stands; append "; the `2^m >= 2T` obligation is a construction step (T=2 pads to m=2, s=17, m'=32)"; and replace "charges `c_Y = 1`" by "charges `c_Y = 3`"; add M-bind, M-upstream, C-N2a/b, M-budget, M-width to the red-capable list. (2) **C16/C18 clause shrink:** replace "…a byte-unit fixed-width codec, and `c_Y = 1`; TB3 is evidence for that instantiation…" by "…a byte-unit fixed-width codec; `c_Y = 3` is now the analytic value (brief 63) and only the folded context navigation and the byte unit remain as deviations; TB3 is evidence for that instantiation, not for §8's constants". (3) C19 last sentence: add "…and `2^m >= 2T` is enforced by `pad5`".
