# Brief 46 — TB1 + TB2 repair r2 (work orders: verdicts/tb1-r2.md N1–N3 MAJOR, N4–N7 MINOR/NOTE; verdicts/tb2-r2.md N1 MAJOR, N2–N3 MINOR, O10/O11 PARTIAL) **plus** the §9 description-API preparation both critics flagged: make every CL map DESCRIBABLE

Proposer (Fable). Autonomous; no questions; **no git** (orchestrator commits). Lane: `src/samplers/**`, `src/verifiers/ldt.jl`, `src/verifiers/answer_reduce.jl`, `test/tb1_ld_sampler.jl`, `test/tb2_answer_reduce.jl`, `test/mutations/tb1_*.jl`, `test/mutations/tb2_*.jl`, `test/mutations/run.jl` (additively). TB0 files untouched. Report: `briefs/46-tb1-tb2-repair-r2-describable.last.md` (≤ 35 lines, response table).

Scratch: `/tmp/claude-1000/-home-tobias-Projects-discussions/548f633b-c857-457a-bb33-0aa1a7879403/scratchpad/tb1-tb2-r2/`

## Read order
1. `~/.claude/skills/rk-light/SKILL.md` (laws 1, 4, 5); `CLAUDE.md`; `docs/DESIGN.md` §1.5, §5.3–5.4, **§9 in full** (9.1 four query modes with prefix-addressed `Factor(n,w,j,u)` / `Linear(n,w,j,u,y)`; 9.2 replay; 9.3 `describe_cl` and `QuotedBranch`; 9.4 padding order = APPEND, zero map promoted to a whole-ambient stage-1 factor under `SOURCE_REPAIR(zero-map-factor-partition)`), `docs/definitions.md`; `claims/CLAIMS.md` C4a, C4b, C9 (all TESTED now — you may not change statuses; MERGE PROPOSALS only, including striking C4a's N3 scope clause once N3 is fixed).
2. `verdicts/tb1-r2.md` IN FULL and `verdicts/tb2-r2.md` IN FULL — every FIX DEMAND is binding: TB1 N1 (diagonal `md` bound red-capable: `tb1_dline_degree.jl`), N2 (four `@test_throws` on lazy `_child` validation reached via `apply` + do-block span check: `tb1_child_validation.jl`), N3 (rebuild `_build_L_Point` in ambient-factor form; `enu:cl-space-sum` replay for all three maps: `tb1_space_sum.jl`), N4 (`SOURCE_REPAIR :ld_off_line_rejects` node or comment naming `gt-07-ldt.tex:377-384`), N5 = TB2 N3 (`pad_level` APPENDS; `CLZero` padded to a whole-ambient zero stage 1 with the SOURCE_REPAIR tag; mutant swapping the order), N6 (`tb1_chifree.jl` NM5), N7 (print warm-memo and fresh-seed timings); TB2 N1 (`sample_answer_reduce_questions` is a projection of `sample(verifier.sampler, edge, seed)` — or equality asserted over ≥ 20 seeds × all 54 types; permanent summand-swap mutant KILLED), N2 (`_answer_reduce_replay_steps` asserts honest-accept AND corrupted reject per case with expected rule; ND4 KILLED), O10/O11 PARTIALs (all 8 TB2 mutants carry `expected_evidence`), N4/N5 NOTEs (loop order; compute the 54-type graph as `E^ora x E^pcp` and assert equality with the complete graph on this fixture).
3. Ground truth as cited in the two verdicts (recompute; never from memory): `gt-04-cl.tex` L122–L130 (rk:higher-level), L151–L180 (lem:cl-kth), L572–L601 (def:sampler); `gt-07-ldt.tex` L377–L384, fig:ld-decider; `gt-10-answer-reduction.tex` fig:decider-pcp.

## The §9 preparation (binding; this is what unblocks TB5 without a design round)
Both critics' forward-look sections list the same gaps. Close them here, additively, so TB5's `describe_cl` is a thin adapter:
- **`QuotedBranch`**: a named pure branch constructor with canonical captured data (the branch selector is a serializable term over the previous stage's value: e.g. `BranchByAxis(m)`, `BranchConst(child)`, `BranchByIndex(table)` — choose the minimal set that expresses all 3 TB1 and all 18 PCP maps) with `canonical_bytes` and `description_size`; anonymous Julia closures remain allowed in-memory but `describe_cl` returns `NotDescribable` for them (test both paths). Rebuild all 21 maps on `QuotedBranch`; assert every one is describable and print its exact description_size.
- **Prefix-addressed walks**: `walk_prefix(L, j, u)` splitting `u` by each stage's factor register; public `Factor(L, j, u)` returning a length-`Dimension` 0/1 indicator (domain `u in L_{<j}(V)`), and `Linear(L, j, u, y)` (domain the broader `u in V_{<j}`, per §9.1 — not narrowed); `Marginal`, `Dimension` already exist — expose them under the four §9.1 names.
- **Replay**: the §9.2 `enu:cl-space-sum` and `enu:cl-map-sum` replay as a reusable function over any `AbstractCL`, run on all 21 maps (exhaustive for TB1 at (8,2,1); declared branch-directed chain set for the (2048,…) PCP maps, with the chain_set_id / distinct-chain / completed-replay counts printed).
- **Memo**: bounded or evictable `_child` cache (N7/forward-look 6); assert the memo size stays bounded across 10^4 distinct `Linear` prefixes.
Each item gets a red test and a registered mutant (e.g. `M9-describe-closure` accepts an opaque closure; `M9-factor-not-indicator`; `M9-linear-narrowed-domain`; `M9-replay-skips-k`).

## Rules
- Red/green per objection (the critics' NM1–NM5, ND2, ND4 mutants are your red witnesses); every new assertion has a registered mutant shown KILLED under the unmutated-first runner (brief 42 adds it — if brief 42's runner change has landed in `run.jl`, keep it; if not, do not implement it yourself, note it).
- Whole suite under the TB0 gate warm on a quiet machine; runner exit 0, every rung's mutants KILLED; quiet and loaded walls reported.

## Report
Response table (TB1 N1–N7; TB2 N1–N5, O5/O10/O11): FIXED / DOWNGRADED / RESIDUE with file:line; the §9-prep table (item → API name → test → mutant → KILLED); description_size of all 21 maps; test summary + walls; full runner summary; CROSS-LANE EDITS; MERGE PROPOSALS: C4a with the N3 scope clause struck, C4b/C9 scope clauses updated (verbatim; proposals only); DESIGN §1.5/§9.3/§9.4 lockstep wording proposals.
