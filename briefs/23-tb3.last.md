# Brief 23 — TB3 quoted front end: report (Fable, 2026-09-05; tree at 1919aff + this lane, no git)

**RED** (`test/tb3_frontend.jl` written first, run before any front-end code existed): `ERROR: LoadError: UndefVarError: `Program` not defined in `Main`` — the file cannot even define its fixtures. After the first `src/` draft: `MethodError: Cannot convert Bool to Symbol` (`:true` is Julia's `true`, so Bool became a literal `PrimName`), then `TypeError` (clash with `Core.TypeError`, renamed `SortError`), then `certificate_replay Quote ... FieldError: BoundedTrace has no field bytes` (nested nodes receive the ROOT term), each fixed and re-run.
**GREEN**: `julia --project=. test/runtests.jl` → 807/807 (638 + 7 new TB0 5c + 162 TB3), exit 0, TB0 body **39.66 s** (< 45 warn / 60 gate), wall 143 s, quiet machine (`pgrep` showed only my processes). TB3 in-suite (a) 1.0 (b) 0.9 (c) 1.1 (d) 0.6 (e) 3.5 (f) 7.6 (g) 4.0 = **18.7 s** (≤ 20 s); standalone file 21.7 s.
**Sizes**: trivial D: |D| = 33 bytes, fnv1a64 f8561ef8c5761695; eval uses 3 (closure+beta+prim), h(d,u) = 63; trace T=1 rows 2 [row0 `Prim(true)` fuel 1 running; row1 `Value(true)` fuel 0 accept]; 3SAT m=1 M=2, 1 clause family (x_1 ∨ * ∨ *), 16/64 present, circuit 1 gate; 5SAT widths (0,0,1,1,1), 1 clause, 0 copy/equality gadgets; padded m=1 s=6 (live 1 = AND(x3,o3), 5 NOT padding) m'=16, 256/1024 present tuples, phi_C witnesses 512/1024; PCP |c0| (i)=10,140 (ii)=162,240, build 0.47 s / 2.7 s, TB2 decider 7/7 guard cases accepted (0.5 s), peak RSS 584 MB.
**(g) equality snapshot** `λnxyab.(a==b)`: T=2 → OutOfFuel (lookups cost 1 each under the C16 charge table, so T=3 is the minimal accepting fuel, used as the snapshot); rows 4; 3SAT m=3 M=8 15 clauses 0 eliminated; 5SAT widths (0,0,3,3,3) 47 clauses (15 formula + 4 copy + 28 equality); padded m=3 live 423 s=492 m'=512; F_arith candidates ≤ 6^69·7^423 ≈ 6.8e412, c0 ≤ 2.2e414; `arith_q` under the witness-(i) budget → **ExpansionRefused(279,936 > 160,000)** at the 7th gadget (2 s). φ5 satisfiable ⟺ a == b on all four answer pairs (DPLL), certificates all pass.
**Trace printout** (test (f), `traceprint` of the PCP certificate; full 17 lines in the test output): `[CHECKED] PCPProof` › `[CHECKED] Tseitin | variables = 16` › `[CHECKED] Pad5 | m = 1; s = 6 (live 1, padding 5 NOT gates); m' = 16; relation check = exhaustive (1024); fnv1a64 = f8561ef8c5761695` › `[CHECKED] Decouple5 | widths = (0, 0, 1, 1, 1); clauses = 1 (formula 1, copy 0, equality 0); ... exhaustive (256)` › `[CHECKED] CookLevin | m = 1; M = 2; clauses = 1; rows = 2; ... exhaustive (64)` › `[CHECKED] BoundedTrace | T = 1 body transitions (eval fuel T + 2); rows = 2; result = Value(true); accepts = true` › `[CHECKED] Quote | |D| = 33 bytes; fnv1a64 = f8561ef8c5761695; sort = Decider`; leaves `[CITED] CookLevinGeneral`, `[ASSUMED] RawAnswerBlocks`; then `ArithTseitin`, 5× `MultilinearExtension`, `BuildC0 | monomials = 162240`, `ZeroBasis | remainder = 0`, `PCPVerifier`. A borrowed front-end certificate on another proof is refused with `:certificate_binding`.
**Mutations** (baseline-first runner, each in isolation): `MUTANT TB3 M-acc accepting_trace_reports_reject target=tb3_cook_levin => KILLED` (evidence `MUTATION_EXPECTED_RULE trace_formula_equivalence`, before any PCP object); `M-size description_size_off_by_one target=tb3_quote => KILLED`; `M-fuel eval_ignores_fuel target=tb3_eval => KILLED` (loop's inner Eval budget 4e9 > host hard cap 1e6 steps → `looped=Aborted`, 10.7 s, no hang); `M-decouple slot1_reuses_block1 target=tb3_decouple => KILLED` (`literal_blocks=[1]`); `M-closure bounded_trace_accepts_closure target=tb3_quote => KILLED` (5-ary closure traced instead of refused). TB0 sweep: `R4M3 pcp_degree_replay_drops_quotients`, `R4M5 multilinear_anchors_collapse_to_g1`, `R4M2 certified_views_bypass_ev_z` all `=> KILLED` under `BASELINE tb0_core.jl TB0_TARGET=certificate => OK`.
**REGISTRY**: `MUTATION REGISTRY: killed=77/77 baselines ok=41/41 wall=506.07 s` (69 prior + 3 R4M + 5 TB3; exit 0; every mutant KILLED, none SURVIVED/LOAD-ERROR/UNATTRIBUTABLE).
**CROSS-LANE EDITS** (addendum 2 only): `test/tb0_core.jl` 5c +30 lines (N15 `@test_throws build_pcp(... bad_gs ...)`; N13 over-degree `c_16 = c_0·z_11`, inddeg 7, root replay refuses; N14 `g_3`-swap chimera → `:certificate_binding` at `:MultilinearExtension`); `docs/DESIGN.md` §5 one clause (N16: r3 with/without delta kept, r4 figure 124.4 s critic / 128.5 s proposer); `test/mutations/run.jl` three TB0 mutants. Lane files: `src/ir/programs.jl` (IR, codec, CEK evaluator, specialize), `src/frontend/{bounded_trace,cook_levin,decouple5,precompile_frontend}.jl`, `test/tb3_frontend.jl`, `test/mutations/tb3_*.jl`; additive: `MIPStarLambda.jl` (includes/exports), `runtests.jl` (one include), `run.jl` (tb3 rung + 5 mutants).
**API REQUESTS**: (1) `build_pcp`'s `_bind_evidence` admits only proof components, so upstream description evidence is grafted as an extra child of the `:Tseitin` node and every front-end replay falls back to its captured term when `verify_certificate` hands it the root term — a first-class upstream-evidence slot (or per-node terms) would remove the fallback. (2) `Circuit.gates::Tuple` compiles fine to ~500 gates; a vector-backed circuit is needed past the 4096-gate `CompilationRefused` budget. (3) `src/precompile.jl` untouched; the TB3 workload lives in `src/frontend/precompile_frontend.jl` (image build grows; `tools/cold_precompile.sh` measured **188.2 s** after this round vs 124.4 s at r4, i.e. the TB3 workload adds ~64 s to the cold build; recorded in the same DESIGN §5 clause).
**Deviations**: canonical bytes are the cl.jl family (0xC2 header, tag bytes, UInt32-BE ints via `_encode_int!`, count-prefixed children) so |d| is in BYTES where the analytic doc's ν-encoding counts bits; `TypeError` outcome is `SortError`; `Quote` carries its sort A; Bool/Int/bits literals are `PrimName`s; `bounded_trace` rows count BODY transitions (row 0 = body with the argument frame), eval(D,u;T+2) installs the frame — hence T=1 for the trivial decider and T=3 (not 2) for `a==b`; Cook-Levin alphabets are enumerated over all 2^(|a|+|b|) answer inputs with per-field support by projection (CITED leaf `CookLevinGeneral`), answer blocks are raw bits (ASSUMED leaf `RawAnswerBlocks`, no enc_Γ/(10)^{L/2-T} clauses), short clauses are don't-care families, equality gadgets only for multi-slot variables; forced internal variables are unit-propagated (interface bits retained); (g) never reaches `build_pcp`.
**MERGE PROPOSALS** (proposals only): new row **C10** (TESTED proposed): "Front-end faithfulness on the fixtures: for D=λnxyab.true (T=1) and D=λnxyab.(a==b) (T=3), one-bit answers, `bounded_trace` rows replay from the canonical bytes; `cook_levin`'s 3SAT is satisfiable iff D accepts on all four answer inputs (DPLL, and exhaustively over 2^M for the trivial fixture); `decouple5`'s five-block relation is satisfiable iff D accepts (exhaustive over 256 witnesses / DPLL); `pad5` gives m=1, s=6, m'=16 with the padded relation equal to the decoupled one on all 1024 tuples; the GENERATED circuit passes `build_pcp` for both witnesses and TB2's decider accepts the honest strategy on the seven guard cases; red-capable by M-acc/M-size/M-fuel/M-decouple/M-closure" (depends C1, C9, C16, C19). **C16/C19**: tests (b) and (a) are the first machine evidence for C16 (single-valued deterministic CEK, fuel monotonicity with exact usage, `Eval_L(Quote(t),u;f+h(d,u)) = eval_L(t,u;f)` with `h = 3+|d|+|enc(u)|` used exactly, `|Specialize|` identity exact); tests (c)–(e) the first for C19 on a fixture — both stay SKETCH pending a verdict. **DESIGN §5.5**: replace "extensionally equal to TB0's 128-clause relation … six-gate circuit / 128/896" by the generated one-live-gate relation (256/1024 present, 512 witnesses, `AND(x3,o3)` + 5 NOT padding); record T = body transitions and the equality decider's T=3. **§1.1**: `SortError` name, `Quote(code, sort)`, literal `PrimName`s, byte-unit |d|, `Aborted` host guard. **§1.2**: add `CompilationRefused` (gate budget) beside `ExpansionRefused`.
