# Brief 67 — CRITIC verdict r6 on rung TB1 after repair r5 (brief 65) — intended closing round

You are the adversarial critic (Opus). ATTACK; do not summarize. Autonomous; no questions. Lane: write `verdicts/tb1-r6.md` ONLY; Julia/files only under scratch; never edit repo files; no state-changing git. Evaluate the ARCHIVED tree at commit `<SHA>` (`git archive <SHA> | tar -x -C <scratch>/tree`; instantiate there; cold precompile ~90 s). Never read src/test from the live tree; `claims/CLAIMS.md` may be read live.

Scratch: `/tmp/claude-1000/-home-tobias-Projects-discussions/548f633b-c857-457a-bb33-0aa1a7879403/scratchpad/critic-tb1-r6/`

Read: `~/.claude/skills/rk-light/SKILL.md`; `CLAUDE.md`; `docs/DESIGN.md` §1.5, §5.3 (DD-4 item 4 as amended), §9.4; `claims/CLAIMS.md`; `verdicts/tb1-r5.md` (your prior — the work order); `briefs/65-tb1-tb2-repair-r5.md`, `briefs/65-tb1-tb2-repair-r5.last.md` (response table, runs, the one cross-lane edit in test/tb3_frontend.jl, MERGE PROPOSALS — the DESIGN sentences are applied by the orchestrator at <SHA>; the CLAIMS edits await your authorization).

Obligations: run suite (summary + TB0 wall + `uptime`) and runner (`MUTATION REGISTRY` line; all 101 KILLED; baselines ok). Adjudicate every r5 row: ACCEPTED / REJECTED / PARTIAL. Recompute on a COPY: N29 variant (b): on a zero-direction line every t in F_q is compared — verify with your own degree-1 cheat at seed (0,0,0,0,0) (must now be REJECTED at location 1 both orders), recount 1,024 degenerate decisions and off_line 0; NM15 KILLED; N30 direct_sum normalisation (full+empty → full register; empty+empty → empty then promoted; sub-register transported and refused); N31 declared order pin; tb1_kappa re-headed mutant still meaningful. Two NEW semantic mutations; survivors MAJOR with the red test as FIX DEMAND. Per-claim: C4a (re-affirm with the brief-65 N30 sentence) and C4c (PROMOTE to TESTED with your pre-written variant-(b) row, or HOLD) — AUTHORIZED verbatim row text. This is intended as the closing round: if no MAJOR remains, say PASS and list residual MINOR/NOTE items for the record (TB5-routed items go in a NOTE for brief 39) without demanding another round.

Output: `verdicts/tb1-r6.md` — adjudication table; recomputations; new objections; runs + walls + load; your mutations; per-claim block; final line exactly `VERDICT: PASS` or `VERDICT: FAIL(<ids>)`.
