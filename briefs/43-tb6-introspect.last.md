# Brief 43 — TB6 Introspect report (Fable proposer, 2026-09-05; NO-GIT, tree 8af9073 + working changes incl. brief 77; lane: src/introspect/{meter,linear,pauli_types,pauli_params,pauli_sampler,pauli_decider,stabilizer,intro_decider,honest,introspect}.jl, test/tb6a_audit.jl, test/tb6b_introspect.jl, test/mutations/tb6_introspect.jl; additive edits to src/MIPStarLambda.jl, test/runtests.jl, test/mutations/run.jl)
**RED (tests first).** `test/tb6b_introspect.jl` (g) against the literal decider text: `Test Failed at :557 Expression: bit` on **128 honest leaves** (every Hide_3/Read, Hide_1/Hide_2, Hide_2/Hide_3 leaf) — `8720 passed, 130 failed` (scratch `tb6/tb6b_run6.log`); also (h)'s `lem:commute` criterion false and (g):542's dual-map check `Lp*[1,0] == [0,1]` false. Cause: gt-08:L669-L678 takes S = the CANONICAL COMPLEMENT of a kernel basis as "a basis for ker(L)^⊥"; on TB6b-M's stage-2 map `[[1,1],[0,0]]` (kernel ⟨e4+e5⟩) that gives ⟨e5⟩, whose X-family anticommutes with L's Z-family. GREEN after `SOURCE_REPAIR(intro-perp-orthogonal)` (below). Earlier reds: the suffix register of enu:hiding-same (30 of 64 honest Hide_1/Hide_2 leaves rejected under the literal `V_{>k+1}(y_w)`), the stabilizer tableau's destabilizer phases, `Tableau` name clash with the front end.
**TB6a** (`tb6a_audit.jl`, three testsets, 139 assertions): TypePauli 26 types / 30 undirected (hand list from fig:type-graph-ms L586-L592 + fig:type-graph-pauli L1055-L1061) / **86** oriented; TypeIntro ell=1,3,9: types 34/38/50, undirected 41/45/57 (= 2ell+39, hand list from gt-08:L294-L310), oriented **116/128/164** (= 6ell+110), Hide-incident oriented 10/22/46; every schema generated at (2,1,1) and (8,2,1) (Point k, ALine (d+1)k, DLine (md+1)k, Pair 2, PairW 1, Constraint 3, Variable 1, PauliW Q, Hide exactly 3Q, Introspect/Sample/Read Q-bit vector fields + a); guard census item1..7 = 26/4/2/2/2/54/18 ordered pairs, and the executable `_pauli_guard` fires on exactly those 82 non-loop pairs; the emitted `intro_query_plan(ell)` equals the §11.4 schedule (Dimension first; Hide_k/Hide_{k+1}: Factor(1,0) for w, Factor(1,0)/Marginal(k,y)/Factor(k+1,u) chain for w̄, Linear on every basis vector of V_{k+1}). `_require_image` at level 5: **dimension 142 → 74,671 steps (148 child calls); 179 → 113,946 steps (185 calls)**; step ratio 1.526 > dimension ratio 1.261 (SUPER-LINEAR: the graph stage's 2|Type|-column elimination is cubic). Choice: NOT switched in this brief — the column-space rebuild is the only query-purity-preserving reachability test at the parent; the stored-stage-matrix alternative is the `(:Pad, extra, S)` grammar node of verdicts/tb5-r1.md O12, requested for TB7 (API REQUEST) where 206→1696 makes it binding.
**GREEN.** Suite `julia --project=. test/runtests.jl`: **12105/12105, exit 0**, 2m07.2 s wall (TB0 body 16.529 s, ratio 36.8 under the brief-77 gate; TB6 body 25.85 s: TB6a audit 2.49 s (+5.8 s `_require_image` measurement), TB6b (a)–(i) ≈ 18 s incl. first-use compilation). Walls (warm, in-suite): E construction **0.152 s**, E transcripts **2.09 s** (14,378 enumerated leaves + 256 draws), M construction **0.069 s**, M transcripts **1.62 s** (8 directed = 8,624 leaves, 512 seeded, 128 literal); combined 3.93 s; TB6b maxrss delta 155.8 MiB standalone (0.0 in-suite); one Hide_2/Hide_3 transcript allocates 0.086 MiB (26-qubit tableau, no state vector). The DESIGN 11.6 "TB6a < 1 s" target is missed by first-use JIT (2.49 s); the in-suite gate is the measured ceiling 5 s (disclosed).
**Policy report TB6b-E (as printed):** `R_at_least_4 PASS (4>=4); admissible_field PASS (2=2^1, k odd); m_divides_q PASS (1|2); d_equals_1 PASS; capacity_s_le_R PASS (1<=4); capacity_M_ge_R FAIL (2<4); capacity_M_le_Q PASS (2<=2); consequent_Q_ge_R FAIL (2<4); embedding_Q_ge_s PASS (2>=1); canonical_introparams FAIL (matching exponent 1 = c*1+1 requires c = 0, forbidden (c even, c >= 2); least allowed even c=2 -> m=4; tuple m = 1); description_le_lambda FAIL (|V| <= lambda: 66 bytes > 1; |S|=66, |D|=2); TIME_child_{Dimension,Marginal,Factor,Linear,Decider}_le_R NOT_EVALUABLE(owner=tb6-child-meter) at construction; toy_child_fuel FAIL(owner=tb6-child-meter) (F_child = R: 65536 != 4); low_degree_margin VACUOUS (dm/q = 1/2 >= 1/2); hiding_same_guard_set VACUOUS (k in {1..ell-1} = {})`.
**Policy report TB6b-M (as printed):** `R_at_least_4 PASS (16>=4); admissible_field PASS (8=2^3, k odd); m_divides_q PASS (2|8); d_equals_1 PASS; capacity_s_le_R PASS (6<=16); capacity_M_ge_R FAIL (4<16); capacity_M_le_Q PASS (4<=12); consequent_Q_ge_R FAIL (12<16); embedding_Q_ge_s PASS (12>=6); canonical_introparams FAIL (matching exponent 3 = c*2+1 requires c = 1, forbidden (c even, c >= 2); even that forbidden c=1 gives m=4; least allowed even c=2 -> m=8; tuple m = 2); description_le_lambda FAIL (388 bytes > 2; |S|=388, |D|=10); TIME_child_*_le_R NOT_EVALUABLE(owner=tb6-child-meter) at construction; toy_child_fuel FAIL(owner=tb6-child-meter) (65536 != 16); low_degree_margin PASS (dm/q = 1*2/8 = 1/4 < 1/2 (live)); hiding_same_guard_set PASS ({1..2})`. Both certificates' `HypothesisAudit` therefore REFUSES (`hypothesis_violated` at `lambda_bounded_description`), asserted; every other CHECKED replay passes (asserted node by node).
**Ten cost slots (exact metered maxima over the honest child-call records; unit = one primitive interpreter step).** TB6b-E (R=4, F_child=65,536; 130 records, one honest trace per oriented pair): Dimension **5** (TIME_child<=R FAIL, fits F_child PASS); Marginal **13** (FAIL/PASS); Factor **10** (FAIL/PASS); Linear **13** (FAIL/PASS); child decider **10** (FAIL/PASS). TB6b-M (R=16; 18,311 records): Dimension **5** (PASS/PASS); Marginal **53** (FAIL/PASS); Factor **60** (FAIL/PASS); Linear **50** (FAIL/PASS); child decider **22** (FAIL/PASS). Consequences printed and asserted: under the source budget every honest Introspect/Sample transcript times out at exactly R (records `steps = 16 = supplied_fuel`, outcome `timeout`) and the decider rejects — acceptance under the source gate is withdrawn; under F_child every record returns and the operative-toy acceptance is PASS only after this fit. **Charge table (M child at z*, by depth):** Dimension 5 = 1+2 index bits+1 mode+1 output; Marginal(3) 53; Factor(2,e1) 39; Linear(2,e1,e4) 40; unit: input decoding 1 per symbol (+1 per index bit), control transfer 1, register read/write 1 per element, multiply-accumulate 1 per (row, column) field element, branch selection 1, canonical-elimination row op 1 per (row, column), output serialization 1 per element — proposed as DESIGN §11.4's definition of the unit (`Meter.steps`, budget semantics: the (budget+1)-th step is never executed, a return at step budget is permitted; `by_depth` attributes per machine depth).
**Rejection counts.** TB6b-E: literal `>=3Q` rejects exactly **10 of 116** oriented pairs (= the Hide-incident set, honest Hide answers 6 = 3Q bits, every other honest answer ≤ 5 bits); `P_intro_operative = 1` (every leaf of every pair, total mass 1 per (pair, seed)), `P_intro_literal = 53/58 = 106/116`. TB6b-M: literal rejects exactly **22 of 128** (36 = 3Q bits); `P_intro_literal = 53/64`, operative 1; 512/512 seeded typed+detyped accepts; T6-view-swap: typed reject, detyped reject preserved (`game` fired). Literal register of enu:hiding-same rejects 30 of 64 honest Hide_1/Hide_2 leaves; the literal canonical-complement dual map rejects 48 of 64.
**Certificate tree, Introspect(TB6b-M)** (82 nodes: CONSTRUCTED 7, CHECKED 25, CITED 18, ASSUMED 23, SOURCE_REPAIR 9; every CHECKED node has a replay; every CITED leaf's `\label{}` sits inside its line range):
`[CONSTRUCTED] Introspect (lambda=2, ell=3, (8,2,1), N=4, R=16, Q=12; field 2; level 5; dimension 179; |S^intro| = 24 B, |D^intro| = 1996 B; F_child = 65536)`
`  [ASSUMED] lambda_bounded_description => FAIL (388 > 2)   [ASSUMED] lambda_bounded_time => NOT_EVALUABLE   [ASSUMED] ell_level => PASS (3 = 3)   [CHECKED] HypothesisAudit (refuses: hypothesis_violated)`
`  [CITED] thm:introspection gt-08:L784-L817   [CITED] lem:commute gt-08:L923-L953   [ASSUMED] toy_override → 19 policy leaves (above)`
`  [SOURCE_REPAIR] intro_3Q_guard (L424-L425 vs L588-L591)   [SOURCE_REPAIR] intro_hide_suffix_register (L464-L473, L641-L657)   [SOURCE_REPAIR] intro_perp_orthogonal (L669-L678)   [CHECKED] SamplerIndependence (deps {b3c0aac09c1a7d26}, fresh bytes equal)`
`  [CONSTRUCTED] Detype → [CITED] lem:detyping-verifiers; [CONSTRUCTED] DL9-detype {LawCert, DescriptionSize 24 B, DependencySet, SamplerValidity, MeteredCalls ≤1, CITED ×3} ⊃ [CONSTRUCTED] DL9-downsize {5 CHECKED rows, CITED ×2} ⊃ [CONSTRUCTED] TildeSIntro {LawCert (8,3,9), DescriptionSize 22 B, DependencySet {S}, SamplerValidity ⊃ SOURCE_REPAIR zero_map_factor_partition, MeteredCalls 0, GraphTranscription 38/45/128, SOURCE_REPAIR zero_map_factor_report, pad_level ⊃ SOURCE_REPAIR, CITED lem:intro-sampler-complexity, fig:type-graph-pauli, def:typed-sampler, lem:cl-kth}`
`  [CONSTRUCTED] DetypeDeciderDescription {CHECKED DetypeDecider, DescriptionSize 1996 B, DependencySet} ⊃ [CONSTRUCTED] IntroDeciderDescription {CHECKED IntroDecider, DescriptionSize 953 B, DependencySet {hash(S), hash(D), F_child, ell, introparams, lambda}, CITED fig:intro-decider, lem:intro-decider-complexity, fig:decider_pauli, thm:pauli, def:cl-canonical, def:Lperp, SOURCE_REPAIR ×3, relocated DiagnosticDecider certificate}`.
**Mutations (`test/mutations/tb6_introspect.jl`, baseline-first runner):** M6-pauli-edge KILLED; M6a-count KILLED; M6a-guard KILLED; M6-pauli-gamma KILLED; M6-sampler-nonzero KILLED; M6-N KILLED; M6-factor-prefix KILLED; M6-perp KILLED; M6-game KILLED; M6-boundary KILLED; M6-noncommuting KILLED; M-factor-partition (a zero-matrix stage reports the literal all-zero factor) KILLED; M-detype-view-orientation KILLED; M-intro-fuel (production budget R²) KILLED; M-intro-fuel-after (budget checked after the step) KILLED — 15/15.
**Runs (quiet; `pgrep -fa 'runtests|mutations/run'` showed only mine).** `MUTATION_JOBS=4 julia --project=. test/mutations/run.jl`: **`MUTATION REGISTRY: killed=182/182 baselines ok=75/75 wall=1021.09 s`, exit 0** (167 after brief 77 + the 15 TB6 mutants; `uptime` 16:04:11 load 1.18 → 16:21:12 load 2.05). Suite before the registry: **12105/12105, exit 0**, 2m07.2 s (TB0 16.529 s, ratio 36.8); after: **12105/12105, exit 0**, 2m07.2 s (`/usr/bin/time` 2:09.2, maxrss 1807 MiB; TB0 16.205 s, ratio 30.8; TB4 5.38 s; TB5 0.258/0.506 s; TB6 25.9 s: E 0.213 + 2.066 s, M 0.069 + 1.608 s; `uptime` 16:21:12 load 2.05 → 16:23:21 load 1.17). A first post-TB6 registry (before the TB6a gate was set to the measured ceiling) had every TB6 mutant KILLED and the suite baseline broken only by the 1 s TB6a gate (`tb6/registry_tb6_1.log`).
**CROSS-LANE EDITS (all additive, TB0–TB5 green, every affected TB5 mutant re-anchored):** `src/descriptions/machines.jl` — `Meter` gains `steps/budget/by_depth`, `_charge!`, `FuelExhausted`; charges in `_forward`, `_validated_answer` (input decoding, sizing probe on the same meter, output serialization), block splits/concatenations, padding, Detype/Downsize/Anchor/Repeat machines; leaf queries route through the metered walk (`src/introspect/meter.jl`, exhaustively re-derived against `marginal_k` by TB5 (a)); `downsize` admits q = 2 (kappa = 1, identity); `AbstractGraphMachine` so `GraphMachine` shares the graph-register helpers; the three primitive tags in `compile_sampler`. `src/descriptions/sorts.jl` — tags `:Pauli 0x09`, `:Intro 0x0A`, `:Graph 0x0B` (leaves for the dependency walk). `src/descriptions/transformations.jl` — `_term_laws` for the primitives. `src/descriptions/deciders.jl` — `(:TypedDecider, labels, body)` 0x06 with bodies `:Pauli`/`:Intro`, `(:ZeroAnswers, coords)` 0x07, `_decider_typing` reads labels from the bytes (`:TypedAnchor` kept as the compact byte spelling of the `TypedDecider(["Game","Anchor"])` instance, so TB5's |D^anch| = 46 / |D^rep| = 63 are unchanged), dependency symbols, `_decide_typed` dispatch, `trace::Vector` (Any[] when a TypedDecider is embedded). `src/MIPStarLambda.jl` includes/exports; `test/runtests.jl` includes tb6a/tb6b; `run.jl` rungs `tb6a_`/`tb6b_`.
**DESIGN wording proposed (addendum).** §11.4: "The child fuel unit is one primitive step of the universal interpreter machine executing the child description, counted by `Meter.steps`: one per decoded input symbol (plus one per index bit), per control transfer, per register read or write, per multiply-accumulate over one field element, per branch selection, per canonical-elimination row operation and per serialized output symbol; a whole vector operation or child query never counts one. The budget refuses the (budget+1)-th step before it executes and permits a return at step budget; the sizing Dimension probe runs on the same meter; `by_depth` attributes steps per machine depth." §9.5: "The stored type graph is the oriented pair set: an undirected source graph is stored as both orientations of every non-loop edge plus every loop (G^pauli: 26 loops + 30 non-loops = 86 oriented pairs; G^intro: 2ℓ+39 non-loops, 6ℓ+110 oriented pairs), asserted against the undirected transcription, and `detype_decider` iterates the oriented list." §9.6: "The primitive sampler-producing signatures `pauli_sampler(q,m,d)` (tag :Pauli), `tilde_S_intro(λ,ℓ; tuple)` (tag :Intro) and `graph_sampler(G)` (tag :Graph) carry the mandatory output-sampler replay row, the exact byte length and the `{S}` dependency set; `TypedDecider(TypeSet, body)` is the general typed decider term whose bytes carry the labels, with `pauli_decider` and `typed_intro_decider` as instances and `:TypedAnchor` as the compact spelling of the anchor instance."
**API REQUESTS (TB7).** (1) Lowering the step unit into the §1.1 `Eval` fuel (`FuelBound`) — not done here, as the addendum allows. (2) Nested typed introspection deciders as metered children (`_metered_decide_typed` refuses a `TypedDecider` child). (3) The `(:Pad, extra, S)` grammar node replacing `_require_image` (super-linear at 142→179; binding at TB7's 1696). (4) `REPEAT_CONTRACT`'s `completeness_decider_time` index and `_normal_form_status` display (verdicts/tb5-r1.md O11) live in `src/compress.jl`, outside both lanes.
**Deviations from §11 and the gt line that forced each.** (1) `SOURCE_REPAIR(intro-perp-orthogonal)`: gt-08:L669-L678's "canonical complement S of F, a basis for ker(L)^⊥" is not the orthogonal complement unless ker(L) is a register subspace (gt-03:L333-L340); the executable takes the orthogonal complement (def:Lperp gt-03:L386-L392, lem:L_perp_perp), else lem:commute fails and honest Read/Hide answers are rejected (128 leaves, RED above). (2) `SOURCE_REPAIR(intro-hide-suffix-register)`: enu:hiding-same (gt-08:L464-L473) projects x_w onto `V_{>k+1}(y_w)` whose stage-(k+1) register is selected with y_{w,k} = 0; on a prefix-dependent register (TB6b-M stage 2; every detyped sampler's revealed-type stage) it differs from `V_{>k+1}(y_w̄)` and the honest x_w (X on V_{>k}(y_w), gt-08:L1166-L1169) is rejected (30/64 leaves); both suffixes are taken on `V_{>k+1}(y_w̄)`, the register the footnote L652-L657 singles out. (3) Pauli answers are kappa-encoded in the SELF-DUAL NORMAL basis of lem:pauli-binary (gt-03:L1173-L1198; F_8: {3,5,7}), not the downsize polynomial basis of the questions, so a (Pauli,W) answer IS the qubit-outcome vector and γ equals the symplectic product (asserted). (4) The embedded L_Point/ALine/DLine put V_{W̄} ⊕ V_r into their stage-1 factor under the zero map (gt-07:L1085-L1101 footnote: "embedded in the natural way"; the source fixes no partition). (5) The typed intro decider embeds the toy tuple and F_child in its bytes (DD-28: machine-visible); |D^intro| = 953 B (M), 1996 B detyped. (6) `_metered_node` pins Factor/Linear exactly (brief 77 O2). (7) TB6a's 1 s target missed (JIT) — measured 2.49 s, gate 5 s.
**MERGE PROPOSAL (proposal only) — C14:** "(TB6 Introspect) `introspect(V, lambda, ell; tuple, F_child)` is executable end to end on TB6b-E (n=2, N=4, λ=1, ℓ=1, (2,1,1), dimension 6→142) and TB6b-M (λ=2, ℓ=3, s=6, (8,2,1), 27→179): TypePauli/G^pauli (26/30/86) and TypeIntro/G^intro (32+2ℓ, 2ℓ+39, 6ℓ+110 at ℓ=1,3,9) are CHECKED against hand transcriptions; the Pauli family, tilde S^intro and graph_sampler carry the §9.6 replay rows with the promoted zero maps (`SOURCE_REPAIR(zero-map-factor-partition)`); `D^pauli` executes all eight guards in both orders on hand-built accept/reject transcripts; the typed introspection decider reads V only through the four queries and one decider call (recording log == the §11.4 schedule; M6-N red), each under the step meter with budget N^λ in production (honest calls time out at exactly R=16 and the decider rejects) or F_child = 65,536 in toy mode (`toy_child_fuel = FAIL(owner=tb6-child-meter)`); the ten cost slots are measured (E 5/13/10/13/10, M 5/53/60/50/22 steps); the exact stabilizer simulation of the honest strategy accepts every leaf of every oriented pair of E (14,378 leaves, mass 1) and every leaf of eight directed M transcripts plus 512 seeded draws, typed and detyped; the literal ≥3Q guard rejects exactly 10/116 and 22/128; `T6-view-swap` rejects typed and detyped; two further source defects are disclosed and repaired (`intro-perp-orthogonal`, `intro-hide-suffix-register`); the thm:introspection ASSUME clause |V| ≤ λ FAILS on both toy quotes so the HypothesisAudit refuses and no theorem conclusion is invoked; `thm:pauli`, `thm:introspection`, `lem:intro-sampler-complexity`, `lem:intro-decider-complexity`, `lem:commute` stay CITED. Fifteen owned mutants KILLED." Status proposal: CONJECTURE → TESTED at the critic's discretion (depends-on C12, C13; where-tested test/tb6a_audit.jl, test/tb6b_introspect.jl; red test/mutations/tb6_introspect.jl).
**Unresolved.** `_require_image` not switched (measured super-linear, choice reported); TB6a 1 s target; the Pauli-decider certificate is relocated under the intro decider only as CITED leaves (its own replay runs in `pauli_decider`'s certificate, exercised by (b)); `lambda_bounded_time` stays NOT_EVALUABLE (Opaque TIME laws); the production `introparams(R)` AST is built but never numeric (a, b symbols).
