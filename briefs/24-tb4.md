# Brief 24 — TB4 implementation (red/green TDD): the Compress skeleton with contracts, Hole/Specialize, and the quoted fixed point D_{M,L} = Y Ψ_{M,L}

Proposer (gpt-5.6-sol, xhigh). Autonomous; no questions; no git. Lane: `src/compress.jl` (new), `src/ir/programs.jl` (additive edits for Hole/Specialize/YCode only if TB3 did not provide them — coordinate via "CROSS-LANE EDITS"), `test/tb4_compress_ir.jl`, `test/mutations/tb4_*.jl`, MINIMAL additive edits to `src/MIPStarLambda.jl`, `test/runtests.jl`, `test/mutations/run.jl`. Report: `briefs/24-tb4.last.md`.

## Read order
1. `handoff.md` §"The fixed-point part" (Ψ_{M,L}, D_{M,L} = Y Ψ_{M,L}) and §"The compression skeleton" (Compress = Repeat ∘ AnswerReduce ∘ Introspect and the five contracts). `CLAUDE.md`; rk-light law 4/5.
2. `docs/DESIGN.md` §1.1 (grammar incl. Hole; YCode; the displayed Ψ_{M,L} term; DD-1/DD-2), §1.6 (Verifier sort; the four ASSUME/PROVE contracts for Introspect/AnswerReduce/Repeat/Compress; StubVerifier; DD-9; `detype` CITED), §2 (`Introspect`, `AnswerReduce`, `Repeat`, `Compress` signatures), §3 (certificate grammar; trace printer; DD-10..12), §5.6 (TB4 instance: two-state machine M, symbolic L; specialize the quoted body; Compress returns a 9-level StubVerifier; |D_{M,L}| from canonical bytes; the trace has CHECKED program/specialization nodes, the CHECKED PCP subtree, and exactly-named CITED leaves for introspection, quantum answer reduction, repetition, compression; mutations: composition order; relabel a CITED leaf as CHECKED; the design-r4 MINOR residues R9 (Lambda argument sorts) and R10 (Apply(Quote(Compress),…) sort) — fix them in the IR and propose the DESIGN wording). `docs/definitions.md`.
3. Ground truth: `ground-truth/gt-12-compression.tex` fig:compress, thm:compression (statement), lem:compress-independent-samplers; `ground-truth/gt-08-introspection.tex` thm:introspection statement only; `ground-truth/gt-11-parallel-repetition.tex` thm:repetition statement only; `ground-truth/gt-10-answer-reduction.tex` thm:ar statement; the halting-problem verifier in gt-12 (sec "An MIP* protocol for the Halting problem": how the paper's F(F,M,L,…) self-application is written) — cite lines.
4. Existing code: TB3 program IR; TB2 `answer_reduce` entry; certificates.

## Process
1. Tests FIRST (RED excerpt): (a) `Hole`/`Specialize`: specializing a quoted body with holes yields a closed Quoted with a SubstCert; capture-freedom test (a hole under a binder of the same name); (b) `YCode(f)` satisfies eval(YCode(f), u; fuel) == eval(f(YCode(f)), u; fuel) on the two-state machine for all fuels ≤ 6 where both terminate, and is represented by `Fix` (no Julia recursion: test by inspecting the term); (c) `Ψ_{M,L}` is a term of the language (well-sorted per §1.1 after the R9/R10 fixes) and `D_{M,L} = Apply(YCode, Ψ)` has an exactly computed `description_size`; (d) `Compress(D, λ)` returns `Checked{StubVerifier, CompositeCert}` whose certificate tree has: CHECKED Quote/Specialize nodes, the CHECKED PCP subtree from TB0/TB2 (AnswerReduce is the executable part — call the real `answer_reduce_pcp` on the TB3/TB0 fixture), and CITED leaves named exactly `thm:introspection`, `thm:ar` (quantum part), `thm:repetition`, `thm:compression`, each citing gt file + line range that EXISTS (test greps the ground truth for the label); (e) each contract is an ASSUME/PROVE object: constructing `Introspect(V, λ, 9)` on a V that violates a hypothesis (e.g. not λ-bounded per definitions.md) returns a certificate with an `Assumed`/violated-hypothesis node, never silently PASS; (f) the composed verifier reports level 9 and both runtimes as symbolic `poly(n, λ)` bounds (symbolic-bound grammar of §3) derived by composing the three contracts' level/runtime rules — check the arithmetic max(ℓ+2,5) etc. against the theorems; (g) `verify_certificate` rejects a tree in which a CITED leaf was relabeled CHECKED (must fail because CITED nodes have no replay).
2. Implement; GREEN; paste summary, walls, and the full trace printout of Compress on the fixture (this printout is a deliverable for the tutorial).
3. Mutations: composition order (Introspect∘AnswerReduce∘Repeat) → level/contract test fails; relabel CITED→CHECKED → verify_certificate fails; YCode implemented by Julia recursion → term-inspection test fails; drop a hypothesis from the Introspect contract → the violated-hypothesis test fails.
4. Time: TB4 ≤ 5 s (no field sweeps).

## Report (≤ 30 lines)
RED; GREEN; trace printout; mutation lines; the exact list of CITED leaves with their gt line ranges; MERGE PROPOSALS: DESIGN §1.1 R9/R10 wording; new claim row C11 ("Compress skeleton composes the three contracts with correct level/runtime bookkeeping on the fixture; all non-PCP leaves CITED", proposed TESTED) — proposals only.
