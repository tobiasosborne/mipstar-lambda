# Brief 31 — REPAIR round r1 for DESIGN v2 (docs/DESIGN.md §9–13 + docs/definitions.md §H); work order = verdicts/design-v2-r1.md

You are the proposer (gpt-5.6-sol, xhigh). Work fully autonomously; do not ask questions; do not run git; do not run Julia. Lane: `docs/DESIGN.md` (§9–13 and the §13.3 MERGE PROPOSALS only; §1–8 are converged — touch them only where a §9–13 fix forces a cross-reference), `docs/definitions.md`, and a new `docs/design-v2-repair-r1-response.md` ONLY. Do NOT edit claims/, src/, test/, docs/analytic/ (other workers own them). Put any instruction for TB5/TB6/TB7 implementers under "DIRECTIVES FOR TB5–TB7" in the response file.

Read: `~/.claude/skills/rk-light/SKILL.md` (law 5: repair by response table); `verdicts/design-v2-r1.md` IN FULL (O1–O10 MAJOR with FIX DEMANDS, the MINOR list, NOTES, the three offered mutations, recomputations R(a)–R(d), the MERGE PROPOSALS adjudication, the per-claim recommendation for C12–C15); `briefs/28-design-v2-full-compress.md` and `.last.md`; the ground-truth labels the critic cites (recompute from `ground-truth/gt-*.tex`, never from memory): `gt-04-cl.tex` lem:cl-kth, lem:cl-concat, lem:cl-func-prod, lem:cl-dist-prod, def:sampler; `gt-08-introspection.tex` L424–L425 and L588–L591 (the `3Q` guard and the honest Hide answer), thm:introspection (the entanglement-floor branch), rk:higher-level; `gt-10-answer-reduction.tex` L1951–L1955 (E^ar tensor product), L2060 (enu:ar-game); `gt-11-parallel-repetition.tex` (anchoring, lem:downsize_sampler, lem:downsize-cl-dist); `gt-12-compression.tex` lem:compress-independent-samplers.

Address EVERY objection (O1–O10, every MINOR, every NOTE) with a response-table row: id → FIXED / RETRACTED / DOWNGRADED / RESIDUE, exact DESIGN section, one-line what changed. Binding orchestrator directives:
- O1: add the def:sampler well-formedness replay (factor spaces direct-sum to V; marginals telescope — the critic's `enu:cl-space-sum` + `enu:cl-map-sum`) to §9.2's LawCert and to the output contract of EVERY transformation in §9.4–9.6; fix the padding rule for zero maps per rk:higher-level (stage 1 = whole space), stating exactly which transformations produce zero maps.
- O2: pin the non-Pauli answer encoding (Q-bit vs s(N)-bit) in ONE place in §11.4 and cite it elsewhere; keep the literal `≥ 3Q` predicate visible as the paper's reading, tagged SOURCE_REPAIR(intro-3Q-guard) with a definitions.md row, and make the repaired `> 3Q` (or whatever the recomputation from L588–L591 dictates) the operative guard; amend C14's wording so the honest Hide transcript is accepted by the OPERATIVE guard and the literal guard's rejection count is a PRINTED number.
- O3, O4: TB6b gets a second fixture with ℓ ≥ 2 and a genuinely multi-stage input sampler (so the adaptive prefix/dual/Gaussian schedule runs and M6-factor-prefix can be killed), and L^alice ≠ L^bob (so M6-game is red); give the instance parameters and a feasibility estimate (< 60 s warm) with numbers.
- O5: add the printed predicate `P_pcp_encodes_D1` to TB7. Prefer a content-faithful route: a tiny D^{(1)} whose actual arithmetization is the PCP instance fed to pcpverifier, even at absurd parameters — if you can specify one that runs < 60 s, do so with numbers; if not, the predicate reports FAIL visibly in the toy regime and §13.1's "16 honest accepts" claim is retracted/reworded accordingly. Never let a parameter-only substitution look like content fidelity.
- O6: add the twelfth CITED item (CL-function theory background: lem:cl-kth, lem:cl-concat, lem:cl-func-prod, lem:cl-dist-prod, lem:downsize_sampler, lem:downsize-cl-dist) and drop the word "exact" unless the list is then exhaustive — say how you checked exhaustiveness (grep every `lem:`/`thm:` label §9–13 cites).
- O7: restore the `max{…, (1−δ)2^{2^{λn}}}` floor in IntroGap; the gap AST is CHECKED only for the branch actually evaluated — say which.
- O8: replace "Cartesian" by the explicit E^ar tensor-product formula from L1951–L1955, and add the critic's distinguishing type pair as a red test.
- O9: add the negative-transcript block to §10.3 so M5-anchor-zero, M5-anchor-answer and M5-or are each killed by a named transcript; if a mutation cannot be made red, delete it and say so.
- O10: tag the fixed-width λ-byte specialization SOURCE_REPAIR(intro-decider-fixed-width) with a definitions.md row; regrade §13.2 item 10 to state that the dependency is CHECKED for the REPAIRED construction and CITED for the paper's.
- Adopt the critic's two/three offered mutations (M-factor-partition, M-detype-view-orientation, …) into the TB6/TB7 mutation lists with their killers.
- MERGE PROPOSALS: apply the critic's ACCEPT items; for REJECT items either comply or argue in the response table. Rewrite C12–C15 per the per-claim recommendation (C12, C13 as CONJECTURE with the named addenda; C14, C15 reworded with the missing steps named). Put the final proposed rows under "MERGE PROPOSALS (for orchestrator paste)" in §13.3 — do NOT edit claims/CLAIMS.md.
- Every recomputation the critic confirmed (R(a)–R(d)) stays as is; cite the verdict where you rely on it.

Report: `briefs/31-design-v2-repair.last.md` (write it yourself; ≤ 15 lines): disposition counts (FIXED/RETRACTED/DOWNGRADED/RESIDUE), each RESIDUE with reason, the O2 operative guard you derived, the O5 route taken, TB6b second-fixture parameters.
