# Brief 60 — CRITIC verdict r1 on rung TB3 (the quoted front end: program IR, fuel-bounded evaluator, bounded trace → succinct 3SAT → decoupled 5SAT → TB0 PCP)

You are the adversarial critic (Opus). ATTACK; do not summarize. Autonomous; no questions. Lane: write `verdicts/tb3-r1.md` ONLY; Julia/files only under scratch; never edit repo files; no state-changing git. Evaluate the ARCHIVED tree at commit `<SHA>` (`git archive <SHA> | tar -x -C <scratch>/tree`; instantiate there; cold precompile ~190 s). Never read src/test from the live tree (a worker edits it); `claims/CLAIMS.md` may be read live.

Scratch: `/tmp/claude-1000/-home-tobias-Projects-discussions/548f633b-c857-457a-bb33-0aa1a7879403/scratchpad/critic-tb3-r1/`

## Read order
1. `~/.claude/skills/rk-light/SKILL.md`; `CLAUDE.md`; `handoff.md` §"Representation requirements" (closure vs quoted syntax vs compiled circuit vs universal evaluator vs specialization).
2. `docs/DESIGN.md` §1.1 (program sort, Quoted, canonical serialization, description_size, Closure vs Quoted, DD-1/DD-2), §1.2 (BoundedTrace, Succinct3SAT, SuccinctDecoupled5SAT), §2 (quote_program, bounded_trace, cook_levin, decouple5 signatures), §3 (certificate grammar), §5.5 (the TB3 instance); `docs/definitions.md` §F; `claims/CLAIMS.md` (C10 is the proposed row; C16, C19 SKETCH — TB3 is their first machine evidence; C1, C9 TESTED are what the generated path feeds).
3. `briefs/23-tb3.md` (+ both addenda) and `briefs/23-tb3.last.md` (RED/GREEN, sizes, the (g) equality-decider snapshot with `ExpansionRefused`, the trace printout, the five mutations, API REQUESTS, DEVIATIONS incl. bytes-vs-bits and T = body transitions, MERGE PROPOSALS, the three "unresolved" items — the proposer's own doubts: Cook–Levin alphabets by exhaustive answer enumeration + CITED/ASSUMED leaves; the precompile workload trade; the certificate graft under `:Tseitin`).
4. Ground truth (recompute, never from memory): `ground-truth/gt-10-answer-reduction.tex` def:succinct-formulas, prop:standard-succinct-sat, sec:succinct-deciders (the decoupled 5SAT form and the two "undesirable properties"), def:tseitin, fig:pcpverifier; `gt-05-games-normalform.tex` def:decider (the input tuple (n,x,y,a,b)); `gt-03-prelim.tex` Turing-machine conventions; `docs/analytic/parts/part2a.tex` §8 and `part2b.tex` §11 (C16/C19's statements — check the code's charge table and the trace-row convention against them).

## Obligations
- **Run** the suite (summary + TB0 wall; quiet re-run per the caveat) and the runner (`MUTATION REGISTRY` line; all KILLED; baselines ok). Report cold precompile.
- **Independent recomputation on a COPY:** (1) the trivial decider: |D| = 33 bytes and its fnv1a64 from your own reserialization of the canonical term; `h(d,u)=63` from C16's formula; (2) the T=1 trace rows and the one-clause 3SAT / one-clause 5SAT with widths (0,0,1,1,1) — enumerate the clause relation yourself and check satisfiable iff accept; (3) the padded circuit m=1, s=6 (one live AND + 5 NOT padding), m'=16, 256/1024 present tuples, 512/1024 φ_C witnesses; feed it to `build_pcp` and TB2's decider yourself; (4) the equality decider: T=3 minimal accepting fuel under the charge table (is the proposer's charge table C16's? if C16 says lookups are free and the code charges 1, that is a lockstep defect — decide which is right against docs/analytic §8), 15 3SAT clauses, 47 5SAT clauses (15+4+28), `ExpansionRefused(279,936 > 160,000)`; φ5 satisfiable ⟺ a==b on all four pairs by your own solver; (5) `description_size` = exact canonical byte length and the deserializer round trip; (6) the Quote of an open term refused, Closure refused by `bounded_trace`.
- **Fidelity audit**: is `decouple5`'s five-block form the paper's (literals in `w_1,w_2,w_3` per gt-10 L1105–L1107 and eq:5sat; the equality gadgets; the two undesirable properties addressed)? Is the Cook–Levin locality argument the paper's or a fixture-only surrogate — the proposer says field alphabets come from exhaustive answer enumeration and the enc_Γ format is not enforced: grade honestly (is the CITED/ASSUMED labelling correct and visible in the trace, or does any CHECKED node overclaim?).
- **Certificate honesty**: the front-end certificate grafted under `:Tseitin` with replays that fall back to captured terms on a foreign root — can a borrowed front-end certificate pass? (The proposer says `:certificate_binding` refuses it — probe it.) Every CHECKED node red-capable; every CITED label exists in gt.
- **Precompile honesty** (as in tb0-r3/r4): does any TB3 test outcome come from the image rather than the run? Is the 188 s cold build acceptable — objection or NOTE, your call.
- **Two NEW semantic mutations** (e.g. a trace row transition permuted; a 5SAT equality gadget dropped; the fuel charge table off by one); survivors are MAJOR with the red test as FIX DEMAND.
- **Lockstep**: DESIGN §1.1/§1.2/§5.5 ↔ code ↔ the proposed C10 row ↔ C16/C19 statements (bytes vs bits; T = body transitions; `SortError`; `CompilationRefused`) — the proposer's DESIGN wording proposals: adjudicate each.
- **Elegance**: three places the code is more complicated than the mathematics.
- **Forward look (≤ 8 lines)**: TB4 (brief 24) needs `Hole`/`Specialize`/`YCode`/`Fix` and the four contracts on this IR — name gaps.

## Output: `verdicts/tb3-r1.md`
Numbered objections with severity/location/computation/FIX DEMAND/SURVIVING WEAKER STATEMENT; runs + walls; your mutations; per-claim: C10 (PROMOTE to TESTED with authorized verbatim row, or HOLD with the missing step), C16/C19 (RE-AFFIRM SKETCH, or PROMOTE-to-TESTED-on-the-fixture only if the evidence genuinely covers the statement — say exactly what it covers); DESIGN wording adjudication; final line exactly `VERDICT: PASS` or `VERDICT: FAIL(<ids>)`.
