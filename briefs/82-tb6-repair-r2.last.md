**Brief 82 report — TB6 repair r2.** Proposer: the session-5 orchestrator (2026-09-22, cloud container, 4 cores, no governor; walls advisory). NO-GIT inside the round; committed by the orchestrator. Starting state: `main` at 2a1510c (verdicts/tb6-r2.md pasted). Lane as the brief.
**N1 (MAJOR).** RED (the critic's CRIT-6 demo): `_in_V ≡ true` accepts an out-of-V `y⊥` on `(Introspect_bob, Read_bob)`; the suite stayed 12474/12474. GREEN: TB6b (j) adds V1–V4 — `y⊥` on Read (flip 19, `hiding_intro`), `z` on Sample (flip 7, `sampling_intro`), `x` on Hide (flip 31, `hiding_same`), `z` via 2(a) (flip 7, `sampling_pauli`) — each `parse_intro_answer === nothing`, rejected with its fired test, trace `[:Dimension]` only (the honest Sample/Hide leaves make further calls); `MUTATION_EXPECTED_RULE tb6b_out_of_V rejected=4/4`. `IntroDecider` replay gains `bit5`: `(Introspect_alice, Sample_alice)` with `z = e_{s+1}` rejects with no call past Dimension (VACUOUS when `Q = s(N)` or without the embedding). Mutant `M6-in-V` registered.
**N2 (MAJOR).** TB6b (k) pins `(22631, [22618, 13])` at budget 22,632 and `(22622, [22618, 4])` at 22,622; mutant `M6-nested-timeout-uncharged` (CRIT-5, the sampler-query site) registered.
**N3 (MAJOR).** Code fix DEFERRED to brief 44 (the verdict's owner). DESIGN §11.4 "Nested typed deciders" restated: input decoding + child calls (returned OR timed out) metered; own predicate work and nested `:Pauli`/`:AnswerReduce` bodies `UNCHARGED(owner=tb7-nested-own-steps)`. (k) pins `by_depth == [22618, 15]` and `by_depth[2] == Σ flat child-call steps == 15 (5 + 10)` as the documented gap.
**N4.** D1(d) DOWNGRADED: the 13 witnesses are corrupted honest leaves of the TB6b-M fixture; the replay is fixture-generic, so the conjuncts' red capability stays at test level (diagonal kill matrix, verdicts/tb6-r2.md §1); N1's generic case is in the replay.
**N5.** TB6a (3) pins 206 `(155_631, 212)`, 824 `(4_488_710, 830)`, 1648 `(17_899_098, 1_654)` (the last two under TB6A_HEAVY), and `child_calls == dim + 6` at every size; §12.1 says PROXY for 824/1648 and gives the wall as advisory (0.0349 s here, 0.0677 s critic).
**N6.** Corrected: §10.3 and §13.1 TB5 walls → calibrated gates K 4/4/6; §11.6 E/M targets → `tb6b_E`/`tb6b_M`/`tb6b_combined` gates; §11.4 NOT_EVALUABLE sentence → measured/pinned slots with the production fit graded; §11.6 "C14 remains a proposal" → TESTED (scoped); "pinned per oriented pair" → histogram + SET of per-pair counts; the "≥ 12×" ceiling claim (DESIGN §13.1, `test/calibration.jl`) → 12× only at the reference kernel, ~7.4–8× here.
**N7.** DESIGN §11.4's prose list replaced by the exhaustive charge-site table (15 rows) plus worked derivations; TB6b (i) re-derives `Dimension 5, Marginal(3) 53, Factor(2,e1) 39, Linear(2,e1,e4) 40` from the table with its own GF(2) elimination cost count (`elim([1|1]) = 7`) and asserts equality with the pins.
**N8.** `test/mutations/run.jl` `disposition`: `killed` now requires an assertion failure; a crash after the marker is `KILLED-BY-CRASH (not credited)` and fails the registry. Evidence strings: `M6b-gate-body-inflated` → `tb6b_walls E ratio<33 => true M ratio<21 => false`; `M5-gate-body-inflated` → `tb5_walls transcripts ratio<4 => false`.
**Runs.** Rung testsets: TB6b (i) 74/74, (j) 84/84, (k) 16/16, TB6a (3) 25/25. Suite: `MIPStarLambda | 12500 12500 4m28.8s`, exit 0 (uptime 20:19 load 1.53 → 20:24 load 1.02). Registry: REGISTRY_LINE
**C14 scope proposals (for TB6 critic r3, proposal only).** Strike the N1 sentence ("The source's rejection of answer vectors not presented in `V` … has no negative witness …") and the N2 clause ("the steps a timed-out child executed can be dropped …"); KEEP the N3 clause (own predicate work and nested `:Pauli`/`:AnswerReduce` bodies uncharged) until brief 44 lands the fix.
**FIXED 7 / DOWNGRADED 1 / RESIDUE 1** — DOWNGRADED: N4 (D1(d)). RESIDUE: N3 code fix (brief 44, owner tb7-nested-own-steps).
