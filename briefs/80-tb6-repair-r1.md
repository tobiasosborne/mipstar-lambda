# Brief 80 — TB6 repair r1 (work order = `verdicts/tb6-r1.md`: O1, O2 MAJOR; O3–O6 MINOR; O7–O10 NOTE; §7's four TB7 API blockers) — the round that unblocks TB7 (brief 44)

Proposer (Fable). Autonomous; no questions; **no git** (the orchestrator commits). Report: `briefs/80-tb6-repair-r1.last.md` (≤ 20 lines).

**STARTING STATE (orchestrator, 2026-09-08): the uncommitted partial TB7 tree.** Brief 78's hunks are committed first (split by the triage of session 4: `brief78.patch` verified green in isolation, 12338/12338, 188 mutants KILLED, the `:suite` baseline BROKEN only by O2's tb6a wall gate); the TB7 partial (src/compress/, src/policy/, test/tb7_compress.jl and the TB7 hunks in src/**) stays in the working tree as the base, because it already carries the four API blockers' implementations (`:Program` children under one fuel currency, the nested `TypedDecider` branch, `PadMachine`, `_repeat_index`/`_normal_form_status`) that D11–D14 verify, correct and record. The proposer is the orchestrator itself (user directive 2026-09-08: work on the hardest open issue yourself); the one-symbol exception of D9 is NOT granted (machines.jl is TB7's).

CAVEAT the orchestrator resolves in that line: the main tree currently carries, UNCOMMITTED, a partial TB7 tree (`src/compress/`, `src/policy/`, `src/precompile_compress.jl`, `test/tb7_compress.jl`) plus TB7/TB4-repair hunks in `src/{MIPStarLambda,compress,ir/programs}.jl`, `src/descriptions/**`, `src/introspect/{intro_decider,introspect,meter,pauli_decider,pauli_sampler}.jl`, `src/repeat/repeat.jl`, `src/frontend/**`, `src/verifiers/answer_reduce.jl`, `test/tb4_compress_ir.jl`, `test/mutations/{run,tb4_compress}.jl`, `docs/DESIGN.md` §5.6 and `docs/definitions.md` §F. The STARTING STATE line says whether you begin from HEAD (`7578c0d`) or from that partial state. Do not revert, rebase, stash or "clean up" anything you did not write: if a file in your lane already differs from HEAD, edit it additively and list the collision in CROSS-LANE EDITS.

Lane (nothing else, and never `claims/CLAIMS.md`):
`src/introspect/**`; `src/compress.jl` ONLY for D14's two named items; `test/tb6a_audit.jl`, `test/tb6b_introspect.jl`; `test/tb5_repeat.jl` ONLY for D2's wall calibration; `test/mutations/tb6_introspect.jl` and `test/mutations/run.jl` additively; a NEW `test/calibration.jl` plus a one-line include in `test/runtests.jl`, ONLY for D2; `docs/DESIGN.md` §§11.4, 11.6, 12.1, 12.4, 13.1 ONLY; `docs/definitions.md` §H ONLY (D6d). Mutants may PATCH any `src/**` file (the runner applies patches in an isolated sandbox copy — `M-intro-fuel` already patches `src/descriptions/machines.jl`); that is not a lane edit.

Scratch: `<orchestrator scratchpad>/tb6-r1/` — any writable directory OUTSIDE the repo. Never write scratch into the repo.

## Read order
1. `verdicts/tb6-r1.md` IN FULL — §1 (the critic's independent recomputations, which are now oracles), §2 (O1–O10), §3 (CRIT-1..CRIT-4), §4 (DESIGN adjudication), §5 (C14), §6 (TB5 r2), §7 (TB7 readiness). Every directive below carries its verdict id; where the verdict and this brief differ, the verdict wins and you say so in the report.
2. `CLAUDE.md`; rk-light laws 1, 4, 5. `briefs/43-tb6-introspect.md` (+ ADDENDUM) and `briefs/43-tb6-introspect.last.md` (what TB6 delivered, its four API REQUESTS, its deviations).
3. `briefs/44-tb7-compress.md` (+ ADDENDUM) — the *only* reason D11–D14 exist. Resolve exactly what brief 44 is blocked on; do not start TB7.
4. `docs/DESIGN.md` §11 IN FULL, §12.1/§12.4/§12.5, §13.1/§13.2; `docs/definitions.md` §H. These are single-source: code cites, never redefines.
5. Ground truth for anything you re-derive: `gt-08-introspection.tex` L401–L498 (`fig:intro-decider`), L417–L419 (the per-call `R` gate), L451–L455 + L462–L463 (`a_w = a_wbar`), L464–L473 + L641–L657 (`enu:hiding-same` and its footnote), L550–L579, L641–L684 (the query schedule), L669–L678 (the canonical-complement step), L923–L953 (`lem:commute`), L1136–L1163; `gt-03-prelim.tex` L307–L318, L333–L340 (`def:canonical-complement` + its Remark), L386–L392 (`def:Lperp`); `gt-05-games-normalform.tex` L624–L634 (`def:normal-ver`); `gt-11-parallel-repetition.tex` L216–L220; `gt-12-compression.tex` L448–L449 (`fig:halt_f` step 5); `gt-10-answer-reduction.tex` L200–L205 (Bounded Halting).

## Timing rule (binding for every measurement in this round)
Run Julia only when the box is quiet: `pgrep -fa 'runtests|mutations/run|codex exec'` must show only your own process. Print `uptime` immediately before and after every suite run and every registry run, and `powerprofilesctl get` once — it must read `performance`; if it does not, say so in the report and treat every wall number as advisory. Never run a timing check while another agent runs. Calibrated gates are set from the IN-SUITE baseline (the value `suite_calibration_kernel()` takes inside `test/runtests.jl`), never from a standalone run (D2).

## Directives

**D1 (O1, MAJOR; CRIT-1, CRIT-4; §7.6). Negative witnesses for every conjunct of the introspection predicate.**
`_intro_ordered` (`src/introspect/intro_decider.jl` L299–L400) is an AND whose non-Pauli conjuncts have no negative transcript: the critic deleted `aw_p.a == av_p.a` from `enu:sampling-intro`+`enu:hiding-intro` (CRIT-1) and `aw_p.y_perp == av_p.y_perp` from `enu:hiding-read` (CRIT-4) and the whole suite stayed green (12105/12105, exit 0).
(a) Enumerate, as a table in `test/tb6b_introspect.jl` and in the report, EVERY comparison of items 2(a), 2(b), 3(a), 3(b), 3(c), 3(d) — the count is 13 by my reading (2(a): `_V(aw,s)==_V(ans.y,s)`; 2(b): marginal image, `a_w==a_wbar`; 3(a): `y`, `a`; 3(b): `below_w==below_v`, `y_perp`; 3(c): `below`, dual prefix projection, `x_{>k+1}`, the stage-`k+1` dual image; 3(d): the stage-1 dual image, the untouched suffix). Re-derive the list from the code; if it differs from 13 say so.
(b) For each, a NAMED negative transcript built by taking an honest leaf of the corresponding oriented pair and corrupting exactly the field that conjunct compares (the verdict names the minimum set: `a_wbar`, `y_wbar`, `y^perp_wbar`, `x_wbar`, `y^perp_{wbar,k+1}`, `a_hat_w^{V_{>1}}`), asserting rejection AND the fired-test name. On TB6b-M the child answer is the single bit `false`, so each witness is one line.
(c) One registered conjunct-drop mutant per comparison (`M6-answer-consistency`, `M6-read-perp`, …), each shown KILLED. CRIT-1 and CRIT-4 must be among them, spelled exactly as the verdict spells them.
(d) Extend the `IntroDecider` certificate `replay` closure (L467–L478) so its negative cases include the new witnesses: the replay is what KILLED CRIT-3, so it is the right place for the predicate's red capability to live, not only the test file.

**D2 (O2, MAJOR; §7.6; `verdicts/tb5-r1.md` §4 condition (iv)). Calibrated wall gates; a registry that completes.**
The registry does not finish: `test/tb6a_audit.jl:215`'s `@test tb6a_audit_elapsed < 5` measured 5.429 s under the runner's own 4-way load (kernel 1.2769 s vs quiet 0.4798 s), the `runtests.jl TB0_TARGET=all` baseline went BROKEN, no `MUTATION REGISTRY` line printed, exit 1, and `TB0 M-gate-body-inflated` — brief 77's approved-with-conditions red witness — was reported UNATTRIBUTABLE.
(a) Factor `suite_calibration_kernel()` into a new `test/calibration.jl`, included idempotently (`isdefined` guard) from `test/runtests.jl` (one line, the only edit allowed there) and from every rung file that gates on it, so the kernel exists when the runner includes a rung file standalone with `TB6A_TARGET` / `TB6B_TARGET` / `TB5_TARGET`. Keep the kernel deterministic, in-process, and EXCLUDED from every timed body.
(b) Replace `@test tb6a_audit_elapsed < 5` by `elapsed / kernel < K_tb6a`. RECOMMENDED rule for every gate you convert: `K = max(4, ceil(3 * quiet_ratio))`, where `quiet_ratio` is your own quiet in-suite measurement — for TB6a the critic's quiet numbers give 2.765/0.4798 = 5.76, hence `K_tb6a = 18` (≈3.1× headroom; the runner's own loaded ratio was 4.25, so the gate holds under load). Reason: the ratio is load-invariant where the absolute wall is not — that is exactly what the TB0 measurement shows — and 3× headroom is the TB0/TB4 discipline (TB0: quiet 32.3, gate 50). Keep an absolute ceiling as a second hard `@test` at a value the runner's load cannot reach (`4 * K * 0.4798 s`, i.e. ≥ 12× the quiet measurement). Print, TB4-style, both the ratio and `K * kernel` — the budget enforced at this run's own rate.
(c) Same discipline, same rule, for the TB5 walls (`construction < 2`, `transcripts < 5` in `test/tb5_repeat.jl`) and the TB6b walls (`E<3`, `E<15`, `M<25`, combined `<43` in `test/tb6b_introspect.jl`): they are absolute and are exercised inside the same `:suite` baseline. Keep `total < 7` / peak-RSS assertions as they are (they are not clock-bound).
(d) Register a red witness per NEW calibrated gate family — at least `M6a-gate-body-inflated` and `M6b-gate-body-inflated`, built on the `test/mutations/tb5_gate.jl` pattern (inflate the body, not the kernel) — and show them KILLED.
(e) Re-run `MUTATION_JOBS=4 julia --project=. test/mutations/run.jl` to a clean `MUTATION REGISTRY: killed=<N>/<N> baselines ok=<b>/<b> wall=<s> s`, **exit 0**, and quote that line verbatim. The run must attribute `TB0 M-gate-body-inflated => KILLED` (`verdicts/tb6-r1.md` §6, "gate red test … Re-verify after O2") — quote that line too. Report `uptime` at both ends.

**D3 (O3, MINOR). "Total mass one" is a structural identity — strike it and replace it with something that can fail.**
`enumerate_branches` assigns `2^-depth` to every leaf of a complete binary tape tree, so the sum is 1 for ANY deterministic `f`; no mutation of `measure!`, `_rowsum!`, `coarse_measure!` or `_solve_gf2` can change it.
(a) Delete the mass assertion from DESIGN §11.6's "require total mass one" and from the §13.1 TB6b required-output cell ("exact support/mass one over 116 pairs"); keep "every leaf carries an exact dyadic probability" (that is true and structural — say so).
(b) RECOMMENDED replacement (do both): pin, per oriented pair, the exact leaf COUNT and the exact multiset of leaf probabilities; and add a TEST-ONLY dense 64-amplitude reference simulator for TB6b-E (`2(Q+1) = 6` qubits) and assert the tableau's per-pair outcome distribution equals it on all 116 pairs. Reason: the leaf-count/multiset pin is cheap and catches register-rule regressions; the reference state vector is the only check that is independent of the tableau code, and 64 amplitudes is free. DESIGN §11.6 forbids a dense state vector — that prohibition binds the PRODUCTION simulator only; record the test-only exemption in §11.6 in one sentence, and keep the existing "no dense state vector" allocation assertion on the production path.
(c) Register a mutant the mass check provably could not kill and the new check does: e.g. `M6-forced-outcome-free` (drop the forced-outcome detection in `_solve_gf2` so a deterministic outcome is sampled freely — mass stays 1, the distribution changes). KILLED.

**D4 (O4, MINOR). Pin what C14 cites.**
Exactly, as `@test`s, not `println`s: the ten cost slots (E `5/13/10/13/10`, M `5/53/60/50/22`) — today only `haskey` and `<= F_child`; the charge table values Dimension 5, Marginal(3) 53, Factor(2,e1) 39, Linear(2,e1,e4) 40, Factor(stage 3) 60, Linear(stage 3) 50; the two literal counts `literal_suffix_rejections == 30` and `literal_perp_rejections == 48` of 64 (today only `> 0`); `_require_image` at 142 → 74,671 steps / 148 child calls and at 179 → 113,946 / 185. Register one mutant per group, shown KILLED — the verdict names two: drop the `_charge!(ctx, 1)` branch charge in `_metered_stage`, and make `hide_suffix_literal` use `y_wbar` for both registers.

**D5 (O5, MINOR). Hide-incident oriented count 10/22/58, not 10/22/46.**
`2(4 + 2(ell-1)) + 2ell = 6ell + 4` = 10, 22, **58** at ell = 1, 3, 9 — the formula `test/tb6a_audit.jl:81` already asserts. No code change. The wrong figure sits in `briefs/43-tb6-introspect.last.md` (and is quoted in `briefs/79-tb6-critic-r1.md`), both historical files OUTSIDE your lane: **DEFERRED to the orchestrator** (one-token correction in a report). Your obligation: assert 58 at ell=9 explicitly and PRINT `Hide-incident oriented 10/22/58` in your report so the correction is on the record.

**D6 (O6, MINOR, lockstep; `verdicts/tb6-r1.md` §4). Four documentation obligations.**
Land, in your lane, and quote each new or changed line VERBATIM in the report so the orchestrator can verify byte equality:
(a) DESIGN §11.6's ten-slot cost table: replace every `NE` cell with the measured exact per-call count and the finite maximum (§11.6 itself orders this: "At implementation, replace each cost slot with exact per-call counts"), keeping the `TIME_child(N)<=R` / `fits F_child` grades as measured (E: FAIL/PASS except Dimension; M: Dimension PASS/PASS, the rest FAIL/PASS).
(b) DESIGN §11.6 and §13.1: the TB6a target `<1 s` is false — replace it with the calibrated gate D2(b) sets, with the quiet measurement (2.765 s in-suite) and the reason (first-use compilation) named.
(c) DESIGN §13.1: land brief 77's calibration-kernel sentence — `verdicts/tb5-r1.md` §4 condition (ii): name the kernel, state that it is deterministic, in-process and excluded from the timed body, and give its measured quiet value.
(d) `docs/definitions.md` §H: two new rows, `SOURCE_REPAIR(intro-perp-orthogonal)` and `SOURCE_REPAIR(intro-hide-suffix-register)`, matching the existing `intro-3Q-guard` row's shape, carrying the anchors the verdict requires — `gt-08:L669-L678` + `gt-03:L307-L318`, `L333-L340`, `L386-L392` for the first; `gt-08:L464-L473` + `L641-L657` for the second — plus the `k_rep(n)` row's missing integrality clause (`verdicts/tb5-r1.md` §4: the exponent-vs-value distinction, `k(9) = 27` at `c' = 1/2` admitted). The verdict says the ORCHESTRATOR merges (a)–(d); since it supplies anchors, not text, you draft them and the orchestrator verifies at commit.
**Split you must respect:** DESIGN §9.5 (the oriented-edge sentence, APPROVED verbatim) and §9.6 (the `TypedDecider` clause, APPROVED with the merge note "land only the new clause; do not duplicate the tag list") are OUTSIDE your lane — the orchestrator merges them. Do not touch §9. Instead, quote in your report the exact §9.5/§9.6 text you have verified the built code satisfies. The §11.4 fuel-unit sentence IS yours (D8).

**D7 (O7, NOTE). `_require_image` at TB7 scale.**
The measurement is 74,671 → 113,946 steps at dimension 142 → 179 (≈ dim^1.82), but "binding at TB7's 1696" is asserted, not shown: 2e-4 s at 179, cubically extrapolated to 1696, is ≈ 0.17 s. RECOMMENDED: take the measurement rather than downgrade the wording — it is a pure sampler-side cost, needs no fixture, and D13 needs the number anyway. In TB6a, measure and PIN steps and wall at dimensions 206, 848 and 1696 (TB7's own chain), print the fitted exponent, and gate the measurement under the D2 calibrated budget. If the 1696 measurement does not fit that budget, that fact IS the evidence for D13 — report it and put the measurement behind a `TB6A_HEAVY=1` env guard rather than weakening the gate. If you cannot measure at 1696 at all, downgrade the DESIGN/report wording to "to be measured at TB7" and say why.

**D8 (O8, NOTE; §4 row 1). The §11.4 fuel-unit sentence, with the critic's correction.**
Land the brief-43 sentence in DESIGN §11.4 with the charge list CORRECTED to what the code actually charges: `1 + ndigits(n; base=2)` for the decoded index (4 at N=4 — **not** "1 + 2 index bits"), a fixed **2** for the (player, stage) fields, **s** for a Marginal/Factor input vector and **2s** for Linear's two vectors, `k + k^2 + k + 1` per evaluated stage, one per canonical-elimination row operation, one per serialized output element. Include the three clauses the verdict approves: the budget refuses the (budget+1)-th step BEFORE it executes (a return at step budget is permitted); the sizing `Dimension` probe runs on the same meter; `by_depth` attributes steps per machine depth. Add one sentence resolving O8: "It first asks only `Dimension(N)`" describes the FIRST call, not a bound on the schedule — the recorded log carries one sizing `Dimension` probe per vector query (7 records for 6 vector calls on the Hide_1/Hide_2 leaf), which is the same meter and not an extra child query. Assert the 7-for-6 relation in a test.

**D9 (O9, NOTE). `FuelExhausted`'s number is an attempted count, not a step index.**
`_charge!` throws `FuelExhausted(ctx.steps + k, budget)` for a whole block, so the printed number can be `budget + k`, `k > 1`; `ctx.steps` itself stays correct (the timeout records read exactly 16). The rename lives in `src/descriptions/machines.jl` L39–L53 — OUTSIDE your lane. Land the DESIGN §11.4 sentence naming the semantics regardless ("the exception reports the attempted step count, not a step index; `Meter.steps` is unchanged by a refused block") and assert it in a test. Rename the field to `attempted` (with `showerror` and both `throw` sites) ONLY if the STARTING STATE line grants that one-symbol exception; otherwise **DEFERRED to TB7** (brief 44 already owns `machines.jl` for D11/D13) — say which happened.

**D10 (O10, NOTE). E is a degenerate hiding fixture, by construction.**
No code action: at ell=1, `enu:hiding-same` is VACUOUS and `s(N)=1`, so all live adaptive hiding evidence rests on the single TB6b-M child sampler, and both new SOURCE_REPAIRs are exhibited on it. The critic's authorized C14 row already says so. Add one sentence to DESIGN §11.6 recording it, so the fixture inventory and the claim agree.

**D11 (§7.2, TB7 API blocker 1). The fuel currency — decide and record.**
DESIGN §11.4's step unit is not lowered into §1.1 `Eval` `FuelBound`, so TB7's `D_{M,lambda}` budget and the child budget are two currencies. RECOMMENDED DECISION: **one currency** — `FuelBound`'s unit IS `Meter.steps`, i.e. the §11.4 charge table is the definition of an `Eval` unit; reason: brief 44's addendum already requires "the step counts of TB6's ten cost slots are reproduced under `Eval`", `definitions.md` §F's `SOURCE_REPAIR(HaltDeciderFuelBound)` already spends `n^lambda` "units" with no other definition, and two currencies make `lambda_bounded_time` unauditable. Record the decision as a named paragraph in DESIGN §11.4 ("Fuel currency") and in §12.1, citing `gt-08:L417-L419` (the per-call `R` gate), `gt-12:L448-L449` (`fig:halt_f` step 5) and `gt-10:L200-L205` (Bounded Halting). In-lane evidence: run one child sampler query and one child decider call through the `Eval` path and assert the step count equals the corresponding TB6 slot. If reaching that path requires editing `src/ir/programs.jl` or `src/descriptions/machines.jl` (outside your lane), do NOT edit them: print the blocker as `NOT_EVALUABLE(owner=tb7-fuel-lowering)`, record instead an explicit TWO-METER CONTRACT in §11.4 (both names, both units, conversion status, and the rule that no `Eval` budget may be inferred from a `Meter` count or the reverse), and hand brief 44 the exact call sites.

**D12 (§7.3, TB7 API blocker 2). Nested typed deciders as metered children.**
`_metered_decide_typed` (`src/introspect/intro_decider.jl` L149) refuses a `TypedDecider` child, so `Compress = Repeat ∘ AnswerReduce ∘ Introspect` cannot meter a composed introspection decider. This file IS in your lane: make the dispatch total — a `TypedDecider` child is decoded from its own bytes (`_decider_typing` already reads labels from the bytes) and metered on the same `Meter` at depth+1, with `by_depth` attributing its steps separately. Test: a two-level nest (an introspection decider whose child is itself a `TypedDecider`) returns the same verdict as the flat call and the depth-attributed step counts sum to the total. Mutant: charge the nested call at the parent's depth (`M6-nested-depth`) — KILLED. Record the grammar/dispatch rule in DESIGN §11.4.

**D13 (§7.4, TB7 API blocker 3). The `(:Pad, extra, S)` term node — decision procedure, not a decision here.**
The node changes the description GRAMMAR, hence byte layouts, `description_size`, and every `DL9-*` law — that is TB7-scale and cannot land additively in a TB6 repair whose purpose is to unblock. DECISION PROCEDURE, to be executed and recorded in DESIGN §12.1: take D7's measurements at 206/848/1696; if the projected `_require_image` cost per construction exceeds 1 s (or, more sharply, exceeds 1/10 of TB7's 60 s warm target divided by the number of constructions on the TB7 path), then the Pad node is a **REQUIRED TB7 obligation** and §12.1 records that with the measured numbers and the `verdicts/tb5-r1.md` O12 rationale; otherwise §12.1 records "not binding at TB7 scale; `_require_image` retained" with the same numbers. Either way brief 44 must not have to re-litigate it. **Implementation DEFERRED to TB7 (brief 44), lane reason above.**

**D14 (§7.5, TB7 API blocker 4; `verdicts/tb5-r1.md` O11). `REPEAT_CONTRACT` and `_normal_form_status` in `src/compress.jl`.**
Two named edits, nothing else in that file: (i) `REPEAT_CONTRACT`'s `completeness_decider_time` hypothesis evaluates `big(2*p.lambda)^p.tau` — index n = 2 — while the fixture's construction index is 9 and `B(9) = 9`; bind it to the construction index carried by the stage's `params`, so the printed predicate is the one C13 names (the grade stays `NOT_EVALUABLE`; the bound is `Opaque`). (ii) `_normal_form_status(::VerifierDescription)` checks only `field == 2 && untyped` — correct per `def:normal-ver` (`gt-05:L624-L634`) — but its display asserts "decider a total five-input predicate"; weaken the display to exactly what is checked. Assert both, and register one mutant each (`M7-repeat-index`, `M7-normal-form-display`) shown KILLED.

## Rules that bind every directive
- **Every new assertion gets a registered mutant, shown KILLED** (rk-light law 4). Register in `test/mutations/tb6_introspect.jl`; extend `test/mutations/run.jl` additively only.
- **Baseline-first runner, exit 0.** No mutant may be credited while its unmutated target is BROKEN; the final registry line must read `killed=N/N`, `baselines ok=b/b`, exit 0, with no `UNATTRIBUTABLE`.
- **Red before green.** Show each new negative witness RED against the current code before you fix anything, and quote one RED excerpt per MAJOR in the report.
- **`claims/CLAIMS.md` is never edited by you.** The orchestrator pastes the verdict's authorized rows (C14 PROMOTE-scoped, and the C12/C13 strike-and-insert of §6). You may PROPOSE, in the report only, the replacement C14 scope sentences that D1/D3/D4 make stale — the r1 row's O4 sentence ("printed and independently reproduced … but are not pinned") is false once D4 lands, and its O1 sentence is false once D1 lands — as verbatim text for the NEXT critic round to adjudicate. Proposals only; the orchestrator still pastes the r1 row as written.
- **No git**, no `Manifest.toml` churn, no reverting other agents' uncommitted work, no new top-level files outside the lane.
- §7.7 ("ready as-is") and §7.8 (the known TB7 shape issues: fixed-width `sigma_1`, `P_pcp_encodes_D1 = FAIL`, the ToyPolicy predicate table) need NO action from you — they are brief 44's, and nothing you do may fold any of them to PASS.

## Report (`briefs/80-tb6-repair-r1.last.md`, ≤ 20 lines)
Header line (proposer, date, NO-GIT, starting state actually used, lane). Then: one RED excerpt per MAJOR; the conjunct inventory of D1 as one line (count, witnesses, mutants); the D2 gate table (gate, quiet ratio, K, absolute ceiling) and the verbatim `MUTATION REGISTRY` and `TB0 M-gate-body-inflated` lines with `uptime` at both ends; the suite line (`n/n`, exit, wall, TB0 body + ratio, TB6 body); the pinned values of D4 as printed; the verbatim DESIGN §§11.4/11.6/12.1/13.1 and definitions §H text you landed (D6, D8, D11, D13); `Hide-incident oriented 10/22/58` (D5); the D11/D13 decisions in one line each with their reason; mutation lines (label => KILLED); CROSS-LANE EDITS and collisions with the uncommitted tree; C14 scope-sentence proposals (proposal only); and a final three-number summary line **FIXED n / DOWNGRADED n / RESIDUE n**, where RESIDUE lists by id every verdict item you did not fully discharge and why.
