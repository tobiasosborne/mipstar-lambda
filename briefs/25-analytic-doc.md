# Brief 25 — The analytic underpinnings: a pdflatex document

Proposer (gpt-5.6-sol, xhigh). Autonomous; no questions; no git. Lane: `docs/analytic/**` ONLY (create it). Deliverable: `docs/analytic/analytic-underpinnings.tex` that compiles with `pdflatex` (run it twice; fix every error and every overfull box you can; produce `analytic-underpinnings.pdf`), plus `docs/analytic/README.md` (3 lines: how to build). Report: `briefs/25-analytic-doc.last.md` (≤ 15 lines: section list, page count, pdflatex warnings left).

## Purpose (from the user, verbatim in spirit)
"A pdflatex document that explains the analytic underpinnings, i.e., the Turing machine → lambda calculus argument, adapted to the typed stuff we are doing here. I want it clear that we are not just coding, we are literally doing the analytic transformations." The reader is a theoretical physicist who knows quantum information, PCPs and the broad structure of MIP*=RE; no introduction to MIP*=RE, no praise, no generic exposition. Every construction is cited into the ground truth by label (`\texttt{thm:pcp-decider}` style, with file names), and every correspondence to our code names the Julia type/function (from `docs/definitions.md`) and the certificate grade (CONSTRUCTED / CHECKED / CITED) so the reader sees exactly which analytic step is executed, which is certified, and which is only cited.

## Read first
`handoff.md`; `docs/DESIGN.md` (§1.1 program sort incl. YCode/Hole/Specialize and the displayed Ψ_{M,L}; §1.2; §1.3–1.4; §1.5 sampler sort; §1.6 contracts; §3 grades; §4 soundness split; §8); `docs/definitions.md`; `docs/findings.md` (F1 — present it as "a discrepancy as we read NW19; our reading may be wrong", never as an error in the paper); `claims/CLAIMS.md` (state each claim's CURRENT status honestly; never upgrade). Ground truth: `ground-truth/gt-03-prelim.tex` (Turing machines subsection; sec:ld-encoding; lem:schwartz-zippel), `gt-04-cl.tex` (def:cl-func, def:cl-dist, lem:cl-kth, lem:cl-concat, samplers), `gt-05-games-normalform.tex` (def:decider, def:sampler, normal form verifiers, def:lambda λ-bounded), `gt-06-types.tex` (typed samplers, detyping), `gt-07-ldt.tex` (sec:ld-game, fig:ld-decider, lem:ld-soundness), `gt-10-answer-reduction.tex` (def:tseitin, def:formula-arithmetization, prop:standard-succinct-sat, sec:succinct-deciders, prop:zero-basis, fig:pcpverifier, thm:pcp-decider, sec:ld-compiler, fig:decider-pcp, thm:ar), `gt-12-compression.tex` (fig:compress, thm:compression, and the section "An MIP* protocol for the Halting problem": the self-referential verifier F(F,M,L,…) and the remark that an earlier version used Kleene's recursion theorem), `ground-truth/nw19/nw19-tseitin-arith.tex`. Read the actual Julia in `src/` for TB0–TB2 so the correspondence table is true to the code (names, what is CONSTRUCTED vs CHECKED).

## Required structure (amsart or article, amsthm, hyperref, booktabs; tikz optional; ≈15–25 pages)
1. **Objects, not functions.** Deciders and samplers as Turing machines in the paper (def:decider, def:sampler, normal form, λ-bounded); why Compress consumes *descriptions* (|D|, TIME bounds) and never extensional values. The lambda-calculus reformulation: quoted terms, `Quote`/`Eval`/`Specialize`, description size as canonical byte length; the distinction closure / quoted syntax / circuit / universal evaluator / specialization (handoff). Church–Turing is not the point; intensionality is. State precisely what "Turing machine → lambda calculus" means here: a change of *description language* for the same computable functions, with the same resource accounting made explicit (fuel = time bound T; size = |D|), and prove the small lemma that fuel-bounded evaluation of a quoted term is a Turing machine of the required time/description bounds (sketch with the constants the paper needs: poly overheads only).
2. **The fixed point.** Kleene's second recursion theorem vs the Y combinator: the paper's F(F,M,L,…) self-application, the earlier version's Kleene argument, and our D_{M,L} = Y Ψ_{M,L} as a quoted term (display the term from DESIGN §1.1, with Hole/Specialize). Prove the equivalence of the three formulations as *descriptions* (not just as functions), and say what Compress must do with the quoted body. Note honestly this is the easy part (handoff).
3. **Cook–Levin as a transformation on descriptions.** Bounded trace → succinct 3SAT (prop:standard-succinct-sat) → decoupled 5SAT (sec:succinct-deciders: the two "undesirable properties" and the five separate assignments) → Tseitin (def:tseitin, NW19) → arithmetization (def:formula-arithmetization, prop:tseitin-arith-degree). For each: the analytic statement, then the exact IR transformation and its certificate. Present F1 here as the degree bookkeeping *we* compute (occurrence vector), with the softened framing.
4. **The low-degree PCP as algebra.** Multilinear extension as the interpolation operator `a ↦ g_a` (sec:ld-encoding); c₀ = F_arith·∏(gᵢ − oᵢ); prop:zero-basis as polynomial division by zᵢ(1−zᵢ) — give the rewrite rule and prove it yields the paper's cᵢ with the stated degree bookkeeping; fig:pcpverifier; thm:pcp-decider's soundness split into the four layers of DESIGN §4 with Schwartz–Zippel written out (lem:schwartz-zippel), the two degree bounds, and where q enters. Make explicit that the *only* thing the computer adds is: the construction is executed on an instance and the identities are checked coefficientwise; the theorem is not "tested".
5. **The typed layer: conditionally linear functions.** def:cl-func as an inductive type; lem:cl-kth as the marginal decomposition; the level laws for concatenation, direct sum, product (lem:cl-concat); why the datatype makes "ℓ-level CL" a CONSTRUCTED property; L_Point/L_ALine/L_DLine with the canonical representative (def:cl-canonical) and χ; lem:alnf/lem:dlnf as the histogram statements we check exactly; typed samplers and detyping (gt-06) with its level/soundness cost as a CITED transformation. The six-copy PCP sampler and the product with the oracularized sampler (sec:ld-compiler); fig:decider-pcp; thm:ar's ASSUME/PROVE contract. Then the structural hypothesis (handoff §Broader structural question; DESIGN §8): what closure laws the paper uses, which are visibly CL-preserving in the datatype, and what is NOT established (C7 CONJECTURE).
6. **The correspondence table.** One table per rung: paper object (label) → analytic statement → IR sort → Julia name → grade → claim id and current status. This table is the document's thesis: the code *is* the transformation, with the proof obligations carried as data.
7. **What is analytic and what is computed.** A short, sober section: which steps are executed (constructions), which are certified (identities, degree accounts, levels, histograms), which are cited (LDT soundness, rigidity, detyping, introspection, repetition, compression theorem), and what would be required to move each cited leaf into the checked class (Lean? a different kind of object?). Include the midpoint toy as the diagnostic showing why gap preservation, not representability, is the hard requirement (C6, N1: PROVED).

## Style
Terse and precise; theorem/lemma/definition environments with proofs where we actually prove something (the small lemmas of §1, §2, §4 rewrite rule, §5 level laws); \cite-free (cite ground truth by label + file in footnotes or a table); no praise; no "we hope". A physicist should be able to follow every step; define each piece of jargon at first use in one sentence (decider, sampler, normal form, λ-bounded, oracularization, introspection, PCP view, low-degree code, CL function, typed sampler, detyping, anchoring, parallel repetition). The final section must state plainly what remains mathematically difficult and unchanged by the reformulation.
