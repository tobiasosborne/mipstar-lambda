# Brief 16 — TB1 implementation (red/green TDD): CL functions as an inductive datatype; the classical low-degree-test sampler and decider

Proposer (gpt-5.6-sol, xhigh). Autonomous; no questions; no git. Lane: `src/samplers/**` (new), `src/verifiers/ldt.jl` (new), `test/tb1_ld_sampler.jl` (new), `test/mutations/tb1_*.jl` (new), and MINIMAL additive edits to `src/MIPStarLambda.jl` (includes/exports), `test/runtests.jl` (include TB1), `test/mutations/run.jl` (register TB1 mutants). Do NOT modify any other existing file; do NOT edit docs/, claims/, toys/, verifacts/. Report: `briefs/16-tb1.last.md`.

## Read order
1. `CLAUDE.md`; rk-light law 4.
2. `docs/DESIGN.md` §1.5 (Sampler sort: CLZero/CLStep, register subspaces as coordinate index sets, apply/level/marginal_k, concatenate, direct_sum, distribution, product, TypedSampler, L_Point/L_ALine/L_DLine, chi, L_lnf), §5.3 (TB1 instance q=8, m=2, d=1, all 32,768 seeds; χ-free reference sampler; g = 1 + x₁ + x₁x₂; g = x₁² must be rejected; mutations), §6. `docs/definitions.md` (names: chi, pi_{i-1}, L^lnf_v, Point/ALine/DLine, seed_dim, V_pt/V_coord/V_dir, ldparams=(q,m,d,k), D^ld).
3. Ground truth (cite labels in comments): `ground-truth/gt-04-cl.tex` def:cl-func, def:cl-dist, lem:cl-kth, lem:cl-concat, def:cl-canonical (search `label{def:cl-canonical}` in `ground-truth/gt-03-prelim.tex` or gt-04 — find it), def:register-subspace; `ground-truth/gt-07-ldt.tex` def:line, def:line-representative, eq:cl-ptf, eq:cl-alnf, eq:cl-dlnf, eq:chi-func, lem:alnf, lem:dlnf, fig:ld-decider (D^ld: consistency, axis line-vs-point, diagonal line-vs-point; answer formats: k univariate polynomials of degree ≤ d (axis) / ≤ md (diagonal)).
4. Existing code: `src/fields/gf2k.jl` (GF8), `src/polynomials/sparse.jl` (Poly, evaluate), `src/certificates.jl` (Grade, CheckResult, CertNode, Checked) — REUSE, do not duplicate.

## Process
1. Write `test/tb1_ld_sampler.jl` FIRST (RED; paste excerpt), covering DESIGN §5.3: (a) datatype levels: level(L_Point)=1, level(L_ALine)=2, level(L_DLine)=3 from the constructors; (b) apply on all 8^5 seeds and compare EXACT histograms of (L_ALine(x), L_Point(x)) and (L_DLine(x), L_Point(x)) with an independent χ-free reference sampler implementing lem:alnf/lem:dlnf literally (draw i uniform, construct line(u₀,e_i) / line(u₀, π_{i−1}(v)) with the canonical representative), including zero directions; (c) marginal_k replay: sum of stage outputs equals apply for every seed (lem:cl-kth item 3); (d) restriction combinator: `restrict(g, line)` giving the univariate polynomial along the line, degree ≤ d (axis) and ≤ md (diagonal) for g = 1 + x₁ + x₁x₂; (e) D^ld accepts the honest strategy on every type pair for all seeds (or a full deterministic sweep of the CL distribution's support); (f) g = x₁² claimed with d=1 is rejected by an axis-line check at a deterministically named point; (g) consistency test on equal types.
2. Implement: `src/samplers/cl.jl` (CLZero, CLStep with coordinate-index register subspaces ONLY — DESIGN M6 —, apply, level, marginal_k, concatenate, direct_sum, distribution/histogram, product), `src/samplers/typed.jl` (TypedSampler with type graph; sample = choose oriented edge then push one uniform seed), `src/samplers/ldt.jl` (L_lnf_v canonical projection per def:cl-canonical — implement the canonical complement exactly as the paper defines it, not any complement —, chi, pi_prefix, L_Point, L_ALine, L_DLine), `src/verifiers/ldt.jl` (restrict, D^ld as a CheckResult-returning decider). GREEN; paste summary and the trace printout for one sampled question pair of each type.
3. Mutations on copies (`test/mutations/tb1_*.jl` registered in run.jl): (M-χ) shift the χ bucket boundary by one (histogram mismatch vs the χ-free reference); (M-π) L_DLine omits π_{i−1} (diagonal histogram mismatch); (M-lnf) L_lnf uses a non-canonical complement (representative mismatch → histogram mismatch or consistency failure — say which and why); (M-deg) D^ld accepts degree ≤ md on axis lines (g = x₁² no longer rejected); (M-level) CLStep constructor allowed to nest without reducing level (level test fails). Each must be killed by the named test.
4. Time: TB1 ≤ 10 s; whole `runtests.jl` must stay under the 60 s gate (report wall).

## Report (≤ 30 lines)
RED excerpt; GREEN summary + wall; histogram sizes (support counts) for both distributions; the trace printout; mutation lines; deviations from DESIGN (with reasons); MERGE PROPOSAL for claim C4 (proposed TESTED; where-tested pointers) — proposal only.
