# Brief 44 — TB7 implementation (red/green TDD): Compress = Repeat ∘ AnswerReduce ∘ Introspect on descriptions with full structural bookkeeping (§12.1–12.3), ToyPolicy with fail-visible predicates (§12.4), the concrete TB7 execution (§12.5), and the halting fixed point D_{M,L} = Y Ψ_{M,L} (§12.6)

Proposer (Fable). Autonomous; no questions; **no git** (orchestrator commits). Lane: `src/compress/**` (new or replacing the TB4 skeleton's stubs — the TB4 contracts and certificate grammar stay the interface), `src/policy/**` (new: ProductionPolicy, ToyPolicy, predicate report), `test/tb7_compress.jl`, `test/mutations/tb7_*.jl`, MINIMAL additive edits to `src/MIPStarLambda.jl`, `test/runtests.jl`, `test/mutations/run.jl`. TB2's answer-reduce is re-wired to consume/produce DESCRIPTIONS (§9) — that is in lane, additively, with every changed line listed. Report: `briefs/44-tb7-compress.last.md` (≤ 45 lines).

Scratch: `/tmp/claude-1000/-home-tobias-Projects-discussions/548f633b-c857-457a-bb33-0aa1a7879403/scratchpad/tb7/`

## Why this rung matters
This is the north star: the complete executable `Compress` on verifier descriptions, every invariant tracked, with the two non-executed layers (`P_pcp_encodes_D1`, non-Pauli intro schemas at `Q_I<s_0`) printed FAIL/VACUOUS with owners rather than hidden. The construction must be the paper's general one (production mode with symbolic universal constants, DD-29 compact descriptions, `BudgetExceeded` instead of capping); the toy is only a policy (DD-28). Design for reuse: another paper's transformation should be a new `src/<name>/` module against the same description API and certificate grammar.

## Read order
1. `CLAUDE.md`; rk-light laws 1, 4, 5; `handoff.md` §"The fixed-point part" and §"The compression skeleton"; `claims/CLAIMS.md` C12, C13, C14, C15 (CONJECTURE rows; C15 is the target; statuses change only by verdict).
2. `docs/DESIGN.md` §12 IN FULL (constructor order = fig:compress; the universal-constant AST `mu, gamma, epsilon1, epsilon2, tau, k(n)`; the `IntroGap` max with the CHECKED scalar floor branch and CITED Ent branch; the six-row bookkeeping table with exact dimension laws and the level chain `9→5→7→9`; `SOURCE_REPAIR(intro-decider-fixed-width)` and the two fixed `lambda`-byte slots; `dependencies(S_compress)={lambda, universal_constant_ids}` with two byte-distinct input pairs, one straddling `|V|<=lambda`; ToyPolicy semantics and DD-28/DD-31; the TB7 fixture `s_0=9, n=2, lambda=32768`, toy tuples, dims `206→840→848→1696`, 3894 symbols / 42,834 bits, guard 65,536; the 13-row predicate report — it is the test oracle verbatim; per-sampler chain/replay counts; `representation=structural-evaluator`; the eleven `M7-*` mutations; §12.6's five-step fixed-point run), §9 (API), §10–11 (the transformations you compose), §13.1 TB7 row, §13.2 (what stays CITED — the residue inventory your certificate tree must match label for label). `docs/definitions.md` §G–H. `verdicts/design-v2-r3.md` §2.3 (the critic's TB7 recomputation) and the N2 adjudication.
3. Ground truth (recompute, never from memory): `ground-truth/gt-12-compression.tex` L75–L98 (fig:compress), L108–L147 (lem:compress-independent-samplers), L156–L187, L229–L232, L263–L308, L332–L359 (parameters, eq:mu-gamma, eq:re-eps-1/2, eq:c_rep), L426–L492 (the self-description construction and halting branches), L502–L519, L569–L576 (CITED conclusions); `gt-10-answer-reduction.tex` L1813–L1828, L1948–L1965, L2060–L2063, L2094–L2096 (thm:ar interface, enu:ar-game, downsize before direct sum); `gt-08-introspection.tex` L757–L776, L809–L815.
4. Existing code: TB4 (`src/compress.jl` skeleton, contracts, `YCode`/`Fix`/`Hole`/`Specialize`), TB5 (`src/descriptions`, `src/repeat`), TB6 (`src/introspect`), TB2 (`src/verifiers/answer_reduce.jl`, `src/samplers/oracularize.jl`), TB3 (bounded_trace/cook_levin/decouple5 — needed for `P_pcp_encodes_D1`'s honest FAIL: the instance must be checked against the ACTUAL fixed-width `D1` trace and found not to encode it).

## Process
Tests FIRST (`test/tb7_compress.jl`, RED excerpt): (a) constructor order AST equals fig:compress (`M7-order` red); (b) universal-constant ASTs equal hand-transcribed expected ASTs, symbols never replaced by literals in production (`M7-constant`); (c) the bookkeeping table: every row's field/level/dimension law as a `LawCert`, level chain `9→5→7→9`, exact dims `206→840→848→1696` (`M7-level`), explicit PCP downsize before the direct sum (`M7-field-align`), typed product tensor edge `((O,Point_1),(A,DLine_6))` and 54-type support (`M7-product-cartesian`); (d) `sigma_1` fixed-width: two byte-distinct inputs at the same `lambda` give identical `S_compress` bytes and hash, a second pair straddles `|V|<=lambda`; `dependencies(S_compress)` by syntax walk (`M7-unpadded-sigma`); (e) `IntroGap` AST with both branches, scalar floor branch evaluated symbolically without materializing the integer (`M7-intro-floor`); (f) ToyPolicy: identical code path, ASSUMED `toy_override` child, every predicate printed; the 13-row report of §12.5 reproduced verbatim, including `FAIL(owner=Q_I<s_0)`, `VACUOUS(owner=Q_I<s_0)` for every non-Pauli schema (executed-schema count 0), `FAIL(owner=pcpverifier-D1-trace)`, `NOT_EXECUTED(owner=pcpverifier-D1-trace)`; `M7-grade`, `M7-cap`, `M7-pcp-content`, `M7-intro-schema` red; (g) execution: all four final sampler queries on a declared chain set, 16 final questions, per-sampler `sampler_id / chain_set_id / selected / distinct / replayed` counts for every primitive, intermediate and final sampler (empty set VACUOUS(owner=chain-coverage)); local Pauli / PCP encoding-consistency / low-degree / detype / anchoring / repetition sub-tests with their own grades; `representation=structural-evaluator` and no dense monomial vector (assert allocation); (h) §12.6 fixed point: `D_{M,L}=Fix(Psi_{M,L})` reserialized, `self_code` hash equals outer quote hash, two steps of `M_loop` simulated taking the nonhalting branch, `Compress((S_L,D_{M,L}),lambda)` sampler hash equals TB7's independent hash, the compressed decider runs on the supplied transcript with no host recursion, and the fuel-limited final child call returns `OutOfFuel` at the declared boundary (separately labelled synthetic evaluator-entry test); (i) the complete certificate tree printed, and a test that the set of CITED labels in the tree equals §13.2's residue inventory exactly (no phantom citations, no missing residue).
Implement; GREEN; walls broken out (Introspect / AnswerReduce / Repeat / transcripts; target < 60 s warm, < 512 MiB — measured values replace targets); the eleven `M7-*` mutations registered and KILLED under the unmutated-first runner; whole suite under the TB0 gate; runner exit 0.

## Report (≤ 45 lines)
RED excerpt; GREEN summary + walls + peak; the 13-row predicate report as printed; the bookkeeping table as printed; per-sampler chain/replay counts; the certificate tree for `Compress(TB7)` (this printout is a tutorial deliverable — make it beautiful); mutation lines; CROSS-LANE EDITS (incl. every TB2 line re-wired to descriptions); deviations from §12 with the gt line that forced each; MERGE PROPOSAL: C15 row text with the measured evidence, and a ≤ 8-line "generalisation surface" note: what a new transformation module must provide to plug into Compress (proposal only).
