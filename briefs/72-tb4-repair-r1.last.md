# Brief 72 — TB4 repair r1 report (Fable proposer, 2026-09-05; NO-GIT, tree be383bd + working changes; built on TB5's StageVerifier/params carrier, nothing reverted)
**RED (the critic's survivors).** CRITIC-1 (`_levels(input)` for `COMPRESS_LEVELS`) and CRITIC-2 (`Set(origins)`) SURVIVED the whole TB4 file at `278b1ac` (`verdicts/tb4-r1.md` §3); both are registered verbatim as `M-ell-from-input` / `M-order-blind` (target `tb4_levels`) and are KILLED by the new (f) witnesses: a hand-built `Introspect → Repeat → AnswerReduce` chain (`StubVerifier(out; input=w3.term)`) has `level_chain == [9,5,7,9]` and is refused by the `:LevelChain` node's own replay (`rule=:level_chain`, `actual=[9,5,7,9]`); `Compress` of the fixture verifier re-labelled 5-level (explicit stages, no fixture rebuild) records `levels = 5, ell = 9 => FAIL` at `ell_level`, chain `[5,5,7,9]`, refusal at `nine_level`.
**O3.** `halting_verifier` adds `[ASSUMED] CompressStubInTerm` under `:Verifier`: names `COMPRESS_STUB`, the constant `(pair, lambda) -> Quote(lambda n x y a b . true)`, **46 of 376** term bytes (computed, not literal; facts `compressor/stub_bytes/term_bytes`), and that `COMPRESS_IDENTITY` (`snd_code`) is the only non-constant compressor exercised; (b) now asserts the looping machine's compressed branch is that constant (`ev.code.head` program-equal to `COMPRESS_STUB`; `Apply(COMPRESS_STUB, …)` evaluates to `Code(TRIVIAL_DECIDER)`) — `M-stub-undisclosed` KILLED (`tb4_psi`).
**O4.** `halting_decider` appends `SOURCE_REPAIR(:HaltDeciderFuelBound)` (facts `source/lines` = gt-12:451-453) to the CHECKED `:Specialize` node; the verifier's runtime bound reads "TIME_D: budget n^lambda enforced by construction, not measured (FuelBound(n, lambda) …, SOURCE_REPAIR HaltDeciderFuelBound)", which is what every `lambda_bounded_time` display now quotes; `docs/definitions.md` §F row added (fig:halt_f step 5 / lem:lambda L570-L638) — `M-fuelbound-undisclosed` KILLED. Verifier certificate rules: `[:Verifier, :Quote, :Specialize, :Quote, :HaltDeciderFuelBound, :CompressStubInTerm]`.
**O5.** DESIGN §5.6: brief 24 MERGE PROPOSAL (2) applied verbatim ("require the origin sequence to differ from (Introspect, AnswerReduce, Repeat); at ℓ = 5 both rules give 7, so levels alone cannot witness the swap"), plus the ℓ = 9 pin, the two disclosures and the budget sentence below.
**O6.** §F: `Compressor` → "`lambda : P{Nat}`, the resource bound of `def:lambda`" and the decider-only output recorded as a named narrowing licensed by fig:halt_f step 3 + lem:dhalt-values (L502-L519) + lem:compress-independent-samplers (the MP-2 (i) citations; the circular §1.1 sentence replaced by the same two); `Psi_{M,L}`/`D_{M,L}`/`S_L` renamed in rows 21, 165, 166, 168, 223; row 169 (`lambda`) given its third cell `gt-05-games-normalform.tex:L641-L653 (def:lambda)` — every §F row has 4 pipes.
**O7.** All three `INTROSPECT_CONTRACT` hypotheses prefixed "(completeness/soundness only)", its `ell_level` source widened to L784-L803, conclusions marked "(unconditionally)"; DESIGN §1.6 bullet 1 rewritten in bullet 4's form; asserted for Introspect and Compress — `M-intro-unscoped` KILLED; `M-hyp`'s anchor updated (same mutation).
**O8.** `pcp.jl`: `:UpstreamEvidence`'s CHECKED content is the identity anchor only; the build-time reproduction is its CONSTRUCTED child `:UpstreamReproduction` (second child — TB3 (f) pins `children[1] == :Pad5`), and the comment now says `TseitinFormula.program` is mutable in place and a tampered formula is caught by `:PCPVerifier`'s replay — `M-reproduction-undisclosed` KILLED.
**O9.** New testset (h) `tb4_two_verifiers`: `Compress` of the halting and looping machines at λ = 1024 (byte-distinct `D_{M,λ}`, both 388 bytes, same fixture stages): the two 71-line `traceprint`s differ in **exactly one line**, the input decider's `[CHECKED] Quote` (`dbf2c0815358657f` vs `2b9de4fb8ea9b2f9`), dependency symbols equal, both verify — `M-independence-leak` (the `:SamplerIndependence` display leaking `quote_hash(input.decider)`) KILLED. The byte/hash form stays TB5's (`S^rep`, brief 39 (h)).
**O10.** `AnswerReduceSurrogate` is now the PARENT of the relocated `detyped.certificate` and `fixture.pcp.certificate`; (d) asserts every `:PCPProof/:UpstreamEvidence/:Pad5/:Decouple5/:CookLevin/:BoundedTrace/:Tseitin/:PCPVerifier/:TypedAnswerReduce/:AnswerReduceSamplerProduct/:Detype` node under the root is a descendant of it — `M-surrogate-sibling` KILLED.
**O11.** `:Detype` (`gt-06-types.tex:L445-L475 (lem:detyping-verifiers)`) and `:Oracularization` (`gt-09-oracularization.tex:L34-L86 (sec:orac-def)`) carry `source/lines/label`; (d) greps every CITED node with those facts (7 now) — `M-detype-unlocated` KILLED. **O12.** `L239-L243`; (e) now greps every `\label` named by every hypothesis source inside its own range (12 labels; `nine_level`'s range widened to L27-L41 so it contains `thm:compression`) — `M-offbyone` KILLED.
**O13.** The 5 s body budget is REVISED to 6 s in DESIGN §5.6 (the O1/O2/O9 witnesses add two `Compress` calls; measured in-suite 5.9 s quiet) and enforced as tb1-r5 N33's clock-calibrated ratio: a GF(8) kernel timed in-process before the first testset (0.14 s standalone / 0.18 s in-suite here) and `elapsed / calibration < TB4_RATIO = 42` (6 s at the standalone rate; in-suite ratio measured 32) when the file runs inside the suite; a standalone `all` run is cold-JIT (7–13 s) and prints ungated; `TB4_BUDGET_SECONDS` only adds a wall bound; `M-gate-uncalibrated` (target `tb4_gate`, test-file mutant) KILLED. CROSS-LANE: `test/runtests.jl`'s TB4 print reworded to point at the gate (one line).
**O14.** `specialize`'s `:Specialize` replay is `_bound_replay`-anchored and recomputes `substitute(p, env)` (`program_equal`); (a) asserts an open replacement throws and a byte-identical twin `Quoted` is refused as `:certificate_binding` at `:Specialize` — `M-specialize-open` (the verdict's text) and `M-specialize-unbound` KILLED.
**N1–N4.** N1 (part2a §10 stale on MP-2 (ii) and `c_fix`): analytic-doc lane, not touched — MERGE PROPOSAL (2) below. N2: recorded in the C11 sentence below. N3 (`:LevelChain` CHECKED over constructed data): unchanged, display already says "by introspect_levels(9) = 5, …". N4 (`AnswerReduce`/`Repeat` namespace): unchanged; TB5's `Repeat(::ExecutableRepeat, …)` already coexists with `Repeat(::RepeatStub, …)` by dispatch.
**Certificate.** 67 → **70 nodes** (CONSTRUCTED 7, CHECKED 30, CITED 9, ASSUMED 19, SOURCE_REPAIR 5): +`CompressStubInTerm`, +`HaltDeciderFuelBound`, +`UpstreamReproduction`. TB4 assertions 208 → 333 (the 2-assertion gate included); mutants 13 → **26**, all KILLED with passing baselines (`tb4_two_verifiers`, `tb4_gate` are new baseline targets).
**MERGE PROPOSALS (proposals only).** (1) C11 strengthened (replaces the critic's authorized weaker row; C16/C18 untouched): "| C11 | (Compress skeleton — composition, contracts and the quoted fixed point) On the TB4 fixture `V = (S_λ stub, D_{M,λ})` at λ = 1024, with `|D_{M,λ}| = 388` bytes and fnv1a64 `dbf2c0815358657f` (critic-reserialized independently, `verdicts/tb4-r1.md` §1), `Compress = Repeat ∘ AnswerReduce ∘ Introspect` builds the three ASSUME/PROVE contracts in fig:compress order (`gt-12-compression.tex:L75-L98`) with level chain 9→5→7→9 (`introspect_levels(9)=5`, `answer_reduce_levels(5)=max(7,5)=7`, `repeat_levels(7)=9`); the ORDER conjunct of the certificate's own `:LevelChain` replay is red-capable — a hand-built Introspect→Repeat→AnswerReduce chain has the same levels (at ℓ = 5 both rules give 7) and is refused (`M-order-blind`, the critic's CRITIC-2, KILLED) — and fig:compress's literal ℓ = 9 is pinned: `Compress` of a 5-level verifier records `levels = 5, ell = 9 => FAIL` at `ell_level` (`M-ell-from-input`, CRITIC-1, KILLED); composed runtime bounds whose free parameters are exactly {n, λ} after binding ℓ = 9, the toy literals μ = γ = τ = 1, and `c'` and `|D1|` to prose descriptions of unexposed constants (`bind_parameter` ignores an absent name and accepts any host value, so that closure is partly a naming fact, `verdicts/tb4-r1.md` N2); a composed sampler-DEPENDENCY-SYMBOL set ⊆ {λ, ℓ, μ, γ, τ, |D1|} containing no component of `V` (no sampler bytes exist at TB4: the output is a `StubVerifier`; the byte/hash form is TB5's `S^rep`), and, in the form TB4 supports, `Compress` of two byte-distinct 388-byte inputs at one λ yields 71-line traces differing only at the input decider's `Quote` hash (`M-independence-leak` KILLED). The executable evidence in the tree is CHECKED Quote/Specialize on `D_{M,λ}` (host-side specialization, size law with `c_fix = 5`; `specialize`'s own node is identity-bound and recomputes the substitution, `M-specialize-open`/`M-specialize-unbound` KILLED) and the TB0/TB2/TB3 PCP and typed-answer-reduction subtrees **on the 33-byte trivial front-end fixture**, every one of them a DESCENDANT of `[ASSUMED] AnswerReduceSurrogate` with σ = 33 passed explicitly (`M-surrogate-sibling` KILLED), the front end's Tseitin reproduction being the CONSTRUCTED child `:UpstreamReproduction` of the identity-anchored `:UpstreamEvidence`; the certificate is 70 nodes (CONSTRUCTED 7, CHECKED 30, CITED 9, ASSUMED 19, SOURCE_REPAIR 5), every CITED leaf carries `replay === nothing`, every CITED node with a locatable citation (the five theorem leaves, `:Detype`, `:Oracularization`) and every `\label` named by a hypothesis source (12) is grepped inside its cited range; a violated checkable hypothesis (|V| > λ; wrong ℓ; a Concrete TIME > n^λ at n = 2) is an ASSUMED FAIL node that `verify_certificate` rejects at that hypothesis, and every Introspect/Compress hypothesis is scoped "(completeness/soundness only)" per gt-08:789-803 / gt-12:26-53. **Scope:** three stubs stand in for the construction, all three disclosed in the tree — `S_λ` (`λx.x`, 34 bytes), the `Introspect` and `Repeat` stages (CITED, no execution), and the `Compress` program inlined in `D_{M,λ}` itself (`[ASSUMED] CompressStubInTerm`: `COMPRESS_STUB`, 46 of 376 term bytes, the constant `(pair,λ) ↦ Quote(λnxyab.true)`), so the "compressed branch" evaluated in testset (b) is that constant; and the outer `Eval` runs the returned decider under an enforced `FuelBound(n,λ)` where `fig:halt_f` step 5 (`gt-12-compression.tex:L451-L453`) imposes no budget — `SOURCE_REPAIR(:HaltDeciderFuelBound)`, the verifier's time bound reading "enforced by construction, not measured" (never binding on the fixtures: 12 and 91 units at n = 2). Red-capable by 26/26 registered mutants (`test/mutations/tb4_compress.jl`), the in-suite body gated at `elapsed / calibration < 42` (tb1-r5 N33). | TESTED | C9, C10, C16, C18 | `src/compress.jl`, `src/ir/programs.jl` | `test/tb4_compress_ir.jl`; red: `test/mutations/tb4_compress.jl` | `verdicts/tb4-r1.md` |". (2) N1, analytic lane: part2a §10 to state MP-2 (ii) as ADOPTED (`fuel − c_Y`, `c_Y = 3`, verified at every fuel) and `c_fix = 5` bytes = one tag byte + a 4-byte child count with no serialized sort annotation. (3) DESIGN §1.6's invariant table row "CL levels | CONSTRUCTED" vs the CHECKED `:LevelChain` (N3): add "the chain's arithmetic and origin order are CHECKED over the constructors' data; the stage levels themselves are CITED theorem literals".
**Unresolved.** The ratio gate is calibrated on one box (i7-1365U, performance governor); a differently balanced machine may need `TB4_RATIO` re-set once — the numbers to re-derive it are printed on every suite run. `M-relabel` still targets `src/certificates.jl` (unchanged).
**Final runs (after brief 73; performance governor; no other runner in `pgrep`).** Registry: **`MUTATION REGISTRY: killed=157/157 baselines ok=64/64 wall=709.43 s`, exit 0** (TB4 26/26 KILLED, the two survivors at 17.8 s / 14.8 s; new baselines `tb4_two_verifiers`, `tb4_gate` OK; `uptime` 13:19:18 load 2,19 → 13:31:08 load 4,45). Suite quiet: **2393/2393, exit 0**, wall 1m39.0 s, TB0 body 16.115 s (gate 60), **TB4 body 5.348 s, calibration kernel 0.1875 s, ratio 28.5 (gate 42.0; gated)**, TB4 include 5.727 s, TB5 23.72 s; `uptime` before/after `13:31:40 load 2,86 4,39 4,12` / `13:33:20 load 1,57 3,52 3,83`. Earlier in the round, with a recompile and a TB3 process concurrent, a STANDALONE cold-JIT run of the file measured body 12.7 s at kernel 0.267 s (ratio 47.6) — the reason the gate is in-suite only.
