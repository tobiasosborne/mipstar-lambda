# Brief 54 report — TB1 + TB2 repair r3 (tree at HEAD `5641bee` + this lane; NO git)

| objection | disposition | where |
|---|---|---|
| N12 (TB1) = N6 (TB2) MAJOR | FIXED. (a) the encoder already wrote every matrix entry and every `ByAxis` table (the critics' NM7/NE1 were edits of the encoder, not the tree), so no format change and the sizes are unchanged: TB1 75/132/156, TB2 3009/2893/10228/2754/10479 — now ASSERTED exactly (`test/tb1_ld_sampler.jl:295`, `test/tb2_answer_reduce.jl:249-254`). (b) `decode_cl` (`src/samplers/cl.jl:562-687`: bytes -> term -> `_term_to_cl`/`_branch_to_cl`, child shapes rebuilt per branch, `:Padded` via `_pad_tail`) with the round trip `canonical_bytes(describe_cl(decode_cl(b))) == b`, `apply` equality on all `3 x 8^5` seeds + 2 padded maps (`tb1:303-315`) and on the 36-seed chain set for all 18 PCP maps incl. `factor_spaces` (`tb2:295-309`). (c) separator `alt` = projector onto `V_coord (+) V_dir`: `apply` differs, size 75, bytes differ (`tb1:325-335`); 18 PCP byte strings pairwise distinct (`tb2:257`); copy-6 `ByAxis` tables hold 16 distinct children (`tb2:263-266`). (e) stage-1 matrix at bytes 41:65, both windows asserted exactly, header/tail bytes equal (`tb1:336-342`); truncated/trailing bytes rejected. Mutants `tb1_describe_matrix.jl` (NM7 verbatim) and `tb2_describe_byaxis_collapse.jl` (NE1 verbatim), evidence `describe_roundtrip ok=false`: KILLED. |
| N13 (TB1) MAJOR | FIXED. `tb1_prefix_walk.jl` registered verbatim (target `tb1_space_sum`): KILLED (`report.map_sum_ok` is its owner). `map_sum_ok` is now red-capable. |
| N14 (TB1) / G3 | FIXED. `Marginal` throws `ArgumentError` for `j` outside `1..ell` (`cl.jl:305-310`); `cl_kth_replay` builds `prefix_1` as the explicit zero vector (`cl.jl:406`); asserted in `queries` for all three maps, `j=0` and `j=ell+1` (`tb1:242-248`). |
| N4 PARTIAL / N15(a) | FIXED. `ld_honest_answer`, `ld_honest_sweep`, `ld_sweep_evidence` (`src/verifiers/ldt.jl:189-278`): the sweep is a CHECKED `:ld_honest_sweep` node whose replay RE-RUNS the sweep from `(params, g, samplers, seeds)` and requires the recount to equal the recorded report with 0 off-line hits; `:ld_off_line_rejects` is its SOURCE_REPAIR child; facts carry `d=1`, `md=2`, `kappa=1`, 2,880/37,888. `verify_certificate` passes; a tampered report (`off_line_hits=1`) is rejected (`tb1:591-612`). C4c proposal below. |
| N15 (TB1) / N16 | FIXED. `_pad_tail(CLZero)` is one rule for every register (`typed.jl:14-24`); `_pad_top` (`typed.jl:31-33`) reads a top-level `CLZero(F,n,Int[])` as the zero map on `F^n` and promotes with `V_1={1..n}`; inside a chain the empty-register terminal stays empty (its V is `{0}`, and `_child` requires the rest register), which is why "ambient in both cases" cannot mean `1:n` internally. Asserted: `pad_level(CLZero(F,5,Int[]),3)` factors `[[1..5],[],[]]`, `cl_kth_replay(...).space_sum_ok`, padded `L_ALine` third factor `[]`, sub-register `{2}` still promoted on `{2}` (`tb1:175-188`). DESIGN wording below. |
| N7 (TB2) | FIXED (honest route). `pad_level_evidence(L, target, seeds)` (`typed.jl:51-90`): CHECKED `:pad_level` node, replay checks level, child-marginal survival and both `lem:cl-kth` obligations; `ZERO_MAP_FACTOR_PARTITION` is its child exactly when the promotion ran — verified at TB1 for `CLZero(F,5)`, the empty-register zero map (child present) and `L_ALine` (no child) (`tb1:161-174`). TB2 never pads a zero map: asserted that no described stage has an empty factor register, all 54 products are level 3 before `TypedSampler` pads, and no TB2 certificate carries `:zero_map_factor_partition` (`tb2:213-224`, `:267`). |
| N9 (TB2) | FIXED test-side (the certificate replay in `answer_reduce.jl` is out of lane and still runs at the zero seed): new testset `replay_seeds` (`tb2:565-609`) runs the seven cases with honest TB0-proof answers at the zero seed, `tb2_seed 5` and an RNG(0x9E) full-field seed; all 21 (case, seed) pairs: honest accepted, corruption rejected with the expected rule, step reached. |
| N10 (TB2) | FIXED. `no_check` asserts `(2736, 180, 107, 92, 54, 53)` (`tb2:549-557`); mutant `tb2_guard_split.jl` widens the step-2 guard of `answer_reduce_guard_branches` to line counterparts: no-check stays 2736, step-5-alone drops 92 -> 84, evidence `guard_split actual=(2736, 180, 107, 84, 54, 53)`: KILLED. |
| N17 | `tb1_factor_reachability.jl` (dual of linear_narrowed, target `tb1_queries`): KILLED. N18: sizes pinned; the `chi`-indexed literal table is charged one child per axis (`ALine_6` 10228 vs `ALine_1` 2893) — DESIGN §9.3 sentence proposed below. N19: orchestrator lane (§9.4 citation already repaired at HEAD:1191; `(single factor space = all of V)` still absent from §1.5 — proposed). N20: walls below. N21: `levels` now pins the field guard's message (`tb1:121-128`). N22: unchanged (memo flush; LRU is DL9 shape). TB2 N8: DESIGN — proposal below. Cosmetic: TB1 labels `N3-space-sum`, `N5-pad-order` (the pad-order mutant re-anchored to `_pad_top`). |

**Runs.** `julia --project=. test/runtests.jl`: 638/638, exit 0, twice — loaded (load 4.5, my own runs decaying): TB0 body 44.113 s, suite 2m50.8, maxrss 1196 MiB; quieter (load 2.9, one critic probe resident): TB0 body **39.056 s**, suite 2m14.8, maxrss 1187 MiB. `julia --project=. test/mutations/run.jl`: **killed=69/69 baselines ok=37/37 wall=555.67 s**, exit 0 (load 4.2 -> 5.4; 9:16 elapsed). New mutants all KILLED: describe-matrix 17.8 s, prefix-walk 16.1 s, factor-unreachable 11.3 s, byaxis-collapse 24.4 s, guard-split 31.9 s. Description sizes after the fix: unchanged (see N12). Testset deltas: TB1 levels 26->37, queries 23->27, describe 22->33, decider 12->17; TB2 sampler 22->26, describe 117->126, no_check 2->3, replay_seeds 86 new.

**CROSS-LANE EDITS.** `src/MIPStarLambda.jl:50-51,56-57`: exports `decode_cl`, `pad_level_evidence`, `ld_honest_answer`, `ld_honest_sweep`, `ld_sweep_evidence` (additive only). Nothing else outside the lane was touched; no git command run.

**MERGE PROPOSALS (verbatim; orchestrator applies).** C4a Scope — strike: "and its map-sum comparison currently has no red witness (`verdicts/tb1-r3.md` N13); the canonical description bytes are asserted only by length and determinism — no test separates two distinct CL maps by their bytes (ibid. N12); `Marginal` accepts the stage index `j=0` that `Factor`/`Linear` and `def:sampler` reject (ibid. N14)." — replace by: "; its map-sum comparison is owned by `tb1_prefix_walk.jl` (`verdicts/tb1-r3.md` N13). The canonical bytes determine the map: `decode_cl` round-trips all three maps on all `8^5` seeds, `L_Point` and the `V_coord (+) V_dir` projector (75 bytes each) have distinct bytes, and the stage-1 matrix occupies bytes 41:65 (ibid. N12). `Marginal` rejects `j=0` like `Factor`/`Linear` (ibid. N14). `D^ld` is C4c." Red list += `describe_matrix,prefix_walk,factor_reachability`.
C4b Scope — strike "The `rk:higher-level` zero-map promotion is implemented by `pad_level` but is never reached by this rung's maps, and its `SOURCE_REPAIR :zero_map_factor_partition` node is not carried by any TB2 certificate (ibid. N7)." → "The `rk:higher-level` zero-map promotion is never reached by this rung's maps (asserted: no described stage has an empty factor register, all 54 products are level 3 before padding, no TB2 certificate carries `:zero_map_factor_partition`); `pad_level_evidence` carries it at TB1 (ibid. N7)."; strike "the suite prints rather than asserts those integers and no test ties `canonical_bytes` to the map, so a description that misstates the `BranchByAxis` table survives the suite (ibid. N6)" → "asserted exactly by the suite, the 18 byte strings pairwise distinct, `decode_cl` round-tripping every map on the declared chain set and the copy-6 `BranchByAxis` tables holding 16 distinct children (ibid. N6)". Red list += `tb2_describe_byaxis_collapse`.
C9 — strike "only the 2736/2916 figure is asserted by the suite" → "all six numbers `(2736, 180, 107, 92, 54, 53)` are asserted by the suite (`tb2_guard_split.jl`)"; strike "The seven-case certificate replay runs at the all-zero seed only (`verdicts/tb2-r3.md` N9)." → "The seven-case certificate replay runs at the all-zero seed; the suite re-runs the seven cases with honest TB0-proof answers at three seeds (zero, `tb2_seed 5`, RNG 0x9E) and asserts the same four facts per case (ibid. N9)". Red list += `tb2_guard_split`.
C4c (new row, proposal): "| C4c | (TB1 `D^ld`, `(q,m,d,kappa)=(8,2,1,1)`) `ld_decider` implements `fig:ld-decider` with answer bounds `d=1` (axis) and `md=2` (diagonal, tight: `verdicts/tb1-r3.md` §1.4), symmetrized over the pair order, `kappa=2` exercised (accept / second-entry cheat / short answer). Over all `8^5` seeds the honest prover for `g = 1 + x1 + x1 x2` is accepted on all 71,360 distinct support decisions of the nine ordered type pairs; 40,768 are non-noop, split 2,880 equal-type tautologies / 37,888 line-versus-point; the off-line branch is reached 0 times. The sweep is a CHECKED `:ld_honest_sweep` node (`ld_sweep_evidence`) whose replay recounts it, carrying `SOURCE_REPAIR :ld_off_line_rejects` (`gt-07-ldt.tex:377-384` accepts vacuously when no `t` exists; the executable rejects, strictly stricter). **Scope:** one polynomial, one field row, no soundness claim; equal-type decisions are identical-question tautologies (`verdicts/tb1-r2.md` N11). | TESTED (proposed) | D2, C4a | — | `test/tb1_ld_sampler.jl` (`decider`, `decider_rejections`, `degree`, `restrictions`, `trace`); red: `test/mutations/tb1_{deg,agreement,symmetry,verifier_pi,online,question_arity,kappa,dline_degree}.jl` | `verdicts/tb1-r1.md`..`tb1-r3.md` |".
DESIGN §9.4 (HEAD:1191-1192) replace "`pad_level` implements this for any zero map on a nonempty register ... (`verdicts/tb1-r3.md` N15, TB5 obligation)." by "`pad_level` promotes a zero map with `V_1` = the space the value acts on: its register `R` (for a continuation, the enclosing stage's rest register — the empty register is the zero-dimensional chain terminal and stays empty), and the whole ambient `{1..n}` for a top-level value declared on the empty register (`CLZero(F,n,Int[])` is the zero map on `F^n`). `pad_level_evidence` carries `ZERO_MAP_FACTOR_PARTITION` whenever the promotion ran (`verdicts/tb1-r3.md` N16, `tb2-r3.md` N7)." §1.5 (HEAD:372): "a zero map on a register is promoted with that register as stage 1" → "a zero map is promoted with the space it acts on as stage 1 — its register for a continuation, the whole ambient for a top-level zero map"; and append "(single factor space = all of V)" to the `L_Point` line. §9.3: after "which the adapter maps to `QueryError`" add "; `Marginal` rejects `j=0` like `Factor`/`Linear`, and `cl_kth_replay` supplies the §9.2 zero prefix itself (N14). At TB2's `m'=16` the literal `chi`-indexed `BranchByAxis` table is charged one child term per axis (`ALine_6` 10228 vs `ALine_1` 2893 bytes); the compact-loop form is `DL9` work (N18)"; and for tb2-r3 N8 reword "the pair adapter `describe_cl(LA,LB,q)` wraps it" to name it as TB5 work (already partly done at HEAD).

**Unresolved.** `SamplerDescription`, the pair adapter and description-level `direct_sum` (G1/G2/G5) untouched (brief 39). The certificate replay in `answer_reduce.jl` still runs at the zero seed (lane); the three-seed evidence lives in the test. Quiet-box walls were taken with one critic probe resident (load 2.9); the TB0 body 39.056 s is 6 s under the warning.
