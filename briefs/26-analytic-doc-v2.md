# Brief 26 — Analytic document v2: develop the Turing-machine → lambda-calculus material extensively

Proposer (gpt-5.6-sol, xhigh). Autonomous; no questions; no git. Lane: `docs/analytic/**` only. Revise `docs/analytic/analytic-underpinnings.tex` in place (keep the sections that exist; this brief EXPANDS §1–§2 into a self-contained development and adds what is listed). Compile with pdflatex twice; zero errors; report warnings. Report: `briefs/26-analytic-doc-v2.last.md` (≤ 15 lines).

## The user's directive (binding)
"I want the whole Turing machine → lambda calculus stuff to be more extensively developed. The MIP*=RE paper is all Turing machine, and this is new context." The reader knows the paper's Turing-machine formalism (gt-03-prelim.tex §Turing machines; gt-05 def:decider, def:sampler, normal form verifiers, λ-bounded) and NOT lambda calculus as a resource-accounted description language. So the document must build that bridge from the ground up, with theorems and proofs, not by allusion.

## Required new/expanded content (new §1–§3 replacing the current §1–§2; renumber the rest)
1. **The paper's machine model, exactly.** Transcribe the paper's conventions: multi-input Turing machines, the (n,x,y,a,b) input format of deciders, TIME_D(n), TIME_S(n), description length |D|, λ-bounded (def:lambda), what "description of a verifier" means and how Compress/ComputeIntroVerifier/ComputeAnsVerifier take descriptions as input (cite gt-12 fig:compress, thm:compression; gt-10 thm:ar "outputs the description of the answer reduced verifier"). Point out every place the paper needs a *universal* machine or a source-level manipulation (the introspection games' use of the sampler description, the answer-reduced decider computing Circuit = PaddedSuccinctDecider(D,…) from the *description* of D (fig:pcpverifier step 1), the self-referential F(F,M,L,…) in gt-12's Halting section). This is the bookkeeping the handoff says homoiconicity might remove; list it explicitly as "obligations".
2. **A lambda calculus with resources.** Define the description language L (DESIGN §1.1 grammar: de Bruijn variables, λ, application, Fix, If, bounded primitives Prim with cost contracts, Quote/Eval/Specialize, Hole) as a formal system: syntax, canonical serialization (so |t| is a number), small-step operational semantics with an explicit fuel counter (each step consumes fuel; Prim consumes its declared cost), the value/OutOfFuel/TypeError trichotomy. Prove: (a) determinism; (b) fuel monotonicity; (c) that Quote/Eval satisfy eval(Quote t, u; f) relates to eval(t u; f') with an explicit fuel overhead; (d) that Specialize is capture-avoiding substitution with |Specialize(t,σ)| ≤ |t| + Σ|σ|.
3. **Simulation theorems with constants (the bridge).** (a) TM ⇒ L: for every Turing machine M (paper's model) there is a closed term ⟦M⟧ ∈ L such that eval(⟦M⟧, ⟨x⟩; fuel = c·T·(T+|x|)) = M(x) whenever M halts within T steps, with |⟦M⟧| = O(|M|); give the construction (tape as a list/zipper Church-style or via bounded primitives — choose one and be explicit; make the encoding of the (n,x,y,a,b) input format explicit). (b) L ⇒ TM: a Turing machine U_L (the universal evaluator for L) that on input (t, u, f) runs in time poly(|t|+|u|+f) and description length O(1); hence a decider given as a term is a decider in the paper's sense with TIME_D(n) ≤ poly(fuel(n), |t|). (c) The resource dictionary: a table mapping each paper quantity (TIME_S, TIME_D, |S|, |D|, λ-bounded, the "n ≥ C₀" thresholds) to its L counterpart with the polynomial-overhead statement, and a proof that λ-boundedness is preserved in both directions up to a constant change of λ. State clearly which constants are absorbed by the paper's poly() and which are not.
4. **Self-reference.** (a) Kleene's second recursion theorem for the paper's model, stated and proved in the standard way (s-m-n + diagonalization), with the description-size accounting. (b) The Y/Z combinator in L: definition, the fixed-point equation as descriptions (not just functions) — prove eval(YCode(f), u; fuel) = eval(f(YCode(f)), u; fuel − c) and the size bound |YCode(f)| = |f| + O(1). (c) The paper's explicit F(F,M,L,…) self-application (gt-12 Halting section): show it is the s-m-n/Kleene construction spelled out, and that D_{M,L} = Y Ψ_{M,L} is the same object as a description up to the simulation of (3), with matching |D| and TIME bounds; display the term. (d) What Compress must do with the quoted body: Specialize, then traverse; why extensionality is never needed. Note honestly (handoff) that this part is not the hard part.
5. **Cook–Levin for L-descriptions.** Restate prop:standard-succinct-sat's input as an L-term plus fuel: the bounded trace is the sequence of (term, environment, fuel) configurations of the small-step semantics; show the local-consistency structure that makes the trace succinctly checkable (each configuration is determined by a bounded window of the previous one) and hence gives the succinct 3SAT circuit with size poly(log T, |t|); then decoupled 5SAT as in the paper. Give the explicit transition-window lemma with a proof. This is what TB3 implements; say so and cite the TB3 brief as planned work (no claims).
6. Keep the existing §§ on PCP algebra, typed layer, correspondence tables, and the evidence boundary; update cross-references and the correspondence tables to reference the new theorem numbers. Keep the F1 framing softened ("as we read NW19; our reading may be wrong").

## Style
amsthm theorem/lemma/proof; every proof complete at the level of a careful TCS paper (no "clearly"); constants explicit; a physicist should be able to verify each step. Target 28–40 pages total. No praise, no hedged marketing; "we" is fine.
