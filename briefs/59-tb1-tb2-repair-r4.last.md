# Brief 59 report — TB1 + TB2 repair r4 (Fable). Base 0093328; no git; lane respected (src/samplers/{cl,typed}.jl, test/tb1_ld_sampler.jl, test/tb2_answer_reduce.jl, test/mutations/{run.jl + 7 new files}); no claims/ or docs/ edit.

| item | fix | evidence (file:line) / mutant (all KILLED, evidence line matched) |
|---|---|---|
| TB1 N23 MAJOR | `off_line.location == :question` (+ `expected == :point_on_line`) pinned on the off-line fixture; sweep prints `MUTATION_EXPECTED_RULE off_line reached=false hits=0` | `test/tb1_ld_sampler.jl:750`, `:674`; `test/mutations/tb1_off_line.jl` (`(line_point(line,t)==point,t)`→`(false,t)`, target `tb1_decider`) KILLED 23.9 s by `off_line_hits==0`, `accepted`, the CHECKED replay |
| TB1 N24 | row-major pinned on a one-stage `[1 1; 0 1]` map: bytes `35:38 == [1,1,0,1]`, transpose `[1,0,1,1]`, bytes differ only there, both decode to their own `apply` | `tb1:364-379`; `tb1_describe_transpose.jl` (`_field_ints` r/c loops swapped, target `tb1_describe`) KILLED 32.6 s |
| TB1 N25 | DECIDED: **throw**. `_pad_top` refuses a top-level `CLZero` on a proper nonempty sub-register (`ArgumentError`); empty→ambient and full-register rules unchanged, `extra==0` identity; the tb1:105 level-1-on-`{2}` fixture now built by the in-chain `_pad_tail` (same guard exercised) | `src/samplers/typed.jl:36-42`; `tb1:193-199` (`@test_throws` on `pad_level` and `pad_level_evidence`); `tb1_pad_subregister.jl` (target `tb1_levels`) KILLED 26.5 s |
| TB1 N26 | `register_indices(decode_cl(bytes(CLZero(F,5,(2,))))) == (2,)`, full ≠ empty bytes, three registers give three byte strings | `tb1:382-392`; `tb1_describe_zero_register.jl` (NM13 edit, target `tb1_describe`) KILLED 30.5 s |
| TB1 N27 / N28 | N27: my loaded suite hit the gate at 92.7 s (own 4-job runner + critic resident), 49.9 s otherwise — the TB3 brief should carry the budget line; N28: kept as C4c scope (critic's text already says it) | — |
| TB2 NF1 MAJOR | lockstep testset: decider run on ALL 2916 ordered pairs at the zero seed with `_answer_reduce_replay_answer`; per pair `Set(name(e.branch))` == enumerator set (`TB2_GUARD_NAMES` maps axis/diagonal→`*_low_degree`), 2916 accepted, 2736 silent; `MUTATION_EXPECTED_RULE guard_lockstep mismatches=0` | `test/tb2_answer_reduce.jl:567-612`; `tb2_decider_guard_widened.jl` (step 4(b) `== i`→`in (i, 6)`, target `tb2_lockstep`) KILLED 62.0 s with `mismatches=12` |
| TB2 NF2 MAJOR | same testset: five equal-type copy-6 pairs (the critic's four + `(oracle,Point_6)`), entries 1/6/7/22 of the 22-entry right answer corrupted → all 20 rejected by step 1 with `:global_consistency`; `rejected_with_rule=20/20` | `tb2:613-645`; `tb2_global_consistency_first_entry.jl` (`[1:1]` narrowing) KILLED 47.2 s with `rejected_with_rule=5/20`. Replay-case list left at seven (C9's authorized text counts them) |
| TB2 N23–N25 | N23 (wire `pad_level_evidence` into `TypedSampler`), N24 (padding context marker), N25 (rest register / `position` pins): brief 39 (TB5), not this closing round | — |
| TB2 N26 | done: `decode_cl` re-imposes the top stage's `factor ⊎ rest = {1..n}` (one check; inner stages already checked by `_clstep`); a non-spanning description (internal `_clstep`, `{1,2}` of `F^5`) is refused | `src/samplers/cl.jl:683-687`; `tb1:398-405`; `tb1_decode_ambient.jl` (target `tb1_describe`) KILLED 31.8 s |

RUNS. Suite `julia --project=. test/runtests.jl`: exit 0, **907/907** (was 807). QUIET wall (`pgrep -fa runtests|mutations/run` empty, 0 other julia processes, load 1.96): TB0 body **40.557 s** (no warning), suite 3m02, maxrss 1466 MiB. LOADED wall (critic instantiate + 2 julia resident, load 3.8→5.5): TB0 body 49.924 s (warning fired), 4m21, 1442 MiB; one further run under my own 4-job filtered runner + critic FAILED only the TB0 gate (92.7 s) — load artefact, not code. Runner `julia --project=. test/mutations/run.jl` (baseline-first): exit 0, **killed=84/84 baselines ok=43/43 wall=643.02 s** (10m44, load 4–5, critic resident), no SURVIVED/LOAD-ERROR/UNATTRIBUTABLE; filtered pre-runs (`N2`,`N4`,`NF`) also 9/9 KILLED.
Red/green: the five critic survivors (NM11, NM12, NM13, NF1, NF2) are the red witnesses and are now registered and KILLED; the N25 throw and the N26 ambient gate each own one registered mutant (red = check removed). No new target except `tb2_lockstep` (one extra baseline).
CROSS-LANE EDITS: none. MERGE PROPOSALS (orchestrator): **C4c** — apply `verdicts/tb1-r4.md` §5's pre-written row verbatim (N23 landed: `tb1_off_line.jl` KILLED). **C4a** — strike "The bytes are not yet checked to carry the matrix index order … (ibid. N24) — nor a `CLZero`'s register (ibid. N26)." → "The bytes carry the matrix index order (row-major; pinned by the nonsymmetric `[1 1; 0 1]` window `35:38`, `tb1_describe_transpose.jl`) and a `CLZero`'s register (`tb1_describe_zero_register.jl`); `decode_cl` re-imposes the top stage's ambient partition (`tb1_decode_ambient.jl`)."; strike "`pad_level` of a top-level zero map declared on a proper sub-register produces … certificate fails (ibid. N25)." → "`pad_level` refuses (`ArgumentError`) a top-level zero map declared on a proper nonempty sub-register (ibid. N25, throw branch; `tb1_pad_subregister.jl`)."; add the four mutants to the red list. **C9** — strike "They are, however, properties of the guard enumerator … undetected (`verdicts/tb2-r4.md` NF1)." → "and the decider itself, run on all 2916 ordered pairs at the zero seed, fires exactly the enumerated branches pair-for-pair — an empty trace on exactly the 2736 — with honest accept everywhere (`tb2_decider_guard_widened.jl`)"; strike "Step 1's answer comparison is exercised only through one-entry answer types … (`verdicts/tb2-r4.md` NF2)." → "Step 1 rejects a corruption of entries 1, 6, 7 and 22 of the 22-entry bundle with `:global_consistency` on all five equal-type copy-6 pairs (`tb2_global_consistency_first_entry.jl`)"; add both mutants to the red list. **C4b** unchanged (its N23 sentence is still true). DESIGN lockstep (orchestrator): §9.4 add "a top-level zero map declared on a proper nonempty sub-register is refused by `pad_level` (`ArgumentError`) — every originator below is whole-space, so it is a declaration error, not a promotion case (verdicts/tb1-r4.md N25)"; §9.3 byte-format paragraph add "stage matrix entries are serialized row-major, `(1,1),(1,2),…,(w,w)`, pinned by an off-diagonal witness; `decode_cl` re-imposes `factor ⊎ rest = {1..n}` on the top stage".
