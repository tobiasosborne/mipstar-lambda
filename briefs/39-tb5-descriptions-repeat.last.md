# Brief 39 — TB5 report (Fable proposer, 2026-09-05; NO-GIT, tree 9a6f20c + working changes)

**RED.** `test/tb5_repeat.jl` before any `src/descriptions/**`: `Error During Test … MethodError: no method matching describe_cl(::CLStep{GF8}, ::CLStep{GF8}, ::Int64) … Closest candidates are: describe_cl(::AbstractCL{F}) … cl.jl:536` at (a) line 75 (every testset needs the pair adapter).
**GREEN.** 11 testsets, **432 TB5 assertions**: (a) 40 four-query modes on `describe_cl(L_Point/L_ALine/L_DLine,·,8)` — all 8^5 seeds × every j × both players agree with `marginal_k`, every reached prefix (1/9/297 distinct) with `Factor` and every factor basis vector with the stage matrix; `QueryError` for j=0, j>ℓ, wrong player, wrong length, a type on an untyped sampler and an unreachable prefix (G3/G5, incl. the TB2 `ALine_1` PCP map at q=2048); opaque branch ⇒ `NotDescribable`; memo observability 3 entries after one stage-3 marginal (M9-adapter-enumerates owner); (b) 25 size/round-trip/dependency (leaf sizes 130/244/292 bytes; pair bytes = `0xC3 01 q ‖ termA ‖ termB`); (c) 21 replays through the four queries — TB1 exhaustive 8^5 (distinct chains 1/8/288), `S_copy` 2, typed anchor 2 per type, `S^anch` exhaustive 2^9 = 512 (18 chains), `S^rep` rng32(0x5E) (32/32), chain_set_id/distinct/completed printed per view; (d) 165 laws: `direct_sum(P,D)` 8/3/10 with `:(max(ell_1,ell_2))`, `:(s_1(n)+s_2(n))`, bytes = tag ‖ children's term bytes; `product(T_ora,T_pcp)` 2048/3/40, **54/54 product maps describable and agreeing** with the in-memory maps, tensor edge `((oracle,Point_1),(alice,DLine_6))`; `downsize(ALine)` 2/2/15 with 3-fold Factor expansion; `detype` `:(ell_1+2)`, `:(s_1(n)+4*TypeCount)`; zero summand promoted (level-1 and level-3 sums); query purity via `RecordingMachine`; (e) 46 anchor: `Typed(["Game","Anchor"], 4 oriented edges)`, Anchor Factor `[1]` at stage 1 / `QueryError` off `{0}`, Anchor-scoped `enu:cl-space-sum`, `pad_level` witness with `padding_context = :top_level_ambient`, detyped 2/3/9, the (Game,Game) seed questions `101100101`/`001010111`, accept-on-invalid literal; (f) 30 repeat: **B = 9, k = 81, level 3, dimension 729**, `|S^rep| = 85 bytes` at λ = 1 and λ = 2 (k = 324, dim 2916), `k_law == :((lambda*n)^((1+c_prime)*tau))`, integrality ASSUMED PASS, `UniversalConstantBound` NOT_EVALUABLE(owner=tb5-decider-meter), gt-12:L70 finding, c' = 1/2 ⇒ `QueryError`, blockwise replay, 81 metered child calls; (g) 19: **128/128 honest repeated pairs accept** (81 child calls each), `T5-game-seed1`, `T5-anchor-one`, `T5-one-corrupt` (81 calls, one rejecting), `T5-boundary` (9-bit QUESTION component accepted; 10-bit, oversized answer, 80-tuple, trailing bit all rejected with the child-call log EMPTY); (h) 9 independence: identical `S^rep` fnv1a64 `925f2085c0763b30` for `copy_decider`/`trivial_decider`, dependency set exactly `{ecb7eba35b93f04b = hash(S), lambda, tau, c_prime}`; (i) 66: `completeness_decider_time … NOT_EVALUABLE(owner=tb5-decider-meter)`, 7 CITED labels each grepped as `\label{…}` in its range, every CHECKED node has a replay; stage swap 8; (j) tree 55 nodes: CONSTRUCTED 9, CHECKED 27, CITED 10, ASSUMED 4, SOURCE_REPAIR 5.
**Walls.** In-suite (final quiet run): construction **1.35 s** (target 2), 128 + negative transcripts **0.49 s** (target 5), total **1.84 s** (target 7); product/54-map test 2.65 s; TB5 body 22.8 s (the description-layer (a)–(d) tests dominate). Allocation during construction 342 MiB total (GC churn of the 512-seed `S^anch` and 32-seed `S^rep` replays inside the certificate; the 256 MiB target was a peak figure, and the process peak RSS 1.55 GB is the image + TB0). Suite: see the final line below.
**Design.** `src/descriptions/`: `sorts.jl` (records, term grammar Pair/TypedFamily/DirectSum/Repeat/Anchor/Detype/Product/Downsize, bytes `0xC3‖term`, `dependency_walk`), `laws.jl` (QuotedLaw evaluator, `expected_laws` table, `k_rep`/`B_rep`, `LawCert`), `machines.jl` (the universal interpreter: one machine per constructor answering the four queries by forwarding to children; padding/promotion in one place; `_require_image` decides `u ∈ L_{≤r}(V)` at padded stages through queries alone; `DescribedCL <: AbstractCL` so TB1's `cl_kth_replay` runs unchanged), `transformations.jl` (constructors + the mandatory rows LawCert/DescriptionSize/DependencySet/SamplerValidity/MeteredCalls/CITED), `adapter.jl` (`describe_cl(LA,LB,q)`, `describe_typed_cl`), `deciders.jl` (Copy/Trivial/TypedAnchor/Detype/Repeat terms, `0xC4‖term`, `decide`/`decide_traced`, framing). `src/repeat/anchor.jl` (typed anchor decider, detype decider, `detype(s,d)`, `anchor(v)`, lifted honest strategy), `src/repeat/repeat.jl` (`repeat_sampler`, `repeat_decider`, `anchored_repeat`, `StageVerifier`, `ExecutableRepeat <: CompressStage` behind `Repeat(stage, V, λ, τ; params)`).
**Mutations.** 21 TB5 mutants in `test/mutations/tb5_repeat.jl`, all KILLED with passing baselines (7 targets): M5-anchor-zero (`tb5_transcripts`), M5-anchor-answer, M5-detype-level (`tb5_anchor`), M5-shared-seed (`tb5_repeat`), M5-or, M5-no-guard, M5-decider-hash (`tb5_independence`), M-factor-partition (Anchor-scoped, NOTE-A), M9-size-unrolled, M9-adapter-enumerates (`tb5_queries`), M-law, M-dependency, M-tensor, M-downsize, M-boundary, M-detype-parser, M-frame, M-zero-promotion (first SURVIVED on the level-1 sum; the level-3 witness `direct_sum(0, L_DLine)` was added and it is KILLED), M-repeat-level, M-anchor-graph, M-validity. Full registry: see the final line below.
**CROSS-LANE EDITS.** `src/MIPStarLambda.jl`: includes after `traceprint.jl` (6 description files, `compress.jl`, 2 repeat files) and a TB5 export block (additive). `src/compress.jl` (tb4-r1 §7 A/B): `abstract type AbstractStageVerifier` with `StubVerifier <: AbstractStageVerifier` (L221-227); `_VERIFIER_INPUT = Union{Verifier,AbstractStageVerifier,VerifierDescription}` (L246); accessors `_levels/_sampler_time/_decider_time/_description_bound/_sampler_dependencies/_gap` widened to `AbstractStageVerifier` plus `VerifierDescription` methods (L249-265); `_chain`/`_original`/`level_chain`/`runtime_composition_ok`/`_level_chain_ok` over `AbstractStageVerifier` (L271-278, L611-632); `_normal_form_status(::VerifierDescription)` (L330-332); `; params::NamedTuple=(;)` on the three stub stage methods (L512, L538, L578) and `stage_params` on `Compress` (L652-658). `test/runtests.jl`: TB5 include + wall line (L27-29). `test/mutations/run.jl`: include, `_rung` tb5 row, `TB5_MUTANTS` in the queue (L230, L271, L376). `test/mutations/tb4_compress.jl` L13-14, L21-22: the two order mutants' `before`/`after` strings updated for the `; params=stage_params` suffix (same mutation). `_DESCRIPTION_FIELDS[2] = GF2` is registered from `sorts.jl` at load (no edit to `cl.jl`).
**API REQUESTS.** (TB3/TB7) lowering: `SamplerDescription.code :: Quoted{:SamplerMachine}` and `DeciderDescription.code :: Quoted{:TotalPredicate}` are canonical bytes for the fixed Julia interpreters `query`/`decide`, not §1.1 Programs; TB7's `Eval(Apply(Compress, …))` needs `sampler_machine`/`decider_machine` primitives in `PRIMITIVES` (payload = the bytes as a `Vector{Bool}` literal, 8× byte blow-up, or a new `Vector{UInt8}` PrimName) so a description runs under fuel. (TB2, tb2-r5 N23) `TypedSampler` still calls `pad_level`; TB5 carries its own `pad_level_evidence` witness with the padding context instead.
**Deviations (gt line that forced each).** (1) Repeat tuple framing: 32-bit length fields before each component, so `|question|,|answer| ≤ k(n)(B(n)+32)` replaces `k(n)B(n)` — gt-11:L216-L220 fixes no tuple encoding (`SOURCE_REPAIR :RepeatTupleFraming`). (2) Detype `Linear` on an unreachable graph prefix forwards iff the own vertex register is `e_t` and the selected opposite-edge bit is 1, else zero — gt-06:L370-L380 defines `L^w_z` on reachable z only. (3) A typed decider REJECTS an out-of-range type (gt-06:L185-L195 asks only for totality; the typed sampler's `0` at L133-L136 is the sampler's rule). (4) Leaf descriptions are index-independent (one CL pair for every n; `dimension_law` a constant) — def:sampler L572-L601 quantifies over n; every TB5 fixture is. (5) `normal_form` is graded PASS structurally (q = 2, untyped, total decider; gt-05:L624-L635). (6) Anchor requires a child of level ≥ 1 (gt-11:L139-L159 promotes the 0-level Anchor map to the child's level). (7) tb1-r5 N31 at the description level: set-valued registers serialized increasing, positional registers must be declared increasing (else `NotDescribable`) — byte equality decides description equality on the admitted class. (8) `dependency_set` of a leaf is `{:S}` (TB4's convention); composites carry leaf hashes + parameter literals. (9) The u16 register cap binds leaf CL terms only; composites (S^rep) carry no index vectors, so the compact loop is unaffected by it.
**MERGE PROPOSALS (proposals only).** (1) C12 → TESTED: "(Description-level CL closure) For every `SamplerDescription` (leaf adapters `describe_cl(LA,LB,q)`, `describe_typed_cl`, and composites), `DL9-downsize`, `DL9-direct-sum`, `DL9-product`, executable `DL9-detype`, `DL9-anchor` and `DL9-repeat` are definable using only the field/level headers and the four sampler queries (query purity logged by an opaque `RecordingMachine`; children embedded by bytes, so a composite's bytes are a pure function of its children's bytes and its parameters); their field/level/dimension laws are CHECKED as LawCert AST equality against the separately transcribed §9.4 table and evaluated at the tracer index (`direct_sum(L_Point,L_DLine)` at q=8: 8/3/10; `product(T_ora,T_pcp)`: 2048/3/40, 54 types, the 2916-edge tensor graph with `((oracle,Point_1),(alice,DLine_6))`; `downsize(L_ALine)`: 2/2/15; `detype(anchor(V_copy))`: 2/3/9; 81-fold repeat: 2/3/729); `description_size` is the exact byte length of the composite's canonical term (reserialized and decoded; NOT additive under `direct_sum`: 130 + 292 → 426 bytes); dependency sets are the syntax walk replayed against the bytes; metered same-mode child calls per Marginal are exactly r for a direct sum, exactly k(n) for a repetition and at most 1 for detype/anchor; every output sampler, every intermediate included, replays `enu:cl-space-sum`/`enu:cl-map-sum` on a declared chain set with printed chain/replay counts, with the `rk:higher-level` zero-map rule for promoted summands (`direct_sum(0,·)` at levels 1 and 3) and the Anchor type (`SOURCE_REPAIR(zero-map-factor-partition)`); all 54 TB2 product maps are describable and agree with the in-memory maps. Illegal calls return `QueryError`; an opaque host branch is `NotDescribable`. Semantic implications stay CITED (`lem:cl-func-prod`, `lem:cl-concat`, `lem:downsize_sampler`, `lem:detyping-verifiers`). No claim about unselected reachable chains, index-dependent leaves, or lowering into the §1.1 program IR." depends-on C4a,C4b; where-tested `test/tb5_repeat.jl` (a)–(e); mutants M-law, M-dependency, M-tensor, M-downsize, M-boundary, M-zero-promotion, M-validity, M9-adapter-enumerates, M9-size-unrolled, M-factor-partition, M5-detype-level. (2) C13 → TESTED: "(TB5 Repeat fixture) For `V_copy` at n=9, λ=τ=1, c'=1 (an explicit rational toy substitution; integrality ASSUMED PASS, the universal-constant bound `NOT_EVALUABLE(owner=tb5-decider-meter)`), executable anchoring and 81-fold repetition have checked levels/dimensions 1,1 → 3,9 → 3,729 (B = 9, k = 81; `|S^rep| = 85` bytes independent of k — λ = 2 gives k = 324, dimension 2916, the same 85 bytes; `|D^rep| = 63`), accept the classical value-one strategy (Game → its one-bit question, Anchor → 0) on 128 seeded repeated transcripts with exactly 81 child calls each and on `T5-game-seed1` (seed `10111011|1`, question 1, golden answer 1; mapping Game to zero rejects it), reject `T5-anchor-one` (an Anchor-typed answer 1, anchored and repeated), reject `T5-one-corrupt` (one of 81 Game/Game answer components flipped; 81 calls, one rejecting), accept `T5-boundary`'s exactly-9-bit QUESTION component and reject a 10-bit component, an oversized answer component, an 80-tuple and a trailing bit before any child call (DD-26; 32-bit component framing, `SOURCE_REPAIR :RepeatTupleFraming`), and produce `S^rep` bytes identical for the byte-distinct `copy_decider`/`trivial_decider` with dependency set exactly `{hash(S), lambda, tau, c_prime}`. `TIME_D(n) ≤ B(n)` prints NOT_EVALUABLE(owner=tb5-decider-meter); `prop:anchoring`/`thm:repetition` stay CITED; gt-12:L70's `(λn)^τ` is reported inconsistent with gt-11:L200/gt-12:L355 and not used." depends-on C12; where-tested (e)–(j); mutants the seven M5-*, M-factor-partition, M-frame, M-detype-parser, M-repeat-level, M-anchor-graph. (3) DESIGN §9.2: "description_size is the exact byte length of the composite's canonical term; it is not additive under direct_sum". §9.3: the N31 rule of deviation (7); "the k-fold repetition is one `Repeat` term, never k children". §10.2: the framing sentence of deviation (1). (4) Gate (tb1-r5 N33, addendum 3c): keep `elapsed < 60` and add, in `test/runtests.jl`, a calibration kernel timed in-process before TB0 (e.g. the existing `GF(8)` exhaustive triple loop, ~0.1 s on this box at 4.8 GHz) and a second gate `elapsed / calibration < RATIO` with `RATIO` set once from a quiet performance-governor run; read an optional `TB0_BUDGET_SECONDS` only to *lower* the wall budget.
**Unresolved.** The leaf machines' CL values decode from bytes and are cached per description (never hashed); the in-suite TB5 body (22.8 s) is dominated by the exhaustive 8^5 description-level replays of (a)/(c), which a critic may prefer moved to a declared chain set.

## Appendix — `traceprint` of `anchored_repeat(V_copy, 1, 1; c_prime=1, tracer_index=9)` (verbatim, in-suite)
```text
[CONSTRUCTED] Repeat | V^rep = repeat(anchor(V), lambda = 1, tau = 1); c' = 1//1 (toy substitution); at n = 9: B = 9, k = 81; field 2; level ell + 2 = 1 + 2 = 3; dimension k(n)(s(n) + 8) = 729; |S^rep| = 85 bytes, |D^rep| = 63 bytes
  [ASSUMED] normal_form | V is an ell-level normal form verifier: sampler over F_2, untyped, level 1, decider a total five-input predicate (structural check of gt-05:625-635; the value/PCC content is not decided here) => PASS (gt-05-games-normalform.tex:L625-L635 (normal form verifier))
  [ASSUMED] completeness_decider_time | (completeness only) TIME_D(n) <= (lambda*n)^tau: TIME_D = TIME_D(n): 1, not metered in the source's unit; NOT_EVALUABLE(owner=tb5-decider-meter) (DD-31) (opaque) => NOT_EVALUABLE (gt-11-parallel-repetition.tex:L240-L243 (enu:pr-completeness))
  [CHECKED] HypothesisAudit | thm:repetition ASSUME clauses re-evaluated on the attached input: normal_form=PASS, completeness_decider_time=NOT_EVALUABLE
  [CITED] thm:repetition | gt-11-parallel-repetition.tex:L229-L258 (thm:repetition): V^rep is (ell + 2)-level with k(n) = (lambda n)^((1 + c') tau); TIME_S = O(k(n) TIME_S(n)); TIME_D = O(k(n) max(TIME_D(n), (lambda n)^tau)); S^rep depends only on S, lambda, tau; completeness (PCC), soundness Ent(V^rep_n, p) >= Ent(V_n, 1 - eps)
  [ASSUMED] UniversalConstantBound | c' is the universal constant with TIME_{D^anch}(n) <= c' (TIME_D(n))^c' for all n >= 1 (gt-11:200); neither TIME is metered in the source's unit, so the predicate is NOT_EVALUABLE(owner=tb5-decider-meter); tests substitute c' = 1 and never round the exponent
  [SOURCE_REPAIR] RepetitionCountInconsistency | gt-12-compression.tex:L70 prints k(n) = (lambda*n)^tau inside ComputeParrepVerifier, conflicting with gt-11-parallel-repetition.tex:L200 and gt-12-compression.tex:L355, which give (lambda*n)^((1+c')*tau); the executable retains the latter two and does not use L70
  [SOURCE_REPAIR] RepeatTupleFraming | the source parses x, y, a, b as k(n)-tuples (gt-11:216-220) without fixing the tuple encoding; the executable frames every component with a 32-bit length field checked against B(n) before its payload is read, so |question|, |answer| <= k(n) * (B(n) + 32) replace the source's k(n) * B(n)
  [CHECKED] SamplerIndependence | S^rep depends on {hash(S) = ecb7eba35b93f04b, lambda, tau, c'} and never on hash(D) = 0ab16f07b70a4056 (gt-11:257-258); dependency set = {c_prime, ecb7eba35b93f04b, lambda, tau}
  [CONSTRUCTED] DL9-repeat | k(n)-fold direct sum of the anchored sampler as one compact loop term (never unrolled); k(n) = (lambda * n) ^ ((1 + c_prime) * tau) with lambda = 1, tau = 1, c' = 1//1; field 2; level 3; dimension at n = 9: 729; typing untyped; |S| = 85 bytes; fnv1a64 = 925f2085c0763b30
    [CHECKED] LawCert | expected == actual: field = 2, level = ell_1, dimension = k(n) * s_1(n), query time = k(n) * C_1(n); at n = 9: field 2, level 3, dimension 729 (AST equality is a transcription check, DESIGN 9.2)
    [CHECKED] DescriptionSize | description_size = length(canonical_bytes(code)) = 85 bytes, reserialized and decoded round trip; fnv1a64 = 925f2085c0763b30
    [CHECKED] DependencySet | syntax walk over the embedded leaf descriptions and parameter literals = {c_prime, ecb7eba35b93f04b, lambda, tau}
    [CHECKED] SamplerValidity | enu:cl-space-sum and enu:cl-map-sum (lem:cl-kth, gt-04-cl.tex:151-180) replayed through Dimension/Marginal/Factor/Linear on chain set tb5-rng32(0x5a)@n=9: alice: chains 32/replays 32; bob: chains 32/replays 32
    [CHECKED] MeteredCalls | child calls per query at n = 9 over 32 seeds: Marginal max 81, Factor 81, Linear 81; law O(k(n) C_S(n)), k(9) = 81 => exactly 81 same-mode child calls per Marginal
    [ASSUMED] KRepIntegrality | the source calls k(n) an integer while declaring only c' > 0 (gt-11:200, 237); the stored term is (lambda * n) ^ ((1 + c_prime) * tau) with c' = 1//1 an explicit toy substitution; k(9) = (1*9)^((1 + 1//1)*1) = 81 => PASS
    [CITED] lem:cl-func-prod | gt-04-cl.tex:L315-L327 (lem:cl-func-prod): the direct sum of CL functions on complementary registers is CL of level max_j ell_j
    [CITED] lem:cl-kth | gt-04-cl.tex:L151-L180 (lem:cl-kth): the marginal decomposition, factor partition and telescoping identity characterizing ell-level CL functions; the general statement is CITED, its two obligations are replayed finitely
  [CONSTRUCTED] RepeatDeciderDescription | B(n) = (lambda * n) ^ tau computed first; x, y, a, b each parsed as exactly k(n) framed components streaming at most B(n) payload bits per component, rejecting before any child call on failure; then exactly k(n) calls of D^anch combined by AND (DD-26); typing untyped (n, x, y, a, b); TIME = O(k(n) * max(TIME_1(n), B(n))); |question| <= k(n) * (B(n) + 32); |answer| <= k(n) * (B(n) + 32); |D| = 63 bytes
    [CHECKED] RepeatDecider | B(n) = (lambda * n) ^ tau computed first; x, y, a, b each parsed as exactly k(n) framed components streaming at most B(n) payload bits per component, rejecting before any child call on failure; then exactly k(n) calls of D^anch combined by AND (DD-26)
    [CHECKED] DescriptionSize | |D| = 63 bytes, reserialized; fnv1a64 = 2301b2b97039d361
    [CHECKED] DependencySet | syntax walk = {0ab16f07b70a4056, c_prime, lambda, tau}
  [CONSTRUCTED] Anchor | V^anch = detype(tS^anch, tD^anch): field 2; level ell + 2 = 1 + 2 = 3; dimension s(n) + 8 = 9 at n = 9; S^anch depends on {ecb7eba35b93f04b} (gt-11:134-135)
    [CITED] prop:anchoring | gt-11-parallel-repetition.tex:L112-L136 (prop:anchoring): V^anch is a normal form verifier: PCC completeness transfers, Ent(V^anch_n, 1 - eps) >= Ent(V_n, 1 - 4 * 16^2 * eps), TIME poly, levels ell + 2 and dimension s(n) + 8, S^anch depends only on S
    [CONSTRUCTED] Detype | detype(S, D) over 2 types: level 1 + 2 = 3; dimension s(n) + 4|Type| = 9 at n = 9; questions add 8 graph bits; answers unchanged
      [CITED] lem:detyping-verifiers | gt-06-types.tex:L444-L475 (lem:detyping-verifiers): completeness, soundness with 16^|Type| epsilon, the Ent map and the polynomial time bounds of detyping; the sampler/decider construction, +2 levels and 4|Type| registers are executed
      [CONSTRUCTED] DL9-detype | detype: V_G = V_vA (+) V_eA (+) V_vB (+) V_eB (4 x 2 bits) then the typed child for the revealed type (gt-06-types.tex:359-404); field 2; level 3; dimension at n = 9: 9; typing untyped; |S| = 68 bytes; fnv1a64 = 777c1fdcd0ccb060
        [CHECKED] LawCert | expected == actual: field = 2, level = ell_1 + 2, dimension = s_1(n) + 4TypeCount, query time = poly(TypeCount, C_1(n)); at n = 9: field 2, level 3, dimension 9 (AST equality is a transcription check, DESIGN 9.2)
        [CHECKED] DescriptionSize | description_size = length(canonical_bytes(code)) = 68 bytes, reserialized and decoded round trip; fnv1a64 = 777c1fdcd0ccb060
        [CHECKED] DependencySet | syntax walk over the embedded leaf descriptions and parameter literals = {ecb7eba35b93f04b}
        [CHECKED] SamplerValidity | enu:cl-space-sum and enu:cl-map-sum (lem:cl-kth, gt-04-cl.tex:151-180) replayed through Dimension/Marginal/Factor/Linear on chain set tb5-exhaustive-2^9@n=9: alice: chains 18/replays 512; bob: chains 18/replays 512
        [CHECKED] MeteredCalls | child calls per query at n = 9 over 512 seeds: Marginal max 1, Factor 0, Linear 0; law poly(TypeCount, C_S(n)); at most one child call per query => at most 1 same-mode child calls per Marginal
        [CITED] lem:detyping-verifiers | gt-06-types.tex:L444-L475 (lem:detyping-verifiers): completeness, soundness with 16^|Type| epsilon, the Ent map and the polynomial time bounds of detyping; the sampler/decider construction, +2 levels and 4|Type| registers are executed
        [CITED] lem:cl-concat | gt-04-cl.tex:L282-L292 (lem:cl-concat): the concatenation of a k-level CL function with an ell-level family is (k + ell)-level
        [CITED] lem:cl-kth | gt-04-cl.tex:L151-L180 (lem:cl-kth): the marginal decomposition, factor partition and telescoping identity characterizing ell-level CL functions; the general statement is CITED, its two obligations are replayed finitely
        [CONSTRUCTED] DL9-anchor | typed anchor family {Game, Anchor} with the complete graph and both self-loops; Game = S, Anchor = 0 on F_2^1; field 2; level 1; dimension at n = 9: 1; typing typed(2 types, 4 oriented edges); |S| = 67 bytes; fnv1a64 = 062a68c7819610da
          [CHECKED] LawCert | expected == actual: field = 2, level = ell_1, dimension = s_1(n), query time = poly(C_1(n)); at n = 9: field 2, level 1, dimension 1 (AST equality is a transcription check, DESIGN 9.2)
          [CHECKED] DescriptionSize | description_size = length(canonical_bytes(code)) = 67 bytes, reserialized and decoded round trip; fnv1a64 = 062a68c7819610da
          [CHECKED] DependencySet | syntax walk over the embedded leaf descriptions and parameter literals = {ecb7eba35b93f04b}
          [CHECKED] SamplerValidity | enu:cl-space-sum and enu:cl-map-sum (lem:cl-kth, gt-04-cl.tex:151-180) replayed through Dimension/Marginal/Factor/Linear on chain set tb5-exhaustive-2^1@n=9: alice/Game: chains 1/replays 2; alice/Anchor: chains 1/replays 2; bob/Game: chains 1/replays 2; bob/Anchor: chains 1/replays 2
            [SOURCE_REPAIR] zero_map_factor_partition | pad_level(CLZero on register R, ell): stage 1 reports the all-ones indicator of R with the zero linear map; stages 2..ell report empty factors (rk:higher-level, gt-04-cl.tex:122-130; DESIGN 9.4)
          [CHECKED] MeteredCalls | child calls per query at n = 9 over 2 seeds: Marginal max 1, Factor 1, Linear 1; law poly(C_S(n)); at most one child call per query => at most 1 same-mode child calls per Marginal
          [SOURCE_REPAIR] AnchorFactorReport | gt-11-parallel-repetition.tex:L96 prints the all-zero factor for Anchor at every stage; the executable reports V_1 = V (rk:higher-level, gt-04-cl.tex:122-130) so enu:cl-space-sum holds: SOURCE_REPAIR(zero-map-factor-partition)
          [CHECKED] pad_level | Anchor = pad_level(CLZero(F_2, 1), 1) in the top-level ambient context: stage 1 reports the all-ones indicator, stages 2..1 empty
            [SOURCE_REPAIR] zero_map_factor_partition | pad_level(CLZero on register R, ell): stage 1 reports the all-ones indicator of R with the zero linear map; stages 2..ell report empty factors (rk:higher-level, gt-04-cl.tex:122-130; DESIGN 9.4)
          [CITED] def:typed-sampler | gt-06-types.tex:L95-L140 (def:typed-sampler): the seven-input typed sampler machine; the type is the seventh input, not a fifth operation
          [CITED] lem:cl-kth | gt-04-cl.tex:L151-L180 (lem:cl-kth): the marginal decomposition, factor partition and telescoping identity characterizing ell-level CL functions; the general statement is CITED, its two obligations are replayed finitely
      [CONSTRUCTED] DetypeDeciderDescription | parse V_G = first 4 x 2 bits of x and y; accept on parse failure or a non-edge view; on the oriented edge (l, r) call D(n, l, x'', r, y'', a, b) once; typing untyped (n, x, y, a, b); TIME = 1 + TIME_1(n); |question| <= Q_1(n) + 4TypeCount; |answer| <= A_1(n); |D| = 46 bytes
        [CHECKED] DetypeDecider | parse V_G = first 4 x 2 bits of x and y; accept on parse failure or a non-edge view; on the oriented edge (l, r) call D(n, l, x'', r, y'', a, b) once
        [CHECKED] DescriptionSize | |D| = 46 bytes, reserialized; fnv1a64 = d47c8c27b533e71e
        [CHECKED] DependencySet | syntax walk = {0ab16f07b70a4056}
        [CONSTRUCTED] TypedAnchorDeciderDescription | Game/Game -> D(n, x, y, a, b); any Anchor-typed player must answer exactly [0]; Game answers ignored on such a pair; out-of-range types reject; typing typed (n, tA, x, tB, y, a, b); TIME = 1 + TIME_1(n); |question| <= Q_1(n); |answer| <= max(A_1(n), 1); |D| = 3 bytes
          [CHECKED] TypedAnchorDecider | Game/Game -> D(n, x, y, a, b); any Anchor-typed player must answer exactly [0]; Game answers ignored on such a pair; out-of-range types reject
          [CHECKED] DescriptionSize | |D| = 3 bytes, reserialized; fnv1a64 = 35b9351c06658d83
          [CHECKED] DependencySet | syntax walk = {0ab16f07b70a4056}
```
**Final runs (quiet, performance governor, no other Julia).** `julia --project=. test/runtests.jl`: **1796/1796, exit 0**, wall 95.3 s, TB0 body 15.66 s (gate 60), TB4 4.84 s, TB5 22.77 s (construction 1.348 s, transcripts 0.493 s); `uptime` before/after: `12:41:25 up 3:53, load average: 2,35, 3,54, 3,12` / `12:43:01 up 3:55, load average: 1,49, 2,94, 2,94` (the 2.35 is the tail of the registry that had just finished). `julia --project=. test/mutations/run.jl` (MUTATION_JOBS=4): **`MUTATION REGISTRY: killed=140/140 baselines ok=62/62 wall=595.4 s`, exit 0** (119 prior + 21 TB5; load 1.38 → 2.74). Earlier in the round, with two critic runners possible, the suite measured TB0 16.57 s.
