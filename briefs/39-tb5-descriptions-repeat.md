# Brief 39 — TB5 implementation (red/green TDD): the sampler/decider DESCRIPTION layer (DESIGN §9) and executable Repeat = anchoring + parallel repetition (DESIGN §10)

Proposer (Fable). Autonomous; no questions; **no git** (orchestrator commits). Lane: `src/descriptions/**` (new: sorts, adapter, laws, transformations), `src/repeat/**` (new: anchor, repeat), `test/tb5_repeat.jl` (new), `test/mutations/tb5_*.jl` (new), MINIMAL additive edits to `src/MIPStarLambda.jl`, `test/runtests.jl`, `test/mutations/run.jl`. Do NOT modify TB0–TB4 files except additively (list every such line under CROSS-LANE EDITS). Report: `briefs/39-tb5-descriptions-repeat.last.md` (≤ 40 lines).

Scratch: `/tmp/claude-1000/-home-tobias-Projects-discussions/548f633b-c857-457a-bb33-0aa1a7879403/scratchpad/tb5/`

## Why this rung matters
From here on every transformation receives CODE, never a host closure: `SamplerDescription` / `DeciderDescription` (§9.1) are the ONLY boundary between rungs, and Introspect (TB6), AnswerReduce (TB7 re-wiring of TB2) and Compress (TB7) will all be written against this API. Build it as the general paper construction, parameterized by the description, with the fixture only in tests. Elegance counts: the code should read like §9–10.

## Read order
1. `CLAUDE.md`; `~/.claude/skills/rk-light/SKILL.md` (laws 1, 4, 5); `claims/CLAIMS.md` rows C12, C13 (the CONJECTURE rows this rung targets — you may NOT change any status; MERGE PROPOSALS only).
2. `docs/DESIGN.md` §9 in full (9.1 sorts and the four query modes; 9.2 LawCert, def:sampler replay with `prefix_i`, description_size = canonical bytes, GapMap; 9.3 `describe_cl` adapter from the lazy `CLStep` with `QuotedBranch`; 9.4 query-only closure lemmas DL9-downsize/direct-sum/product; 9.5 executable detyping; 9.6 transformation contracts incl. the mandatory output-sampler replay row), §10 in full (anchoring, repetition, the TB5 instance, T5-* transcripts, M5-* mutations, DD-26), §13.1 TB5 row, §1.1 (Quoted, canonical serialization — reuse TB3's program IR if it has landed; if not, define the minimal canonical-bytes quoting for descriptions in `src/descriptions/` and list it as an API REQUEST to TB3). `docs/definitions.md` §G–H (SOURCE_REPAIR(zero-map-factor-partition), anchored_repeat row, DD-31 guard rule).
3. `verdicts/design-v2-r3.md` §5 (TB5 readiness notes A–D — binding here) and §1 m12–m15.
4. Ground truth (recompute from it, never from memory): `ground-truth/gt-04-cl.tex` L151–L180 (lem:cl-kth), L315–L327 (direct sum / product levels), L572–L601 (def:sampler), L614–L626 (def:sampler-sample); `gt-06-types.tex` L95–L151 (typed sampler, typed sample), L185–L195 (typed decider); `gt-11-parallel-repetition.tex` L89–L103, L112–L136 (anchoring, prop:anchoring), L200–L220, L229–L258 (k(n), the four-tuple length guard, thm:repetition), L268–L292; `gt-12-compression.tex` L70, L355 (the k(n) exponent inconsistency — report, do not use L70).
5. Existing code: `src/samplers/cl.jl` (lazy `CLStep` after brief 38), `src/samplers/typed.jl`, the certificate/trace printer used by TB0–TB2, TB3's `src/ir/programs.jl` if present.

## Process
1. **Tests FIRST** (`test/tb5_repeat.jl`, RED; paste an excerpt): (a) the four `SamplerQuery` modes on `describe_cl` of TB1's `L_Point/L_ALine/L_DLine` at (8,2,1): every legal j, seed, prefix and factor basis vector agrees with `marginal_k`/`apply`/stored matrix; an opaque host branch returns `NotDescribable`; illegal calls return `QueryError`, never throw; (b) `description_size` equals the reserialized canonical byte length and the dependency set is the syntax-walk result; (c) the §9.2 replay (`enu:cl-space-sum`, `enu:cl-map-sum`) on every exhaustive fixture, with per-sampler `chain_set_id`, distinct-chain and completed-replay counts printed (NOTE-B); (d) DL9-downsize / direct_sum / product / detype: level, field, dimension laws as `LawCert` AST equality against hand-transcribed expected ASTs, plus the output-sampler replay row (§9.6) for each; (e) `typed_anchor_sampler`/`typed_anchor_decider` on `V_copy`: type set {Game,Anchor}, complete graph with self-loops, Anchor's Factor = all-ones at stage 1 and empty thereafter (`SOURCE_REPAIR(zero-map-factor-partition)` node printed), an Anchor-scoped `enu:cl-space-sum` assertion (NOTE-A); detyped `(S^anch,D^anch)`: field 2, level 3, dimension 9; (f) `repeat_sampler`/`repeat_decider` at `lambda=tau=c_prime=1, n=9`: `B=9, k=81`, level 3, dimension 729, the compact loop is NOT unrolled in the description (assert description_size independent of k), `k(n)` stored as the exact source term `(lambda*n)^((1+c_prime)*tau)` with integrality as an ASSUMED witness and the universal-bound predicate NOT_EVALUABLE; the gt-12 L70 inconsistency reported; (g) 128 seeded honest repeated question pairs all accept; the four named transcripts `T5-game-seed1`, `T5-anchor-one`, `T5-one-corrupt`, `T5-boundary` (the exactly-9-bit component is a QUESTION component, NOTE-D; the 10-bit case must reject with the child-call log EMPTY — DD-26); (h) sampler independence: two verifiers with byte-distinct deciders give identical canonical `S^rep` hashes; dependency set exactly `{hash(S),lambda,tau,c_prime}`; (i) `TIME_D(n) <= B(n)` printed `NOT_EVALUABLE(owner=…)` per DD-31 (NOTE-C) and PCC completeness / Ent relations CITED with existing gt labels (test greps the label); (j) the full certificate tree printed (CHECKED / CONSTRUCTED / CITED / SOURCE_REPAIR / NOT_EVALUABLE leaves, one line each).
2. Implement; GREEN; paste summary + walls (construction < 2 s, transcripts < 5 s, total < 7 s, peak < 256 MiB — measured values replace targets).
3. **Mutations** (copies, registered in `run.jl`, each shown KILLED): the seven `M5-*` of §10.3 with their named killers, plus the Anchor-scoped `M-factor-partition` (NOTE-A), plus `M9-size-unrolled` (description_size charged k times → (f) fails) and `M9-adapter-enumerates` (Marginal walks all branches → the adapter timing/observability test fails).
4. Whole suite under the TB0 gate warm; the mutation runner exit 0 with every rung's mutants listed.

## Report (≤ 40 lines)
RED excerpt; GREEN summary + walls + peak allocation; the certificate tree printout for `Repeat(V_copy)`; mutation lines; CROSS-LANE EDITS; API REQUESTS; deviations from §9–10 (each with the gt line that forced it); MERGE PROPOSALS: verbatim row text for C12 and C13 with the evidence this rung supplies (proposals only; statuses change only by verdict).

## ADDENDUM (2026-09-05, from verdicts/tb1-r3.md §readiness and verdicts/tb2-r3.md G1–G5)
The four-query API (`Dimension/Marginal/Factor/Linear`) and `cl_kth_replay` work unchanged on all 54 product maps — build on them. What TB5 must ADD before anchoring/repetition can be described:
- **G1 (blocking):** `direct_sum`/`concatenate` (`src/samplers/cl.jl`) wrap host closures → every composite is `NotDescribable`. `DL9-direct-sum`/`DL9-concatenate`/`DL9-product` must be `CLDescription`-level combinators (quoted branch constructors for the composite, e.g. `BranchDirectSum(left_desc,right_desc)`), so `typed_anchor_sampler`/`repeat_sampler` outputs are describable with exact `description_size`; the 54 product maps become describable as a by-product (assert 54/54).
- **G2:** the `SamplerDescription` record (`code, field_size, level, typing, query_time, description_size, dependency_set`), the pair/typed adapter `describe_cl(LA,LB,q)` with the §9.1 arities `Marginal(n,w,j,z[,type])` etc., and `DeciderDescription`.
- **G3:** the zero marginal stays internal; `Marginal` with `j=0` is a `QueryError` at the description boundary (brief 54 makes the in-memory call throw).
- **G5:** `ArgumentError → QueryError` mapping at the boundary; a TB2-fixture test of `Factor` rejecting an unreachable PCP prefix.
- After brief 54 the serializer is injective with a `decode` round trip — extend the round trip to every description TB5 emits (`S^anch`, `S^rep`, intermediates).
