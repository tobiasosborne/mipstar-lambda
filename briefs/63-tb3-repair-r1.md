# Brief 63 — TB3 repair r1 (work order = verdicts/tb3-r1.md: N1, N2, N3 MAJOR; N4–N10 MINOR; DESIGN adjudications) **plus** the TB4 prerequisites the critic named (§8 forward look, gaps 1–7)

Proposer (Fable). Autonomous; no questions; **no git**. Lane: `src/ir/programs.jl`, `src/frontend/**`, `src/verifiers/pcp.jl` (ONLY the upstream-evidence slot for N2), `test/tb3_frontend.jl`, `test/mutations/tb3_*.jl`, `test/mutations/run.jl` (additively), `docs/DESIGN.md` §1.1/§1.2/§5.5 and `docs/definitions.md` §F EXACTLY as the verdict's §"DESIGN" adjudication admits (ADMIT items with their conditions; nothing REFUSED). Report: `briefs/63-tb3-repair-r1.last.md` (≤ 30 lines).

Scratch: `/tmp/claude-1000/-home-tobias-Projects-discussions/548f633b-c857-457a-bb33-0aa1a7879403/scratchpad/tb3-r1/`

Read `verdicts/tb3-r1.md` IN FULL (binding), `briefs/23-tb3.md` + addenda, `briefs/23-tb3.last.md`, `docs/analytic/parts/part2a.tex` §8 (C16's charge table and codec), `claims/CLAIMS.md` C16/C18/C19 (the orchestrator applied the C19 rewrite and the option-(b) instantiation clause on C16/C18 — your code must match that clause, or shrink it honestly if you align a constant).
- **N1**: red-capable trace-row contents (the equality fixture's rows pinned: state/head/tape/fuel per row; the critic's C-N2a/C-N2b mutants registered and KILLED).
- **N2**: the front-end certificate is bound to the proof it is attached to: add a first-class upstream-evidence slot in `build_pcp` (API request 1) so the graft under `:Tseitin` disappears; a PCP built from a different program (`λnxyab.not(false)`, |D|=51) carrying the trivial program's certificate must fail at a TB3 node with `:certificate_binding`, never print the wrong `|D|`/hash; mutant registered and KILLED. This must land before TB4 (gap 7).
- **N3**: option (b) is chosen (CLAIMS already says so): the analytic §8 stays the mathematics; DESIGN §1.1 gets the explicit instantiation clause; fix the `8.5` cite. If aligning `c_Y` to 3 in `Fix` is a one-line change, do it and shrink the clause accordingly (say which).
- **N4–N10**: as adjudicated (the `:CookLevinGeneral` display names (1)–(3) and `:RawAnswerBlocks` names enc_Γ; the per-index gadgets omitted get an ASSUMED leaf; `Aborted` documented as a host guard; `CompilationRefused` gets a driving test before its DESIGN sentence; the (g)-never-reaches-`build_pcp` fact stated; C-N1/C-N3 registered).
- **DESIGN §5.5**: the replacement with "dead" padding gates named and the "medium confidence … six-gate fixture" clause retracted; §1.2: split the trace invariant, add the `general Cook--Levin locality | ASSUMED` row.
- **TB4 prerequisites (gaps 1–6)**: `YCode` constructor + export + test (`D = YCode(Ψ)` representable; `Fix` charge documented); register `halts_within` and `quoted_pair` primitives with charges so `Ψ_{M,λ}` EVALUATES (not syntax-only) on the two-state machine; sort checking for `Quoted{A}` at construction (`Quoted{Decider}`, `MachineDesc`, `Sampler`, `Compressor`) with a `SortError` red test; `FuelBound` reconciled across DESIGN §1.1 / definitions §F / code (pick `FuelBound(P{Nat},P{Nat})`, fix the other two, and make `_fuel_value` return `OutOfFuel`-style refusal rather than `SortError` when `λ·log₂n` overflows — say so in DESIGN); `specialize` of `Fix(Ψ)` documented as specialize-the-body-then-`Fix` (or made to work) with a contract note; a minimal `Verifier[...]` carrier reachable from `Quoted` (the bridge TB4's `StubVerifier` needs — keep it to the record type + sort check; TB4 fills it). Gap 8 (vector-backed `Circuit`) only if cheap.
Rules: red/green with the critic's survivors as red witnesses; every new assertion has a registered mutant KILLED under the baseline-first runner; suite under the TB0 gate quiet (note: 907 assertions now; TB0 body ~40 s quiet — do not add to TB0's body); cold build reported via `tools/cold_precompile.sh`. Report: response table N1–N10 + DESIGN items + gaps 1–8 with file:line; runs; CROSS-LANE EDITS (the `build_pcp` slot); MERGE PROPOSALS: C10 (the critic's pre-written row once N1/N2/N5 are discharged — verbatim), C16/C18 clause adjustments if any constant was aligned.
