# Brief 06 — REPAIR round r1 for docs/DESIGN.md + docs/definitions.md (work order = verdicts/design-r1.md)

You are the proposer (gpt-5.6-sol, xhigh). Work fully autonomously; do not ask questions. Lane: `docs/DESIGN.md`, `docs/definitions.md`, and the new `docs/design-repair-r1-response.md` ONLY. Do NOT edit claims/CLAIMS.md (put proposed row text under a "MERGE PROPOSALS" heading in the response file). Do not run git.

Read: `~/.claude/skills/rk-light/SKILL.md` (law 5: downgrade over ambition; deletion where offered); `CLAUDE.md`; `briefs/01-design.md`; `verdicts/design-r1.md` IN FULL (13 MAJOR M1–M13, 11 MINOR m14–m24, NOTEs, handoff coverage table); `docs/findings.md` (F1, F2; C8 in claims/CLAIMS.md); `docs/findings-F1-check.jl`; ground truth as needed (`ground-truth/gt-*.tex`, `ground-truth/nw19/nw19-tseitin-arith.tex`).

Address EVERY objection (M1–M13, m14–m24, and every NOTE). One row each in `docs/design-repair-r1-response.md`: id → FIXED / RETRACTED / DOWNGRADED / RESIDUE, with the exact DESIGN.md section/DD edited. Apply the critic's fix demands as adjudicated; do not paraphrase upward.

Orchestrator directives (binding, in addition to the fix demands):
1. **Real Tseitin at TB0.** Replace the `(∏ x_i o_i)^2` fixture entirely. TB0's fixture is a REAL circuit C with 5 one-bit index inputs + 5 sign bits and exactly s=6 gates (fan-in ≤2, AND/OR/NOT; NOT may be folded into gates if you prefer, say so), Tseitin per NW19 (`z_i = (g_i ∧ w_i) ∨ (¬g_i ∧ ¬w_i)`, conjoined, PLUS the output literal w_out — finding F2), arithmetized along the formula tree. m' = 5·1+5+6 = 16. Choose C so that (a) its clause relation φ_C has at least 3 present clauses and at least one absent one, (b) a satisfying 5-block witness exists (brute force over 2^10), (c) some gate has fan-out 2 so that C8's degree phenomenon is exercised. State the resulting per-variable occurrence vector, hence the structural individual-degree bound of F_arith (= occurrences, per F1), the expected inddeg(c₀), and the required d. Give the expected monomial count of c₀ (estimate, then say it must be measured) and the MonomialBudget.
2. **Field regime for TB0.** Per M1, q=2 is vacuous. Use `q = 2^k` with k odd, m | q, and — because m' = 16 — q = 2^11 (paper policy: all six def:pcpparams obligations, list them, and recompute k_min honestly given the NEW degree bound from directive 1, i.e. with (deg_F + 5d)m'/q < 1/2 where deg_F is the structural bound; if the paper's own (2+5k) inequality is what def:pcpparams literally says, record both and flag the discrepancy as a SOURCE_REPAIR candidate tied to C8). Exhaustive verification is over a SMALL field that is NOT vacuous: use q = 8 (k=3, odd) for exhaustive-on-a-subcube checks only where you can name what is exhausted, and sampled z for q = 2^11. No `d ≥ q` rows.
3. **C8 becomes a TB0 test**: compute the actual individual degree vector of F_arith for the fixture and for the 2-gate example in `docs/findings-F1-check.jl`; assert deg_{w1} = 4 there; assert structural (occurrence) bound ≥ actual everywhere and equality on the named variables.
4. **Elegance (m24).** Adopt the critic's three cuts unless a cut would remove a red-capable check; where you keep machinery, justify in one line. Target: the certificate algebra ≤ half the size of the polynomial+sampler algebra it certifies.
5. **fig:decider-pcp full specification (M13)** and **detype (M11)**: transcribe table:tpcp and the five checks with their exact type-pair guards from `gt-10-answer-reduction.tex` (fig:decider-pcp), both ldparams tuples, and the i ∈ {3,4,5} restriction; define `detype` as a CITED transformation with lem:detyping-verifiers' level/soundness cost.
6. Keep every ground-truth citation that the critic verified (all 78 resolved). Do not renumber DDs; add new ones as DD-17….

Run nothing except sanity greps. Final message ≤ 15 lines: disposition counts (FIXED/RETRACTED/DOWNGRADED/RESIDUE) and the list of RESIDUEs with one-line reasons.
