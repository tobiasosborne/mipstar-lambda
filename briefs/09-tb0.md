# Brief 09 — TB0 implementation (red/green TDD): field, low-degree encoding, zero-basis certificate, Tseitin, PCP core

You are the proposer (gpt-5.6-sol, xhigh). Work fully autonomously; do not ask questions. Do not run git.
Lane: `Project.toml`, `src/**`, `test/**` (including `test/mutations/**`), and your report `briefs/09-tb0.last.md`. Do NOT edit `docs/`, `claims/`, `toys/`, `verdicts/`. Merge proposals for docs/claims go in a "MERGE PROPOSALS" section of your final report.

## Read order
1. `CLAUDE.md`; `~/.claude/skills/rk-light/SKILL.md` law 4 (red-capable checkers).
2. `docs/DESIGN.md` — the single source. Implement §1.2 (circuit/formula/Tseitin/arith), §1.3 (Poly with structural + actual degree accounts), §1.4 (zero-basis rewrite certificate), §3 (certificate/trace — minimal, as repaired after m24), §5 header + §5.1 (TB0 fixture, both field rows, tests 1–6, mutations A–E), §6 (layout and test discipline). `docs/definitions.md` for every name. `docs/findings.md` F1/F2 and `docs/findings-F1-check.jl` (C8 test).
3. Ground truth (cite in code comments by label): `ground-truth/gt-03-prelim.tex` sec:ld-encoding; `ground-truth/gt-10-answer-reduction.tex` prop:zero-basis (proof), def:pcp-proof, def:pcp-eval, fig:pcpverifier steps 4–5, def:pcpparams; `ground-truth/nw19/nw19-tseitin-arith.tex` (Tseitin gadget, arithmetization steps (i),(ii)).

## Process (mandatory order; paste real outputs in the report)
1. Create `Project.toml` (name MIPStarLambda, stdlib deps only: Test, Random, SHA if needed) and `test/runtests.jl` including `test/tb0_core.jl`.
2. Write `test/tb0_core.jl` FIRST, covering DESIGN §5.1 items 1–6 exactly (field axioms exhaustive in GF(8) + 10,000 seeded in GF(2^11); multilinear extension for m=1 and m=2 vs a·ind_m(x); zero-basis rewrite of f = x³ − x² + xy² − xy over GF(8) with replay of every rewrite step and coefficient identity; exhaustive circuit/Tseitin/output-literal/arith agreement on all 2^16 Boolean (x,o,w) and the 128/896 clause counts and 512 witnesses; the PCP proof Π and `pcpverifier` on the 16 coordinate lines through b_ρ over GF(8) and ≥10,000 seeded z over GF(2^11); degree/dependency report equalities incl. the two displayed vectors; C8: deg_w1 = 4 on the two-gate circuit). Run it: it must be RED (missing module). Paste the first lines of the red output.
3. Implement the minimum in `src/` (suggested: `src/MIPStarLambda.jl`, `src/fields/gf2k.jl`, `src/polynomials/sparse.jl`, `src/polynomials/zero_basis.jl`, `src/ir/circuits.jl`, `src/verifiers/pcp.jl`, `src/certificates.jl`, `src/traceprint.jl`; do not create files for later rungs). Run GREEN. Paste the Test summary lines and the printed transformation trace (DESIGN §3 format) and the measured normalized monomial count of c₀, peak time.
4. Mutations: `test/mutations/run.jl` applies each of A–E (DESIGN §5.1) plus one for C8 (make the occurrence counter ignore fan-out) on a COPY of the package in a temp dir and asserts the TARGETED test file exits nonzero; it must exit 0 only if every mutant is killed. Run it; paste the per-mutant lines.
5. `julia --project=. test/runtests.jl` must exit 0 in < 60 s total; report wall time.

## Engineering constraints
- Elegance is a deliverable: code should read like the mathematics in DESIGN.md/definitions.md (names identical to definitions.md: `ind`, `g_a`, `dec`, `zero`, `arith_q`, `tseitin`, `pcpverifier`, `ev_z`, …). Pure functions on immutable IR values. No macros defining IR. No bare `@assert` in library checkers (return CheckResult values).
- Sparse polynomials: choose an exponent-key representation that keeps the ≤160,000-monomial c₀ fast (e.g. packed integer keys or `NTuple{N,UInt8}`); measure and report. Refuse expansion above `MonomialBudget` with an explicit `ExpansionRefused` value.
- GF(2^k): explicit irreducible polynomial per k (state which for k=3 and k=11; verify irreducibility in a test), constant-time-irrelevant; inverse by exponentiation or extended Euclid.
- Every claimed degree bound has both accounts (structural derivation + actual support) and the test compares them.
- The trace printer output is part of the deliverable (handoff deliverable 6).

## Report (`briefs/09-tb0.last.md`, ≤ 40 lines)
Red output excerpt; green summary; monomial count and timings; mutation results; deviations from DESIGN.md (each with reason); MERGE PROPOSALS (exact text) for any DESIGN.md/definitions.md correction you found necessary and for CLAIMS rows C1–C3, C8 (proposed status TESTED with the where-tested pointer; do not edit CLAIMS.md yourself).
