# Brief 65 report — TB1 + TB2 repair r5 (Fable). Base 98dd89b; no git; lane respected (src/verifiers/{ldt,answer_reduce}.jl, src/samplers/cl.jl for N30, test/tb1_ld_sampler.jl, test/tb2_answer_reduce.jl, test/mutations/{run.jl, tb1_kappa.jl, 4 new files}) plus ONE cross-lane edit (below); no claims/ or docs/ edit.

| item | fix | evidence (file:line) / mutant (all KILLED on passing baselines, evidence line matched) |
|---|---|---|
| TB1 N29 MAJOR | **Variant (b), source-exact.** On a zero-direction line item 3 (`gt-07-ldt.tex:379-384`) is checked at EVERY `t in F_q` (`admissible = field_elements(F)`; unique `t` otherwise); the off-line SOURCE_REPAIR is unchanged. The sweep now counts `degenerate_hits` and the CHECKED node carries `degenerate_line_vs_point`: **1,024** of 37,888 (512 zero-direction support points x both orders; `accepted` with `off_line_hits==0` means point==base on all). TB2 inherits (b) through the shared `_line_point_test`: at the all-zero seed every DLine is degenerate, so the certificate's three diagonal cases compare at all 2048 t (x22 for 4(c)). | `src/verifiers/ldt.jl:127-139`, `:249`, `:286`; `test/tb1_ld_sampler.jl:677`, `:692`; fixtures `tb1:791-813`: honest constant accepted, named negative transcript `degenerate_t0_cheat` (`f(t)=1+t`, degree 1, agrees at t=0 only) REJECTED both orders at `location==1`, off-base point → `:ld_diagonal_point` at `location==:question`; the critic's NM15 verbatim as `test/mutations/tb1_degenerate_line.jl` (target `tb1_decider_rejections`, evidence `off_base=ld_diagonal_point@1`) KILLED 5.3 s. `tb1_kappa.jl`'s `before`/`after` re-headed to the new loop `for j in 1:params.kappa, s in admissible` (same semantics), KILLED 5.1 s. |
| TB1 N30 | **DECIDED: normalise in `direct_sum`** (not widen `_pad_top`, which would undo N25's throw): if every summand is a whole-space zero map (full or empty register) the sum is `CLZero(F,total)` — `CLZero(F,total,Int[])` only when every summand used the empty spelling; a proper sub-register summand is transported verbatim and `pad_level` still refuses it. | `src/samplers/cl.jl:743-757`; `tb1:206-214` (the critic's pin `pad_level(direct_sum(CLZero(F,3), CLZero(F,2,Int[])),1)` → factors `[[1,2,3,4,5]]`; empty+empty → `()` and promotes; `(2,)+full` → `(2,4,5)`, throws); `tb1_dsum_zero_spellings.jl` (target `tb1_levels`, evidence `promoted=false`) KILLED 7.2 s |
| TB1 N31 | option 2 (declared order; the landed C4a row already states it): one assertion — `CLZero(F,5,[2,1])` and `[1,2]` have `register_indices` `(1,2)` and different bytes. Sorting + `S^rep` normalisation stay TB5. | `tb1:413` |
| TB2 NG1/NG2 MAJOR | eighth and ninth replay cases, inserted in step order: `:proof_individual_diagonal` ((oracle,Point_3)/(oracle,DLine_3), corrupt (:right,1)) and `:proof_simultaneous_diagonal` ((oracle,Point_6)/(oracle,DLine_6), corrupt (:right,7)), both `:ld_diagonal_point`; certificate replay = 9 outcomes; suite replay = 27 (case,seed) pairs, 6 of them diagonal | `src/verifiers/answer_reduce.jl:637`, `:645`; `test/tb2_answer_reduce.jl:200-204`, `:776-778`; the critic's mutants verbatim in effect as `tb2_individual_diagonal_never_rejects.jl` / `tb2_simultaneous_diagonal_never_rejects.jl` (target `tb2_replay_seeds`, evidence `cases=9 outcomes=27 honest=27 corrupted_rejected=24`) KILLED 25.4 s / 30.2 s |
| TB2 N27 | the 2916-pair lockstep repeated at `tb2_seed 5` with honest TB0-proof answers (witness (ii) where 4(a)/4(b) fire, cached per (witness,type)): `guard_lockstep_honest mismatches=0 accepted=2916 silent=2736` — C9's "honest there" qualifier can be dropped | `tb2:662-697` |
| TB2 N28 | lockstep keys are `(check, line_kind)`: decider key = enumerator name + trace line kind, asserted consistent with the branch suffix (`_axis`⇔`:ALine`); enumerator key = name + the pair's line-typed side; `branches` asserts `covered == expected_keys` (37), not ⊆ | `tb2:607-619`, `:489-508` |
| TB2 N30 | disposition: the nine cases at the zero seed are inside the certificate; the three-seed honest replay and both lockstep sweeps stay suite-owned; moving the seeds into `_answer_reduce_replay_steps` (r4 N9 residue) remains TB5 | — |

RUNS (quiet: `pgrep -fa 'runtests|mutations/run'` showed only mine; residual desktop load). Suite `julia --project=. test/runtests.jl`: exit 0, **1038/1038** (was 989), TB0 body **22.309 s** (warning 45 not fired), wall 104.6 s, RSS 1.58 GB, `uptime` load 2.33→2.87. Registry `julia --project=. test/mutations/run.jl` (baseline-first): exit 0, **killed=101/101 baselines ok=47/47 wall=490.04 s** (8m10, RSS 0.79 GB, 4 jobs, load 2.54→5.83, no other runtests/mutations); zero SURVIVED/LOAD-ERROR/UNATTRIBUTABLE; new targets `tb2_replay_seeds` (+1 baseline) only; filtered pre-runs of the five arrivals also 5/5 KILLED.
RED first: the new tests run against HEAD's `ldt.jl`/`cl.jl`/`answer_reduce.jl` (`Base.include`d over the package, as the runner does): `decider_rejections` 2 failed (`t0_cheat_passed=true`), `levels` 1 failed + 1 errored (`promoted=false`), `replay_seeds` 2 failed (`outcomes=21`); NM15/NG1/NG2 SURVIVED at HEAD per the verdicts and are KILLED now.
CROSS-LANE EDITS: `test/tb3_frontend.jl:631-632,650,654` — its bare count pin `length(decisions) == 7` → `9` and the `/7` print (TB3 iterates `_answer_reduce_replay_cases()`; all 9 decisions accepted, 3.7 s). Nothing else.
MERGE PROPOSALS (orchestrator; proposals only). **C4c**: apply `verdicts/tb1-r5.md` §5's pre-written row keeping the **variant (b)** sentence and dropping variant (a); the red list already names `tb1_degenerate_line.jl`. **C4a**: replace "`direct_sum` of a whole-space zero map declared on its full register with one declared on the empty register builds exactly such a value, so the two spellings of a whole-space zero map are not interchangeable under `pad_level` after `direct_sum` (`verdicts/tb1-r5.md` N30)." → "`direct_sum` of whole-space zero maps in either spelling is the whole-space zero map on the summed ambient (full register unless every summand used the empty one), so the two spellings stay interchangeable under `pad_level`; a proper sub-register summand is transported verbatim and still refused (`verdicts/tb1-r5.md` N30; `tb1_dsum_zero_spellings.jl`)."; add `dsum_zero_spellings` to the red list; the N31 sentence stands (now pinned at `tb1:413`). **C9**: "seven" → "nine" in both places, "all 21 (case, seed) pairs" → "all 27 (case, seed) pairs"; DELETE the scope sentence "The *diagonal* orientation of the two step-4 low-degree checks has no corrupted-reject witness anywhere in the corpus: … (`verdicts/tb2-r5.md` NG1, NG2)."; replace "— at the all-zero seed with the certificate replay's all-zero answers, which are honest there —" → "— at the all-zero seed with the certificate replay's all-zero answers and again at `tb2_seed 5` with honest TB0-proof answers —"; add `individual_diagonal_never_rejects`, `simultaneous_diagonal_never_rejects` to the red list. **DESIGN**: next to DD-4 item 4 (`docs/DESIGN.md:1058-1060`) append "Downstream (brief 65, `verdicts/tb1-r5.md` N29, variant (b)): `D^ld` reads fig:ld-decider item 3 literally on a zero-direction line — every `t in F_q` is admissible and the answers are compared at all of them (1,024 of TB1's 37,888 line-versus-point decisions; TB2's zero-seed DLine cases)."; §9.4 after the N25 sentence append the C4a N30 sentence above.
