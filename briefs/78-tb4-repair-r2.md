# Brief 78 — TB4 repair r2 (work order = verdicts/tb4-r2.md: NEW-1, NEW-2 MAJOR; NEW-3, NEW-4, NEW-5 MINOR; C11 stale) — small, closing

Proposer (Fable). Autonomous; no questions; **no git**. Lane: `src/compress.jl`, `test/tb4_compress_ir.jl`, `test/mutations/tb4_*.jl`, `test/mutations/run.jl` (additively), `docs/definitions.md` §F (the Level row's pipes and the L448–L449 citation), `docs/DESIGN.md` §5.6 ONLY for the ratio derivation sentence. Report: `briefs/78-tb4-repair-r2.last.md` (≤ 15 lines).

Scratch: `/tmp/claude-1000/-home-tobias-Projects-discussions/548f633b-c857-457a-bb33-0aa1a7879403/scratchpad/tb4-r2/`

Read `verdicts/tb4-r2.md` IN FULL. **NEW-1**: `SOURCE_REPAIR(:HaltDeciderFuelBound)` must cite fig:halt_f step 5 at `gt-12-compression.tex:L448-L449` (L451–453 is the environment end + caption) at all five sites (compress.jl ×3, definitions.md:223, the test); the critic's CRITIC-3 registered and KILLED (a test that greps the cited range for the step-5 text). **NEW-2**: the `CompressStubInTerm` disclosure test must pin the compressor identity (`COMPRESS_STUB`, not `COMPRESS_IDENTITY`) by content, not by the node's fixed tail sentence — CRITIC-4 registered and KILLED. **NEW-3**: derive `TB4_RATIO` from the IN-SUITE kernel rate so the gate enforces the written 6 s (state the derivation and the measured quiet ratio in DESIGN §5.6; make the §5.6 numbers reproducible). **NEW-4**: definitions §F `Level` row to 4 pipes. **NEW-5**: the two display-only CITED leaves get source/lines facts. Then the strengthened **C11** row the critic authorized (verdicts/tb4-r2.md §C11, conditional on NEW-1) is pasted by the orchestrator in the same commit — quote its text verbatim in your report so the orchestrator can verify byte equality. Every new assertion gets a registered mutant KILLED; baseline-first runner exit 0; suite quiet (`uptime`) with the TB4 kernel/ratio printed.
