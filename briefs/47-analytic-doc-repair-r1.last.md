# Brief 47 — analytic document repair r1 (response to `verdicts/analytic-doc-r1.md`)

Scope `docs/analytic/` only; no git, no Julia; `src/`/`test/`/`toys/` read by grep only. Built in `docs/analytic/build/rep1/` (pdflatex ×3); every figure touched was rendered at 110 dpi and looked at before and after.

## MAJOR

| id | disposition | where |
|---|---|---|
| M1 | FIXED | `L`→`λ` for `fig:halt_f`'s third argument: 44×`_{M,L}`, 17×`S_L`, 5×`\log L`, 4×`ComputeSampler(L)`, `(f,m,L)`, `L:P{Level}`→`λ:P{Nat}`, and figs `halt-f-construction`, `psi-ml`, `three-presentations`, `C-closed-verifier`, `C-diagonal-five-steps`, `final-accounting`, `D-final-seal`, `D-correspondence-compression`. `C_L`, `D_{\mathcal L}`, the zipper `L`, the CL function `L`, `L^{lnf}`, and the 5SAT block length `L=2^{ℓ_0}` left alone. MP-1. |
| M2 | FIXED | `part2b` §11.3 rewritten (first/second/third literal → `u_3/u_4/u_5`, gt-10:1105-1107, eq:5sat); `fig-decoupled-5sat` redrawn (p65), now agrees with Fig 78 on its own page |
| M3 | FIXED | §13.1: memberships not equalities (`lem:level-laws`), both counterexamples (`id⊕id`; zero at level 5) in the text, gt-04:271-277 non-uniqueness added, Fig 91's "rather than one" struck, "levels are respectively 1,2,3" → stage-factorization upper bounds |
| M4 | FIXED | `fig-ladder`: TB5 = anchoring+`Repeat`, TB6 = Pauli+`Introspect`, TB7 = end-to-end `Compress`; chips C13/C14/C15 CONJ., C12 rail, C10/C11 "NO ROW / PROPOSED"; TB2 chip corrected to C4b, C9 TESTED / C5 SKETCH; caption rewritten |
| M5 | FIXED | §14.6 retitled "(TB4–TB7)" with six C12–C15 rows; new §14.7 + `fig-tb7-card` (9→5→7→9, 206→840→848→1696, `Q_I=2<s_0=9`, `P_pcp_encodes_D1=FAIL`, `enu:ar-game NOT_EXECUTED`, non-Pauli schemas VACUOUS); §15.1 destaled |
| M6 | FIXED | thm 9.2 proof made one-way (multitape throughout, then one one-tape conversion); statement now `C_L(·)^8`, propagated to (9.4), thm 9.3 (`8+⌈log₂(C_L3⁸)⌉`), `resource-dictionary`, `lambda-preservation`, `FIGURES.md` rule 2; new `fig-exponent-ladder` shows where the exponent doubles |
| M7 | FIXED | reminder at the head of §8 naming all 13 results **SKETCH — not yet adversarially verified, no ratchet row**; repeated in the §14 preamble and the §14.1 caption; new `fig-three-provenances`. MP-4 offers the alternative rows |
| M8 | FIXED | `fig-four-layers` redrawn: 1(a) green EXECUTED / 1(b) slate CITED — C5 SKETCH; arrows 2,3 ⇒ 1(b) and 1(b),4 ⇒ `thm:ar`; then moved to the head of §12 (S3) |
| M9 | FIXED | `PCPCLMap`→`_pcp_cl_map`/`CLStep`, `PCPPaddedMap`→`pad_level`, `TypedProductCL`→`Checked{TypedSampler}`, `sequential_value`→`sequential_and_optval` (3 sites); laziness re-attributed to `CLStep` (`src/samplers/cl.jl:10-26`) |
| M10 | FIXED | §14.3 `fig:ld-decider` → "no current row"; `lem:cl-concat, lem:cl-func-prod` → "no current row"; also `lem:detyping-verifiers` → "no current row" |
| M11 | FIXED | new §8.2 "Certificates and the derivation tree": `Checked{T,C}`, `CertNode`, five grades, replay, `Concrete`/`Opaque`, plus the four TB7 predicate outcomes; figs `grades` and `certificate-tree` (the real `AnswerReduce` tree of `test/tb2_answer_reduce.jl`) |

## MINOR / NOTE / suggestions

**All 21 MINORs FIXED.** m1 split sentence rejoined (`part1b`) · m2 all seven rows of (8.2) are 5-tuples, `η'` named · m3 Fig 88 marks swapped + "`b_2<b_1` whenever `d≥1`" · m4 0 backward figure/page pairs (was 2) · m5 §4→§§8–10 arrow added, caption drops "exactly" and names §2/§12 · m6 window function → `ω` (`Δ` was taken), μ/γ/τ/k(n)/ε₁/ε₂/C₀ defined in `fig-parameter-card` · m7 forward reference to Def. 13.1 at Def. 7.4 + symbol-table row · m8 "the 3 pays for install, removal and the tag check; it absorbs the two delimiter units" · m9 8 sites → `→_β^*`, `YF =_β F(YF)`, `ZF =_β F(λu.(ZF)u)`, Turing's `Θ` named · m10 `s(z,z)` restated in Part I's form · m11 three DESIGN §1.1 divergences declared `SOURCE_REPAIR` (MP-2) · m12 "Step 1 of `fig:pcpverifier`, which the answer-reduced decider invokes" + `fig-ar-invokes-pcp` · m13 footnote cites `def:typed-sampler-sample` too · m14 `SuccinctDecoupled5SAT` · m15 `zero_basis_decompose` + claim column on all six rows · m16 `copyflow` in `y-derivation` and `psi-ml` · m17 chips below row 2 · m18 copies labelled `(u_3)_1→a`/`(u_3)_2→b`, lengths `L=2^{ℓ_0}`/`R=2^{r_0}` (gt-10:963) · m19 `lem:zero-rewrite`, `def:cl-func-doc`, `lem:level-laws` added and cited · m20 one symbol `\degF` = `deg_F` · m21 "an integer in binary".

**NOTE.** n1 RESIDUE (ratchet-side, not the document): the text now says `CLStep` is lazy and memoised; `claims/CLAIMS.md` C4b still says otherwise → MP-3. n2 FIXED: nine stale *under*-claims also corrected (C4b/C9 Conj.→Tested in five places, the "C4b remains CONJECTURE" paragraph, §15.1, and — picked up mid-round from a concurrent ratchet edit — C2 Conj.→Tested in three places, `verdicts/tb0-r3.md` (PROMOTE)). n3 FIXED: gate reproduced and re-established at the new size.

**S-items.** S1 FIXED (`fig-miniature`, one `Apply` microstep at `F=1` carried window → clause index → five blocks). S2 FIXED (`fig-parameter-card` §7.1 + the TB7 instantiation). S3 FIXED (after M8). S4 DOWNGRADED, exactly as the verdict's own counter-demand directed: M4/M5/M9/M10/m15 fixed, tables not split. S5 FIXED (`fig-symbol-table`, p2, before the TOC).

**Counts.** MAJOR 11 FIXED / 0 DOWNGRADED / 0 RESIDUE · MINOR 21 / 0 / 0 · NOTE 2 FIXED / 0 / 1 RESIDUE · S-items 4 FIXED / 1 DOWNGRADED / 0 RESIDUE.

## Final gate

**91 pages · 0 errors · 0 overfull · 0 undefined · 110 figures · `tools/figcoverage.py`: no page without a figure · 0 backward figure/page pairs.** PDF copied to `docs/analytic/analytic-underpinnings.pdf`.

## Figures redrawn (slug · page · before → after)

`decoupled-5sat` · 65 · 3SAT clauses only in `u_3`, one common `M=2^m` → one literal per block plus `(u_3)_1→a`, `(u_3)_2→b`, `L=2^{ℓ_0}`/`R=2^{r_0}` — `four-layers` · 70 · green EXECUTED stratum 1 on a 1→2→3→4 chain → split 1(a)/1(b) with the real dependency spine, moved to the §12 head — `ladder` · 81 · TB5/TB6 swapped, four C7 chips, chips above row 2 → correct rungs, C12–C15 + C10/C11 NO ROW, chips below, C12 API rail — `correspondence-map` · 82 · no claim column, `zero_basis` → four columns with claims, `zero_basis_decompose` — `D-threshold-margin` · 73 · `b_1` drawn left of `b_2` → swapped with the ordering note — `three-presentations` · 60 · `L`, arrowheads *into* the paper node → `λ`, arrows out of it — `psi-ml` · 59 · `L`, violet `quote` on the hole → `λ`, `copyflow` — `halt-f-construction` · 17 and `C-closed-verifier` · 58 · `L` → `λ` — `y-derivation` · 23 · violet "assemble", `YF→F(YF)` → `copyflow`, `YF =_β F(YF)` — `roadmap` · 3 · four upgrade arrows → five — `D-correspondence-description` · 82 · `Succinct5SAT` → `SuccinctDecoupled5SAT` — `D-correspondence-typed` · 84 · stale C4b chip → `C5 SKETCH` — `D-correspondence-midpoint` · 85 · `sequential_value` → `sequential_and_optval` — `tseitin-fanout` · 68, `succinct-circuit` · 63, `trace-tableau` · 62 · `δ_F`→`deg_F`, `τ`→`ω`.

Nine new: `symbol-table` · 2 (S5) — `parameter-card` · 34 (S2) — `ar-invokes-pcp` · 36 (m12) — `three-provenances` · 37 (M7) — `grades` · 40 (M11) — `certificate-tree` · 41 (M11) — `exponent-ladder` · 51 (M6) — `miniature` · 66 (S1) — `tb7-card` · 87 (M5). All recorded in `FIGURES.md` §3; no ★ plan item was dropped.

## MERGE PROPOSALS (proposals only — nothing outside `docs/analytic/` was written)

**MP-1 — `docs/DESIGN.md` §1.1: the `L`→`λ` rename (M1).** `fig:halt_f`'s third argument is the resource bound of `def:lambda` (gt-12:435-440 has `(\desc{R},\desc{M},\lambda,n,…)`, `ComputeSampler(\lambda)`, `Compress(\overline{\verifier},\lambda)`), not a CL level; DESIGN §12.6 itself writes `set L=lambda=32768`. Proposed edits: `Fuel ::= FuelLiteral(Nat) | FuelBound(P{Nat},P{Level})` → `… | FuelBound(P{Nat},P{Nat})`; drop `Level` from the sort list `Name, PrimName, Nat, MachineDesc, Level, StaticEnv` (nothing else uses it — the CL notion `level(V)=level(S)` is a different symbol and stays); `` `L:P{Level}` are closed literal terms `` → `` `lambda:P{Nat}` are closed literal terms ``; `S_L`→`S_lambda`, `Psi_M_L`→`Psi_M_lambda`, `D_M_L`→`D_M_lambda` throughout §1.1 and §12.6, so `Compress` sees the quote `(S_lambda,d)`. `claims/CLAIMS.md` C15 likewise writes `D_{M,L}=Y Psi_{M,L}`; proposed `D_{M,lambda}=Y Psi_{M,lambda}` — a rewording, not a status move.

**MP-2 — `docs/DESIGN.md` §1.1: three lockstep repairs (m11).** The document now declares each `SOURCE_REPAIR` against DESIGN; the reverse edits would remove the divergence. (i) §1.1's `Psi_M_L` display should carry the outer `Eval` of the *returned* decider and a fuel symbol for the compression call: `Eval(ans(Eval(Apply(Quote(Compress),…),F_C)),(n,x,y,a,b),FuelBound(n,lambda))`. (ii) §1.1's fixed-point equation `eval(Quote(Fix(P)),u;fuel) = eval(specialize(P,{self_code=>Quote(Fix(P))}),u;fuel)` should read `…;fuel - c_Y)` with `c_Y = 3`, matching thm 10.3. (iii) the prose "`Fix(P)` requires the distinguished hole `self_code : Quoted{Decider}`" should read `self_code : Quoted{A}`, matching §1.1's own `Fix` interface.

**MP-3 — `claims/CLAIMS.md` C4b (n1).** C4b asserts that `CLStep` "materialises its branch table eagerly" and that the 18-map family is "not built at any parameter". Against this tree both are false: `src/samplers/cl.jl:10-26` memoises a `branch::Function` into `children::Dict`, and `test/tb2_answer_reduce.jl` builds the family at `q=2^11`. Proposed: strike those two clauses. Not a status move — C4b already stands TESTED.

**MP-4 — `claims/CLAIMS.md`: new rows C16–C19 at SKETCH (M7, first option).** The document already discharges M7 via its explicit unratcheted-status statement; these rows are the alternative. All four are **SKETCH** — written derivations, no machine check, no converged verdict.

| id | statement (quantifiers included) | status | depends-on | where-proved | where-tested | verdict |
|----|----------------------------------|--------|------------|--------------|--------------|---------|
| C16 | (Semantics of `L`) For the language `L` of docs/analytic §8, every closed term `t`, every argument tuple `u` of the declared sorts and every fuel `f`: the CEK relation is a function, so `eval_L(t,u;f)` is single-valued in {Value(v), OutOfFuel, TypeError}; for every `g>=0`, if `eval_L(t,u;f)` returns a value or type error using exactly `c<=f` units then `eval_L(t,u;f+g)` returns the same result using exactly `c`; `Eval_L(Quote(t),u;f+h(d,u)) = eval_L(t,u;f)` for `d=ser(t)` and `h(d,u)=3+|d|+|enc(u)|`, the left side using exactly `h(d,u)+c`; and `|Specialize(P,sigma)| = |P| - sum_h |Hole(h,A_h)| + sum_h |sigma(h)|` under the affine-hole discipline. Written derivations only; not adversarially verified. | SKETCH | — | `docs/analytic/parts/part2a.tex` §8.3--§8.4 (Theorems 8.2--8.4, Lemma 8.5) | — | — |
| C17 | (Costed machine/term bridge) There are universal constants `a_0,b_0,c_0,a_U,b_U,C_L` and a fixed exponent `e=8` such that every k-input paper machine `M` compiles to a closed `L` term of size at most `a_0|M|+b_0` agreeing with `M` on every input and terminating within fuel `c_0(|M|+k+1)*2T(T+|x|)` when `M` halts in `T` steps; and one three-input machine `U_L` of description length `b_U=O(1)` satisfies `TIME_{U_L}(d,u,f) <= C_L(|d|+|u|+f+1)^8`, hardwires to `|(U_L)_t| <= a_U|t|+b_U`, and with lazy input handles gives the suffix-independent `C_L(|t|+f+1)^8`. The exponent is not claimed optimal: the derivation gives `2*max{4,1+max_p e_p}` and only the existence of some fixed `e` is used downstream. Hence a universal integer `A>=1` maps lambda-bounded verifiers to `A*lambda`-bounded ones in both directions. Written derivations only. | SKETCH | C16 | `docs/analytic/parts/part2a.tex` §9.1--§9.3 (Theorems 9.1--9.3) | — | — |
| C18 | (Description-level self-reference with size accounting) There are constants `a_s,b_s,a_K,b_K` and `c_Y=3` such that the s-m-n transformer satisfies `phi^{(k)}_{s_{r,k}(e,z)}(x) = phi^{(r+k)}_e(z,x)` with `|s_{r,k}(e,z)| <= a_s(|e|+sum_i|z_i|)+b_s` in linear time; Kleene's `e_Q=s(p,p)` satisfies `phi_{e_Q} = phi_{Q(e_Q)}` with `|e_Q| <= a_K|q|+b_K`; `YCode` closes the single `self_code` hole at constant runtime charge `c_Y=3` rather than materialising `Specialize(P,{self_code -> d_P})`; and the paper's `F(F,M,lambda,...)`, the Kleene fixed point and `D_{M,lambda}=YCode(Psi_{M,lambda})` are three descriptions of the same partial function, all of size `O(|M|+log lambda+1)`. Written derivations only; the extensional fixed-point value argument stays CITED. | SKETCH | C16,C17 | `docs/analytic/parts/part2a.tex` §10.1--§10.3 (Lemma 10.1, Theorems 10.2--10.4) | — | — |
| C19 | (Cook--Levin for `L`-descriptions) There are a finite alphabet `Gamma_L`, a fixed track count `r_0` and a Boolean window function `omega` such that two consecutive rows of a bounded `L` trace are successive iff they agree with `omega` at every radius-one window and have valid endmarkers and one head marker per track; and an algorithm which, on `(t,n,F,Q,sigma,x,y)` with `|t|<=sigma` and `|x|,|y|<=Q`, outputs a circuit of index width `O(log F+log sigma)` and size/construction time `poly(log n, log F, Q, sigma)` deciding membership of an indexed signed clause in a 3CNF `Phi` satisfiable with the prescribed input bits iff `Eval_L(Quote(t),(n,x,y,a,b);F)` accepts, with `2F` reserved bits per answer block; the decoupled five-block form places each 3SAT clause's first, second and third literal in `u_3,u_4,u_5` with equality gadgets `u_3=u_4=u_5`, retaining the same size bound. Written derivations only; no `bounded_trace`, `cook_levin` or `decouple5` implementation exists. | SKETCH | C16,C17 | `docs/analytic/parts/part2b.tex` §11.1--§11.3 (Lemma 11.1, Theorem 11.2) | — | — |
