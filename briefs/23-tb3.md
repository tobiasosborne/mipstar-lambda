# Brief 23 — TB3 implementation (red/green TDD): the quoted front end — program IR, fuel-bounded evaluator, bounded trace → succinct 3SAT → decoupled 5SAT → (existing) Tseitin/arith/PCP

Proposer (gpt-5.6-sol, xhigh). Autonomous; no questions; no git. Lane: `src/ir/programs.jl` (new), `src/frontend/**` (new: bounded_trace, cook_levin, decouple5), `test/tb3_frontend.jl` (new), `test/mutations/tb3_*.jl` (new), MINIMAL additive edits to `src/MIPStarLambda.jl`, `test/runtests.jl`, `test/mutations/run.jl`. Do NOT modify TB0/TB1/TB2 files; wrap and list "API REQUESTS". Report: `briefs/23-tb3.last.md`.

## Read order
1. `CLAUDE.md`; rk-light law 4; `handoff.md` §"Representation requirements" (distinguish closure / quoted syntax / compiled circuit / universal evaluator / specialization; the compression machine consumes DESCRIPTIONS).
2. `docs/DESIGN.md` §1.1 (program sort: BoundedVar/Lambda/Apply/Fix/If/Prim/Quote/Eval/Specialize/Hole; Quoted{A} with canonical serialization and exact description_size; Closure vs Quoted; YCode; DD-1, DD-2), §1.2 (BoundedTrace, Succinct3SAT, SuccinctDecoupled5SAT, TseitinFormula), §2 (quote_program, specialize, bounded_trace, cook_levin, decouple5 signatures and evidence extension), §5.5 (TB3 instance: closed program `λ n x y a b. true`, fuel T=1, explicit accepting one-transition trace; honest small Cook–Levin: trace → 3SAT → single decoupled clause; succinct membership circuit with one live gate + padding to s=6, m=1, m'=16; feed the GENERATED description and witness into TB0's PCP builder and TB2's decider; assert quote hash/size propagation; mutation: accepting transition → rejecting without changing the formula must fail trace/formula equivalence BEFORE the PCP stage; risk 9: add a two-step equality decider and snapshot gate/monomial growth). `docs/definitions.md` §F (program sorts).
3. Ground truth: `ground-truth/gt-10-answer-reduction.tex` def:succinct-formulas, prop:standard-succinct-sat (statement; the shape of a succinct description, not the asymptotics), sec:succinct-deciders (the decoupled 5SAT clause form and why: the two "undesirable properties"), def:tseitin; `ground-truth/gt-03-prelim.tex` sec on Turing machines / deciders (search `\subsection{Turing machines}` and def:decider in gt-05) for what a decider's input tuple (n,x,y,a,b) is.
4. Existing code: `src/ir/circuits.jl` (Circuit, tseitin, arith_q, phi_C), `src/tb0.jl` (build_pcp path), `src/verifiers/answer_reduce.jl` (decider entry point).

## Process
1. Write `test/tb3_frontend.jl` FIRST (RED; paste excerpt): (a) program IR round-trips through canonical serialization; `description_size` is exact byte length; `Quote` of an open term is refused; `Closure` is a distinct type and is refused by `bounded_trace`; (b) `Eval` with fuel returns Value/OutOfFuel/TypeError never throws; a `Fix` term that would not terminate returns OutOfFuel; (c) `bounded_trace(λnxyab.true, input, T=1)` = the explicit one-transition accepting trace; (d) `cook_levin(trace)` yields a Succinct3SAT whose clause relation, enumerated, is satisfiable iff the trace accepts (exhaustive over the tiny variable set); (e) `decouple5` yields a SuccinctDecoupled5SAT with five index blocks + five sign bits and the guarantee "(a,b,w1,w2,w3) satisfies φ5 iff D accepts (n,x,y,a,b)" checked exhaustively for the fixture; (f) the generated circuit has m=1, s=6 (padding disclosed), and feeding it to TB0's `build_pcp` and TB2's answer-reduced decider gives an accepted honest strategy (reuse their functions; if the fixture differs from TB0's hard-coded circuit, that is fine — the point is the GENERATED path); (g) the two-step equality decider `λ n x y a b. (a == b)` with T=2: trace, 3SAT, 5SAT, circuit sizes and c₀ monomial count printed (snapshot; if the PCP build exceeds the MonomialBudget, report `ExpansionRefused` honestly as the design says — that is a result, not a failure); (h) certificate: the trace printout shows [CHECKED] Quote |D| = N bytes, BoundedTrace, Succinct3SAT, Decoupled5SAT nodes and the quote hash propagating into the PCP certificate.
2. Implement; GREEN; paste summary + walls + trace printout.
3. Mutations (copies, registered): (M-acc) accepting→rejecting transition with formula unchanged → equivalence check fails before PCP; (M-size) description_size off by one → hash/size propagation test fails; (M-fuel) Eval ignores fuel → non-terminating Fix test fails (guard with a hard cap so the mutant does not hang: the test must time-box); (M-decouple) decouple5 reuses block 1 for block 3 → decoupling shape test fails; (M-closure) bounded_trace accepts a Closure → type test fails.
4. Time: TB3 ≤ 20 s; whole suite under the gate warm.

## Report (≤ 30 lines)
RED excerpt; GREEN summary; sizes snapshot for (g); trace printout; mutation lines; API REQUESTS; deviations; MERGE PROPOSALS: new claim rows C10 (front-end faithfulness on the fixtures, TESTED) and any DESIGN §1.1/§5.5 correction — proposals only.

## ADDENDUM (2026-09-05)
- Proposer is Fable (not codex). The description layer's canonical serialization (`canonical_bytes`, `description_size`) landed for CL branches in brief 46 (`src/samplers/cl.jl`, `QuotedBranch`); the program IR's canonical serialization must be the SAME encoding family (one `canonical_bytes` convention across the repo) and add the missing deserializer + round-trip test (brief 46 unresolved item 3).
- C19 (SKETCH, claims/CLAIMS.md) is the Cook–Levin-for-L statement TB3 makes executable on the fixture; C16 the semantics of L (single-valued eval, fuel monotonicity, `Eval` overhead `h(d,u)=3+|d|+|enc(u)|`, `|Specialize|` size law). Tests (b) and (a) are their first machine evidence — say so in MERGE PROPOSALS (proposals only; C16/C19 stay SKETCH until a verdict).
- The mutation runner is baseline-first (`run.jl`, brief 42); register every new mutant and paste the REGISTRY line.

## ADDENDUM 2 (2026-09-05): TB0 minor sweep folded in (verdicts/tb0-r4.md PASS, N13–N16 MINOR)
Before the TB3 work, spend ≤ 30 minutes on the four one-line TB0 coverage items so the TB0 lane is fully closed (additive edits to `test/tb0_core.jl` testset 5c and one DESIGN §5 clause are permitted for this purpose only; list them under CROSS-LANE EDITS): N13 (over-degree quotient proof must fail the root `def:pcp-proof` replay), N14 (the `g_3`-swap chimera must fail with `:certificate_binding`), N15 (`build_pcp` with a `bad_gs` routed around `ev_z` must throw), N16 (DESIGN §5: keep the r3 with/without delta and add the r4 `tools/cold_precompile.sh` figure 124.4 s). Each gets a registered mutant (the critic's R4M2/R4M3/R4M5) shown KILLED.
