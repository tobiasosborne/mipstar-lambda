# Brief 74 — CRITIC verdict r1 on rung TB5 (the DESIGN §9 description layer and the executable Repeat = anchoring + 81-fold repetition) — also the readiness gate for TB6

You are the adversarial critic (Opus). ATTACK; do not summarize. Autonomous; no questions. Lane: write `verdicts/tb5-r1.md` ONLY; Julia/files only under scratch; never edit repo files; no state-changing git. Evaluate the ARCHIVED tree at commit `f7e69ed` (`git archive f7e69ed | tar -x -C <scratch>/tree`; instantiate there; cold precompile ~90–200 s). Never read src/test from the live tree (a worker edits it); `claims/CLAIMS.md` may be read live.

Scratch: `/tmp/claude-1000/-home-tobias-Projects-discussions/548f633b-c857-457a-bb33-0aa1a7879403/scratchpad/critic-tb5-r1/`

## Read order
1. `~/.claude/skills/rk-light/SKILL.md`; `CLAUDE.md`; `claims/CLAIMS.md` (C12, C13 CONJECTURE — the targets; C4a/C4b/C4c/C9/C11 TESTED are what this rung builds on).
2. `docs/DESIGN.md` §9 in full (9.1 sorts + four query modes with prefix domains; 9.2 LawCert, def:sampler replay with `prefix_i`, description_size = canonical bytes, GapMap; 9.3 adapter; 9.4 DL9 closure lemmas + zero-map promotion; 9.5 executable detyping; 9.6 contracts incl. the mandatory output-sampler replay row), §10 in full (anchoring, repetition, TB5 instance, T5-* transcripts, M5-* mutations, DD-26), §13.1 TB5 row, §1.5; `docs/definitions.md` §G–H; `verdicts/design-v2-r3.md` §5 (NOTEs A–D) and §2; the r4/r5 forward-look NOTEs in `verdicts/tb1-r4.md`, `tb2-r4.md`, `tb1-r5.md`, `tb2-r5.md` (brief 39's addenda 1–3 made them binding).
3. `briefs/39-tb5-descriptions-repeat.md` (+ addenda 1–3) and `briefs/39-tb5-descriptions-repeat.last.md` (RED/GREEN, sizes 130/244/292 and `|S^rep| = 85` bytes, the replay chain counts, laws, anchor/repeat numbers B=9 k=81 level 3 dim 729, the four transcripts, the independence hash `925f2085c0763b30`, the 55-node tree, walls, 21 mutants, CROSS-LANE EDITS in `src/compress.jl` (StageVerifier carrier, params), API REQUESTS, the nine DEVIATIONS with gt lines, MERGE PROPOSALS C12/C13 + DESIGN sentences + the calibration-ratio gate, and the four "unresolved" items: allocation measured as total not peak; TB5 body 22.8 s dominated by exhaustive 8^5 replays; descriptions are data for fixed Julia interpreters, not §1.1 programs; two TB4 mutant strings re-pinned).
4. Ground truth (recompute, never from memory): `ground-truth/gt-04-cl.tex` L122–L130, L151–L180, L315–L327, L572–L601, L614–L626, L628–L680; `gt-06-types.tex` L95–L151, L185–L195, L370–L380; `gt-11-parallel-repetition.tex` L89–L103, L112–L136, L139–L159, L200–L220, L229–L258, L268–L292; `gt-12-compression.tex` L70, L355; `gt-05-games-normalform.tex` L624–L635.

## Obligations
- **Run** suite (summary + TB0/TB4/TB5 walls + `uptime`) and runner (`MUTATION REGISTRY`; all 140 KILLED; baselines ok); cold precompile.
- **Independent recomputation on a COPY** (your own code, never the package's decoder/interpreter): (1) the three leaf description sizes 130/244/292 and the pair-byte layout `0xC3 01 q ‖ termA ‖ termB` from your own reserialization; (2) `|S^rep| = 85` bytes at λ=1 AND λ=2 — is the k-fold repetition genuinely a compact loop term (description_size independent of k)? try λ=3; (3) the four-query modes on `describe_cl(L_Point/ALine/DLine,·,8)` against your own `marginal_k`/matrix evaluation on all 8^5 seeds; `QueryError` on j=0, j>ℓ, wrong player, unreachable `Factor` prefix vs `Linear`'s broader domain; (4) the §9.2 replay counts (chains 1/8/288 exhaustive; `S^anch` 2^9 with 18 chains; `S^rep` rng32) and whether `enu:cl-space-sum`/`enu:cl-map-sum` are red-capable at the description level (not just inherited from TB1's `cl_kth_replay`); (5) the DL9 laws: `direct_sum` 8/3/10, `product` 2048/3/40 with 54/54 describable and the tensor edge, `downsize` 2/2/15, `detype` `ell+2` and `s + 4·TypeCount` — each against gt-04 L628–L680 / gt-06 / DESIGN §9.4–9.5; (6) anchoring: type set {Game, Anchor}, complete graph with self-loops (4 edges), Anchor's Factor = whole ambient at stage 1 and empty after (SOURCE_REPAIR(zero-map-factor-partition)), Anchor-scoped `enu:cl-space-sum`; detyped (2, level 3, dim 9); (7) repetition: B = (λn)^τ = 9, k = (λn)^{(1+c')τ} = 81, level 3, dim 729; the gt-12:L70 vs gt-11:L200/gt-12:L355 finding; `k(n)` stored symbolically with integrality ASSUMED and the universal bound NOT_EVALUABLE(owner) — DD-31; `repeat_decider` computes B(n) before reading any payload and rejects the 10-bit component with the child-call log EMPTY (DD-26) — probe with a hostile length prefix; (8) the 128 seeded honest pairs and the four named transcripts (NOTE-D: the 9-bit component is a QUESTION component); (9) sampler independence: identical `S^rep` hash for two byte-distinct deciders, dependency set exactly `{hash(S), lambda, tau, c_prime}` — and the SOURCE_REPAIR `:RepeatTupleFraming` (32-bit framing) honest against gt-11:L216–L220; (10) the CITED labels (66) exist at their ranges.
- **Certificate honesty & red-capability**: 55 nodes (CONSTRUCTED 9, CHECKED 27, CITED 10, ASSUMED 4, SOURCE_REPAIR 5) — every CHECKED node owned by a mutant; the nine deviations each carry a SOURCE_REPAIR/ASSUMED node or an honest note; no CHECKED node stronger than its children.
- **Two NEW semantic mutations** (e.g. `repeat_decider` ANDs all but the last component; the anchor decider ignores an Anchor-typed answer of 1 on one side; `product`'s tensor graph missing one edge; `detype` parser accepting a non-canonical graph encoding); survivors are MAJOR with the red test as FIX DEMAND.
- **Lockstep**: DESIGN §9–10 ↔ definitions §G–H ↔ code ↔ the proposed C12/C13 rows; adjudicate the proposer's DESIGN sentence proposals and the calibration-ratio gate proposal.
- **Elegance**: three places the code is more complicated than the mathematics.
- **TB6 readiness (≤ 10 lines)**: with descriptions, the pair adapter, the DL9 combinators and the StageVerifier carrier, can brief 43 (TB6: Pauli sampler/decider, introspection sampler/decider with the §11.4 query schedule, metered child calls in the quoted interpreter, stabilizer simulation) be executed without a design round? Name blocking gaps — especially the API REQUEST that descriptions are data for fixed interpreters rather than §1.1 programs (TB6's fuel meter must count quoted-interpreter steps of the CHILD sampler/decider).

## Output: `verdicts/tb5-r1.md`
Numbered objections with severity/location/computation/FIX DEMAND/SURVIVING WEAKER STATEMENT; runs + walls + load; your mutations; per-claim: C12 and C13 (PROMOTE to TESTED with authorized verbatim rows, or HOLD with the missing step; you may authorize a scoped weaker row as tb4-r1 did); DESIGN/definitions adjudication; TB6 readiness; final line exactly `VERDICT: PASS` or `VERDICT: FAIL(<ids>)`.
