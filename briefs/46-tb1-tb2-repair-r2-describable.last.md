# Brief 46 report — TB1+TB2 repair r2 + DESIGN §9 describability preparation (proposer: Fable; tree `6f4083a` + lane edits, NO git)
Final quiet-machine runs (`pgrep -fa 'runtests|mutations/run'` empty at start, load 0.91): `julia --project=. test/runtests.jl` **exit 0, 499/499, 1m55.0s, TB0 test-body 39.622 s** (load/precompile 0.327 s ungated; earlier loaded runs: 40.94 s at load 3.96, 40.71 s at load ~3.5; baseline unmodified tree 36.49 s); `julia --project=. test/mutations/run.jl` **exit 0, killed=61/61, baselines ok=36/36, wall 471.1 s (real 7m52)**. Logs: `scratchpad/tb1-tb2-r2/{suite-final2,registry-final2}.log`. Cold-cache precompile of the package after this change: 103 s (ungated).
## Response table
| obj | disp. | evidence (file:testset / mutant) |
|---|---|---|
| TB1 N1 diagonal `md` bound | FIXED | `test/tb1_ld_sampler.jl` `decider_rejections`: `restrict(x1^2*x2, diagonal_line((3,0,0,2,7),2))` (deg 3 > md=2) → `:ld_diagonal_degree`, `(:left,1)`, `!passed`; `test/mutations/tb1_dline_degree.jl` (NM1, `params.m*params.d`→`typemax(Int)`) KILLED with evidence |
| TB1 N2 `_child` validation unwitnessed | FIXED | `levels`: `@test_throws ArgumentError apply(...)` for wrong level (child = `pad_level(CLZero(F,2,(2,)),1)`), wrong rest register, wrong `seed_dim`, wrong field; do-block constructor with non-spanning `factor ∪ rest` throws; `tb1_child_validation.jl` (NM2) and `tb1_ambient_doblock.jl` (NM3) KILLED (first registry pass had NM2 SURVIVED because my wrong-level child used the ambient-checked public constructor; witness rebuilt, re-run 61/61) |
| TB1 N3 `L_Point` ⊬ `enu:cl-space-sum` | FIXED | `src/samplers/ldt.jl:_build_L_Point` = single stage on the whole ambient `1:5` with the projector onto `V_pt`, empty tail (all TB1 counts unchanged: 512/18,432/32,768/98,304/71,360/40,768/16/568); new `space_sum` testset: `marginal_k` factor union = `1:5` for all 3 maps and `cl_kth_replay` exhaustive (3 × 32,768, `map_sum_checks` = level×32,768, distinct chains [1,8,288]) + negative witness (r2 sub-ambient `L_Point` → `space_sum_ok=false`); `tb1_space_sum.jl` KILLED. C4a clause struck (proposal below) |
| TB1 N4 off-line totalization | FIXED | `src/verifiers/ldt.jl:_line_point_test` SOURCE_REPAIR comment citing `gt-07-ldt.tex:377-384` + literal alternative; `ld_off_line_repair(; honest_support_hits, of)` → `CertNode(SOURCE_REPAIR, :ld_off_line_rejects)`; the `decider` sweep counts hits (**0 of 71,360**) and asserts the node |
| TB1 N5 = TB2 N3 `pad_level` prepends | FIXED | `src/samplers/typed.jl:_pad_tail` APPENDS (`BranchPadded`); a `CLZero` on register R promotes to stage 1 = R with the zero map, then empty stages (`ZERO_MAP_FACTOR_PARTITION` SOURCE_REPAIR node, `gt-04-cl.tex:122-130`); asserted `marginal_k(pad(L_ALine,3),z,1) == marginal_k(L_ALine,z,1)`, factors `[[3,4,5],[1,2],[]]`, `pad(CLZero(F,5),3)` → `[[1..5],[],[]]`, `Factor(…,1,0)=ones(5)`; `tb1_pad_order.jl` (inline prepend loop) KILLED |
| TB1 N6 `chifree` unowned; N7 warm-memo timing | FIXED / FIXED | `tb1_chifree.jl` = NM5 verbatim (test-file mutant) KILLED; TB2 `sampler` prints both timings: `DLine_6 apply=2.09 us (warm memo, 20 seeds x 50); 6.95 us (1000 fresh seeds)` (6.41 us in the previous quiet run) |
| TB1 N8 / N9 / N10 / N11 | NOTE / proposals / FIXED / FIXED | walls above; N9 lockstep proposals below; N10: identical-branch ternary deleted, `const D_ld` deleted (export removed, cross-lane); N11: sweep asserts `non_noop = 2,880 equal-type tautologies + 37,888 line-vs-point` and prints the split |
| TB2 N1 product maps off the decider path | FIXED | `sample_answer_reduce_questions` = projection of `sample(verifier.sampler, (left,right), seed)` (`edge_index`, `_answer_reduce_project`, `pcp_question_from_ambient`); the old split survives only as the test reference `tb2_hand_split`, asserted equal over 20 seeds × 54 types × both sides (1,080 compares) plus `sample == apply(product map)`; `tb2_nd2.jl` (summand swap) KILLED with evidence `product_projection agrees=false` |
| TB2 N2 replay = reachability only | FIXED | `_answer_reduce_replay_steps`: 7 cases (steps 1, 2, 3 axis, 3 diagonal, 4a, 4c, 5) at the all-zero seed, each honest-accept AND corrupted-reject with the expected rule (`global_consistency, input_consistency, ld_axis_point, ld_diagonal_point, proof_consistency, ld_axis_point, pcpverifier`); `verify_certificate` asserted and the outcomes printed; `tb2_nd4.jl` (ND4, target `tb2_sampler`) KILLED with evidence |
| TB2 N5 graph written complete | FIXED | `oracularize.jl`: `E^ar = {((l,r),(l',r')) : (l,l')∈E^ora, (r,r')∈E^pcp}` computed as the tensor product; test asserts equality with the complete 54² graph on this fixture and the DESIGN red-test edge `((oracle,Point_1),(alice,DLine_6))`; `tb2_tensor.jl` (Cartesian graph product, 1,080 edges) KILLED with evidence `product_edges actual=1080` |
| TB2 N4 loop order; O5 | proposal / RESIDUE | N4: DESIGN §1.6 sentence proposed below (no code change: verdicts coincide, every rejection is terminal); O5: orchestrator lane (`briefs/18-tb2.last.md`) |
| TB2 O10 / O11 | FIXED | all 12 TB2 mutants carry `expected_evidence`; `i345`/`mc1`/`mc2`/`mc3` print `MUTATION_EXPECTED_RULE …` (`i345 actual=(1, 2, 3, 4, 5)`, `branches first_failure=ld_diagonal_point / input_consistency / ld_answer_arity`), each verified by a filtered run before the registry |
## §9 preparation (all in `src/samplers/cl.jl` unless noted)
| item | API | test (target) | mutant | result |
|---|---|---|---|---|
| `QuotedBranch` | `BranchConst(child)`, `BranchByAxis(m, position, table)` (eq:chi-func selector), `BranchLnf(n, point, tail)` (v' ↦ stage `L_lnf(v')`), `BranchPadded(inner, extra)`; `OpaqueBranch(f)` wraps host closures; `describe_cl(L) :: CLDescription | NotDescribable`, `canonical_bytes`, `description_size` (= byte length of the canonical term: magic, q, n, level, tagged nested term with field-width matrix bytes); all 3 TB1 maps (`ldt.jl`) and 18 PCP maps (`pcp_sampler.jl`) rebuilt on these | `tb1_describe` (3 maps, padded maps, closure → `NotDescribable`, `direct_sum` → `NotDescribable`), `tb2_describe` (18/18, deterministic bytes) | `tb1_describe_closure.jl`, `tb2_opaque.jl` | KILLED |
| prefix walks | `Dimension(L)`, `Marginal(L,j,z)`, `Factor(L,j,u)` → length-n 0/1 `Vector{Int}`, domain `u ∈ L_{<j}(V)` enforced per stage by column-space membership (Gaussian elimination) + zero support outside walked factors; `Linear(L,j,u,y)` domain `u ∈ V_{<j}` (unreachable prefixes answered, e.g. `(0,0,4,1,0)` on `L_ALine`); illegal calls throw `ArgumentError` | `tb1_queries` | `tb1_factor_indicator.jl`, `tb1_linear_narrowed.jl` | KILLED |
| replay | `cl_kth_replay(L, seeds; chain_set_id)` → `(distinct_chains, completed_replays, map_sum_checks, space_sum_ok, map_sum_ok)` through the four queries only | `tb1_space_sum` (exhaustive), `tb2_describe` (chain set `tb2-chi16-directed+rng20(0x9C)`: 36 seeds/map covering all 16 `chi` buckets; distinct chains Point_i 36/36/34/36/36/36, ALine_i 36/36/36/36/35/36, DLine_i 36×6; 108 k-checks/map) | `tb1_replay_skips_k.jl`, `tb1_replay_skips_union.jl` | KILLED |
| memo | `CL_MEMO_LIMIT = 4096`, clear-on-full in `_child`; `memo_report(L)` | `tb1_memo` (10^4 distinct `Linear` prefixes → max 1,808 entries), `tb2_describe` (10^4 at q=2048 on `DLine_6` → max 1,844, 1,916 entries over 53 nodes) | `tb1_memo_unbounded.jl` | KILLED |
description_size (bytes): L_Point 75, L_ALine 132, L_DLine 156; padded-to-3 PCP maps Point_1..6 3009, ALine_1..5 2893, ALine_6 10228, DLine_1..5 2754, DLine_6 10479.
## Runner summary (61/61 KILLED, one process per mutant, baseline-first guard kept)
TB0 22/22: A B C D E F C8 G H I J K L M X1 X1b X2 X3 X4 N O Q. TB1 27/27: M-χ M-π M-lnf M-deg M-level N1 N2 N3 N4 N5 M-concat M-repair M-ambient M-dsum M-kappa NM1 NM2 NM3 N3-space-sum N5-pad-order M-chifree M9-describe-closure M9-factor-not-indicator M9-linear-narrowed-domain M9-replay-skips-k M9-replay-skips-union M9-memo-unbounded. TB2 12/12: c0_plus_one_formula g3_plus_one truncate_line M-guard M-i345 MC1 MC2 MC3 ND2 ND4 N5-tensor M9-pcp-opaque-branch. Existing mutant texts re-anchored: `tb1_ambient.jl` (`BranchConst(child)`), `tb1_pi.jl` (indentation). Mutant logs show harmless `Replacing docs` warnings from `Base.include` of docstring-bearing files.
## CROSS-LANE EDITS
`src/MIPStarLambda.jl` export list only: + `edge_index, AbstractBranch, QuotedBranch, OpaqueBranch, BranchConst, BranchByAxis, BranchLnf, BranchPadded, CL_MEMO_LIMIT, memo_report, Dimension, Marginal, Linear, Factor, cl_kth_replay, CLDescription, NotDescribable, describe_cl, canonical_bytes, description_size, ZERO_MAP_FACTOR_PARTITION, ld_off_line_repair, pcp_question_from_ambient`; − `D_ld`. TB0 files, `docs/**`, `claims/**` untouched.
## MERGE PROPOSALS (verbatim; proposals only — statuses unchanged)
- **C4a**: strike the sentence `**Scope:** the constructed factor-space witness satisfies \`lem:cl-kth\` item 3 but, for \`L_Point\`, not item 2 (\`enu:cl-space-sum\`): its single factor register is \`{1,2}\`, not all of \`{1,...,5}\` (\`verdicts/tb1-r2.md\` N3).` and replace it by `**Scope:**` (the remaining "No claim is made here about …" sentence follows unchanged); where-tested cell → `` `test/tb1_ld_sampler.jl` (`levels`, `chi`, `chifree`, `histogram_axis`, `histogram_diagonal`, `marginals`, `space_sum`, `queries`, `describe`, `memo`); red: `test/mutations/tb1_{chi,pi,lnf,level,repair,ambient,ambient_doblock,child_validation,dsum,concat,space_sum,pad_order,chifree,describe_closure,factor_indicator,linear_narrowed,replay_skips_k,replay_skips_union,memo_unbounded}.jl` ``. Any positive `enu:cl-space-sum` sentence is left for the r3 verdict to authorize.
- **C4b** scope: replace `` `pad_level` prepends its zero-factor stages, so a padded map's marginals `L_{<=j}` for `j<3` are zero rather than the `V_1=V` promotion of `rk:higher-level`/DESIGN §9.4 (`verdicts/tb2-r2.md` N3). `` by `` `pad_level` appends its empty stages, so a padded map's marginals `L_{<=j}` for `j<3` equal the child's, and a register zero map is promoted by `rk:higher-level` (`SOURCE_REPAIR :zero_map_factor_partition`; `verdicts/tb2-r2.md` N3 repaired in brief 46). ``; replace `` The `54^2` type graph is written complete rather than computed as `E^ora x E^pcp`; the two coincide here only because both factor graphs are complete (ibid. N5). `` by `` The `54^2` type graph is computed as the tensor product `E^ora x E^pcp` and equals the complete graph here only because both factor graphs are complete (ibid. N5). ``; replace `` The product is evidenced through … by the critic, not by the suite. `` by `` The decider's questions are the projections of `sample(product, edge, seed)`; the suite asserts their equality with the explicit seed split on 20 seeds x 54 types x both sides and kills the summand-swap mutant `tb2_nd2` (ibid. N1). ``; replace the final sentence by `` No claim is made about any other `(q,m)`; `def:sampler`'s four queries and the `enu:cl-space-sum`/`enu:cl-map-sum` replay are asserted only on the declared chain set `tb2-chi16-directed+rng20(0x9C)` (36 seeds per map, all 16 `chi` buckets), not on unselected reachable chains. ``; where-tested add `` `test/tb2_answer_reduce.jl` (`sampler`, `describe`); red: `test/mutations/tb2_{mc1,nd2,tensor,opaque}.jl` ``.
- **C9** scope: replace `` The questions judged are produced by an explicit seed split … by critic recomputation only (`verdicts/tb2-r2.md` N1). `` by `` The questions judged are the projections of the 54-type product sampler's own `sample` output, asserted equal to the explicit seed split on 20 seeds x 54 types (`verdicts/tb2-r2.md` N1 repaired in brief 46). ``; replace `` The `:TypedAnswerReduce` certificate replays branch reachability and shape, not the checks' verdicts (`verdicts/tb2-r2.md` N2). `` by `` The `:TypedAnswerReduce` certificate replays shape, branch reachability and, for each of seven guard cases at the all-zero seed, one honest accept and one corrupted reject with the expected rule (ibid. N2 repaired). ``; where-tested red list add `nd2,nd4,tensor`.
## DESIGN lockstep proposals (orchestrator lane)
§1.5 after "with functions padded to the common maximum level.": `Padding appends empty stages (\`BranchPadded\`) so every marginal of the child survives; a zero map on a register is promoted with that register as stage 1 under the zero map (\`SOURCE_REPAIR(zero-map-factor-partition)\`, §9.4). Branches are \`QuotedBranch\` values (\`BranchConst\`, \`BranchByAxis\`, \`BranchLnf\`, \`BranchPadded\`) or an \`OpaqueBranch\` host closure; the \`_child\` memo holds at most \`CL_MEMO_LIMIT=4096\` continuations per node.` and in the `L_Point` line add `(single factor space = all of V)`. §1.6 (N4): `The executable decider runs player-outer (alice's steps 2–5, then bob's); the source's "for all w" is step-outer. Verdicts coincide because every rejection is terminal; only the first-reported rule can differ.` §9.3: replace the first paragraph's opening by `\`describe_cl(L)\` serializes one lazy CL value to \`CLDescription\` (canonical term, \`canonical_bytes\`, \`description_size\`) or \`NotDescribable\`; the pair adapter \`describe_cl(LA,LB,q)\` wraps it. \`Dimension\`, \`Marginal\`, \`Linear\` and \`Factor\` exist on \`AbstractCL\` with the §9.1 domains (\`Factor\` enforces \`u ∈ L_{<j}(V)\` by per-stage column-space membership); illegal calls throw \`ArgumentError\`, which the adapter maps to \`QueryError\`.` and replace "This is the remaining adapter work after the lazy-branch repair described by Brief 21." by `The QuotedBranch constructors landed in brief 46; \`direct_sum\`/\`concatenate\` still wrap host closures, so their outputs are \`NotDescribable\` until \`DL9-direct-sum\` answers them at the description level.` §9.4: after the zero-map sentence add `(\`pad_level\` implements this for any register zero map and exposes the node \`ZERO_MAP_FACTOR_PARTITION\`)`.
