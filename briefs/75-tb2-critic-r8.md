# Brief 75 — CRITIC verdict r8 on rung TB2 after repair r7 (brief 73) — closing round

You are the adversarial critic (Opus). ATTACK; do not summarize. Autonomous; no questions. Lane: write `verdicts/tb2-r8.md` ONLY; Julia/files only under scratch; never edit repo files; no state-changing git. Evaluate the ARCHIVED tree at commit `<SHA>` (`git archive <SHA> | tar -x -C <scratch>/tree`; instantiate there). Never read src/test from the live tree; `claims/CLAIMS.md` may be read live.

Scratch: `/tmp/claude-1000/-home-tobias-Projects-discussions/548f633b-c857-457a-bb33-0aa1a7879403/scratchpad/critic-tb2-r8/`

Read: `~/.claude/skills/rk-light/SKILL.md`; `CLAUDE.md`; `claims/CLAIMS.md`; `verdicts/tb2-r7.md` (your prior — the work order); `briefs/73-tb2-repair-r7.md`, `briefs/73-tb2-repair-r7.last.md` (the 19-shape × 3 seeds × 2 orientations block = 114 outcomes, rejecting keys == the 37-key set via the shared `tb2_expected_keys()`, the four survivors registered and KILLED, the C9 proposal minus the copy-index sentence with the flagged under-count clause; NG12 disposition). Note brief 72 (same commit) touched `answer_reduce.jl`/`oracularize.jl` only for CITED-node source facts.

Obligations: run suite (summary + walls + `uptime`) and runner (`MUTATION REGISTRY`; all 157 KILLED; baselines ok). Adjudicate every r7 row. Recompute on a COPY: the 114 outcomes and the 37-key equality with your own enumerator; MH1/MH2/MH3/MH5 KILLED at the printed counts; the seven re-registered evidence lines. Two NEW semantic mutations; survivors MAJOR with the red test as FIX DEMAND. Per-claim: C9 — AUTHORIZED verbatim row (the proposal, with the under-count clause corrected to the 19-case/114-outcome/37-key facts if you wish), C4b re-affirm. This is intended as the closing round: if no MAJOR remains, say PASS and record residuals as MINOR/NOTE.

Output: `verdicts/tb2-r8.md` — adjudication table; recomputations; new objections; runs + walls + load; your mutations; per-claim block; final line exactly `VERDICT: PASS` or `VERDICT: FAIL(<ids>)`.
